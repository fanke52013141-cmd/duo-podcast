# 双声播客工坊 DuoCast · 页面详细设计文档

> 版本 v3.0 ｜ 依据：《详细优化方案》（04-详细优化方案.md）｜ 载体：本地 Web 工作台（SPA）｜ 运行：Windows 本地 ｜ 文档粒度：页面 / 布局 / 元素 / 交互 / 校验 / 数据契约
>
> **本文档的写作目标**：把每个页面的布局、元素、交互、状态、校验、数据流写到「可以直接据此生成页面」的粒度。算法与实施细节的权威定义在 02-项目实施方案文档；本文涉及处只写页面语义并引用。

---

## 目录

- [0. 产品定义与核心机制](#0-产品定义与核心机制)
- [1. 全局框架与公共组件](#1-全局框架与公共组件)
- [2. 工程首页与节目模板](#2-工程首页与节目模板)
- [3. 阶段一 · 创建本期](#3-阶段一--创建本期)
- [4. 阶段二 · 编辑对话](#4-阶段二--编辑对话)
- [5. 阶段三 · 试听配音](#5-阶段三--试听配音)
- [6. 阶段四 · 预览画面](#6-阶段四--预览画面)
- [7. 阶段五 · 生成导出](#7-阶段五--生成导出)
- [8. 资产库](#8-资产库)
- [9. 服务设置](#9-服务设置)
- [10. 任务中心](#10-任务中心)
- [11. 数据模型](#11-数据模型)
- [12. 本地接口契约](#12-本地接口契约)
- [13. 状态机与全局交互规则](#13-状态机与全局交互规则)
- [附录 A：页面清单速查](#附录-a页面清单速查)
- [附录 B：术语表](#附录-b术语表)

---

## 0. 产品定义与核心机制

### 0.1 产品定位

| 项 | 定义 |
|---|---|
| **一句话** | 面向 Windows 本地单用户的双人播客视频制作工作台，编排「创建本期 → 编辑对话 → 试听配音 → 预览画面 → 生成导出」五个阶段，底层为文本 / 图片 API 适配器、本地语音（默认）或 MiniMax、本地 ComfyUI 口型视频与 ffmpeg 后处理 |
| **用户** | 单人创作者（同时是操作者），无登录、无多用户 |
| **产出** | ≤5 分钟（含片头片尾与首尾停顿）的双人对谈视频；横屏 16:9 默认，竖屏版式导出可选 |
| **数据边界** | 工程与成片保存在本机；文本与图片请求发送到所选 API；MiniMax 启用时发送相应语音输入；其余处理全部本地 |
| **核心角色** | 两位角色，A / B 均可替换；角色资产（形象、性格描述、声音绑定）可跨期复用 |
| **形态** | 单页应用，页内视图切换（非路由跳转）；左阶段轨 + 中央主视图 + 右检查器 + 底部时间轨 |
| **对话约束** | 一句一个说话人；首版不开放同时说话或抢话 |

### 0.2 与 v2.1 方案的差异

| 维度 | v2.1 | v3.0（本方案） | 原因 |
|---|---|---|---|
| 页面结构 | P0–P9 十步 | **五阶段 + 独立资产库 / 服务设置 / 任务中心** | 角色库不再是每期必经；服务与任务独立成层 |
| 确认关卡 | 6 个 CP | **3 个默认确认点**（脚本 / 配音 / 样片）+ 3 个可选（角色 / 构图 / 镜头） | 减少打断；可选关卡满足谨慎用户 |
| 时长上限 | ≤10 分钟 | **≤5 分钟**，自动时长为默认 | 按 5 分钟设计上限；5 分钟是上限不是填满目标 |
| 数据边界 | 全程不出本机 | **文本 / 图片走 API**，工程与成片本地 | 移除本地 LLM 与图像模型部署负担 |
| 稿件流程 | 散文稿 → 分角 | **先内容结构、再直接生成双人对话** | 减少一步；对话从结构直接生成，话轮更自然 |
| 合成粒度 | 说话人连续段（run） | **句 / 发言 / 合成单元三层** | 「更自然」与「更容易单句改动」显式取舍；按情绪 / 语速 / 停顿 / 长度拆分 |
| 停顿模型 | pre + post 双重编辑 | **单一 `transitionGapMs`** + `leadInMs` / `tailOutMs` | 消除双重归属歧义 |
| 时间权威 | 最后一句时间码 | **最终 PCM 音轨样本数** | 时间码是对齐产物，样本数才是权威 |
| 双轨输入 | 等长双轨 + 固定 `add` | **工作流适配层**保证不二次拼接 | 官方 `add` 实为串行拼接，旧假设不成立 |
| 分段与镜头 | 切镜影响分段 | **解耦**（output / render / context 三区间） | 改镜头不重跑基础视频，省推理 |
| 页面状态 | done / stale 布尔 | **产物版本 + 任务状态七态** | 布尔无法表达「版本已过期但仍可播放」等状态 |
| 参数暴露 | 全暴露 | **基础 / 节目预设 / 高级三层** | 降低默认路径复杂度 |

### 0.3 三个核心机制

#### 机制一：版本驱动的可调性

**设计原则：编辑产生草稿，生成产生候选，「采用」更新当前使用版本；页面状态由产物版本与任务状态推导，不手写完成标记。**

| 能力 | 说明 | UI 体现 |
|---|---|---|
| **局部重跑** | 只重跑受影响的最小单元，按重跑矩阵传播 | 改一句 → 只重合成该句所在合成单元；改裁切 → 只重跑镜头后处理，不触发 TTS 与基础视频 |
| **草稿 / 候选 / 已用** | 三种版本身份显式区分 | 产物卡片版本徽章：`草稿` / `候选 n` / `已用`；新候选默认不覆盖已用版本 |
| **版本可对比** | 保留历史版本，可并排对比后采用 | 版本选择器 + 「对比」模式 |
| **过期提示** | 上游采用新版本后，下游产物标「已过期」（仍可播放 / 查看） | 橙色「已过期」角标 + 显示其依赖的上游版本 |
| **快照隔离** | 生成任务冻结输入快照；后台运行中编辑不干扰 | 任务卡片显示「生成中 · 基于版本 R7」 |

**重跑矩阵（权威定义见 02 §7.4，此处为页面语义摘要）**：

| 改动 | 音频合成 | 时间轨/标注 | 基础视频 | 后处理 |
|---|---|---|---|---|
| 改台词的实际读法 | 对应单元 | 对应标注及后续偏移 | 变化区间及其上下文依赖 | 相关输出 |
| 只改字幕标点 | 否 | 字幕文本版本 | 否 | SRT 或烧录字幕 |
| 改显式发言间隔 | 否 | 重排 | 实际输入变化的段及依赖 | 是 |
| 改某角色的声音 | 该角色全部单元 | 重新建立相关偏移 | 音频变化区间及依赖 | 是 |
| 只改时间标注 | 否 | 是 | 否 | 镜头与字幕相关部分 |
| 改底图 / 人物区域 | 否 | 否 | 对应视觉版本的段 | 是 |
| 改数字推拉 / 裁切 | 否 | 否 | 否 | 镜头处理及导出 |
| 开关烧录字幕 | 否 | 否 | 否 | 字幕与编码 |
| 改编码规格 | 否 | 否 | 通常否 | 编码或版式 |

#### 机制二：确认点（Approval）

**设计原则：确认是「对明确的输入版本与规格的接受」，不是全局布尔值。三个默认确认点 + 三个可选关卡。**

| 确认点 | 挂载 | 默认 | 确认内容 | 记录字段 |
|---|---|---|---|---|
| **脚本** | 阶段②末 | 开 | 话轮内容、说话人分配、预计时长 | 脚本版本 ID |
| **配音** | 阶段③末 | 开 | 音色、读音、停顿、总时长 ≤300s | 音轨版本 ID + 总时长 |
| **样片** | 阶段④末 | 开 | 口型、身份、边界连续、耗时 | 样片资产 ID + 工作流版本 + 规格区间 |
| 角色 | 阶段①内 | 关 | 角色形象与声音绑定 | 各绑定版本 |
| 构图 | 阶段④内 | 关 | 同框底图与人物区域 | 视觉变体版本 |
| 镜头 | 阶段④内 | 关 | 镜头计划 | 镜头计划版本 |

- 每个确认点可配置为「必须确认 / 自动通过 / 跳过」，配置存于工程（节目模板可预置）
- 自动模式下只在开启的确认点暂停，弹出确认面板；通过 / 打回均写入工程日志
- **打回必须选择原因指向**（脚本 / 配音 / 构图），用于导航回对应阶段

#### 机制三：分层参数

**设计原则：基础选项覆盖默认路径，节目预设沉淀复用配置，高级参数折叠收起。**

| 层 | 内容 | 入口 |
|---|---|---|
| 基础 | 时长偏好、比例、角色、风格、镜头预设 | 各阶段主视图 |
| 节目预设 | 模板保存的对话风格、场景、声音绑定、镜头风格、导出规格、确认策略 | 工程首页「节目模板」 |
| 高级 | 引擎参数、采样、种子、编码、混用语音引擎入口 | 检查器「高级」折叠区（默认收起） |

### 0.4 完整业务流程

```
┌────────────────────────────────────────────────────────────────┐
│ 工程首页：新建本期（选模板或空白）│ 复用素材或图片 API 制作角色与场景 │
└──────────────┬─────────────────────────────────┬───────────────┘
               ▼                                 ▼
┌──────────────────────────────┐   ┌──────────────────────────────┐
│ ① 创建本期                    │   │ 资产准备（与内容并行，独立保存）│
│ 话题/文章/已有脚本 → 提纲 →    │   │ 角色图、场景、同框底图候选      │
│ 双人对话（文本 API，两次生成）  │   │ 人物区域标注                  │
└──────────────┬───────────────┘   └──────────────┬───────────────┘
               ▼                                  │
┌──────────────────────────────┐                  │
│ ② 编辑对话                    │                  │
│ 话轮编辑 / 选区改写 / 读音词典  │                  │
│        [确认点·脚本]          │                  │
└──────────────┬───────────────┘                  │
               ▼                                  │
┌──────────────────────────────┐                  │
│ ③ 试听配音                    │                  │
│ 绑音色试听 → 按单元合成 →      │                  │
│ 校正读音与停顿 → 真实时长      │                  │
│ 超 300s → 精简建议 → 回②      │                  │
│        [确认点·配音]          │                  │
└──────────────┬───────────────┘                  │
               ▼                                  ▼
┌───────────────────────────────────────────────────────────────┐
│ ④ 预览画面（构图 │ 镜头 │ 样片 三区）                             │
│ 同框底图 + 人物区域 → 轻量预览 → 10–20s 代表样片 + 边界预览      │
│ 不满意 → 调整配置或查看替代形式样片 → 回本阶段重试                │
│        [确认点·样片]                                          │
└──────────────┬────────────────────────────────────────────────┘
               ▼
┌───────────────────────────────────────────────────────────────┐
│ ⑤ 生成导出                                                    │
│ 摘要（复用/待生成/预估/磁盘）→ 分段渲染 → 拼接 → 镜头 → 字幕 →    │
│ 音画合并 → 成片检查（定位问题局部重跑）→ 导出 + 保存节目模板       │
└───────────────────────────────────────────────────────────────┘
   全程：资产库 / 服务设置 / 任务中心 为独立入口，可随时进出
```

### 0.5 全局设计语言（沿用 v2.1）

#### 色彩语义

| 用途 | 颜色 | 值 |
|---|---|---|
| 角色 A | 青蓝 | `#0e7490` / 浅底 `#e2f2f6` |
| 角色 B | 珊瑚粉 | `#be4d6e` / 浅底 `#fbe9ef` |
| 品牌 / 主操作 | 焦糖 | `#a96a2c` / `#f7ede2` |
| 成功 / 已就绪 | 绿 | `#2f9e6e` / `#e4f5ed` |
| 警告 / 待确认 / 已过期 | 橙 | `#b97e16` / `#fbf1dc` |
| 错误 / 删除 | 红 | `#c75052` / `#fcebeb` |
| 中性文字 | 三级灰 | `#1f2430` / `#4d5666` / `#858f9f` |

> **无障碍**：颜色不作为唯一信息载体，A/B 与所有状态同时带文字标签。

#### 字体 / 间距 / 断点

- 正文 `"Segoe UI", "Microsoft YaHei UI"` 14px / 1.6；时间码与参数值 `"Cascadia Code"` 12px
- 栅格 4px；圆角：按钮 8px、卡片 12px、弹窗 16px
- 断点：≥1440 完整四区；1024–1439 检查器折叠为抽屉；<768 单栏（仅保证可查看）

---

## 1. 全局框架与公共组件

### 1.1 布局结构

```
┌──────────────────────────────────────────────────────────────────────┐
│ TopBar  56px  工程名 │ 服务状态组 │ 任务入口 │ 保存 │ 导出            │
├──────────┬───────────────────────────────────┬───────────────────────┤
│ Sidebar  │        Stage（主视图）             │   Inspector           │
│  208px   │        阶段视图区                  │   320px               │
│          │                                   │   上下文参数          │
│ 5 个阶段  │                                   │   （基础+高级折叠）    │
│ ──────── │                                   │                       │
│ 3 个独立  │                                   │                       │
│ 入口      │                                   │                       │
├──────────┴───────────────────────────────────┴───────────────────────┤
│ TimelineBar  音频确认后常驻，可折叠（56 / 28px）                       │
├──────────────────────────────────────────────────────────────────────┤
│ FootBar  48px  阶段 n / 5 · 名称 │ 上一步 │ 确认点状态 │ 下一步        │
└──────────────────────────────────────────────────────────────────────┘
```

### 1.2 TopBar

高度 56px，吸顶。

| 元素 | 类型 | 内容 | 交互与校验 |
|---|---|---|---|
| 产品标识 | 图标 + 文字 | 「双声播客工坊」 | 点击 → 返回工程首页；有未保存改动时弹确认 |
| 工程名 | 可编辑文本 | 当前工程名 | 单击编辑，失焦保存；空值回退「未命名工程」 |
| **服务状态组** | 状态标签 ×4 | `文本 API` / `图片 API` / `本地引擎` / `MiniMax` | 状态三档：未配置（灰）/ 已连接（绿）/ 异常（红，悬停显示错误与「去设置」）；**某一服务离线只禁用依赖它的操作**，编辑与播放已有素材不受影响 |
| 任务入口 | 图标按钮 + 徽章 | 后台任务数 | 点击打开任务中心；有任务时徽章显示数量，运行中转圈 |
| 显存条 | 进度条 | `9.2 / 16.0 GB` | 只读；≥90% 转橙 |
| 保存状态 | 文本 | 「已保存」/「保存中…」/「有未保存改动」 | 2s 防抖自动保存 |
| 导出 | 次级按钮 | 快捷入口 | 有成片时可点，跳转阶段⑤导出区 |

> 与 v2.1 的差异：删除「全本地运行」徽章与 LLM 服务状态；模型芯片组并入服务状态组的本地引擎（悬停展开模型清单）；新增任务入口。

### 1.3 Sidebar（左侧阶段轨）

宽度 208px，吸顶，独立滚动。

| 区块 | 内容 | 交互 |
|---|---|---|
| **阶段项 ×5** | ① 创建本期 / ② 编辑对话 / ③ 试听配音 / ④ 预览画面 / ⑤ 生成导出 | 点击切换；当前=焦糖左竖条；状态徽章见 13.1 七态 |
| 分隔线 | — | — |
| **独立入口 ×3** | 资产库 / 服务设置 / 任务中心 | 点击以抽屉或全屏视图打开，不占阶段位 |
| 底注 | 「数据边界：工程与成片本地；文本/图片经所选 API」 | 只读 |

每阶段副标题：① 话题与参数 ② 话轮与读音 ③ 音色与时长 ④ 构图镜头样片 ⑤ 渲染与导出。

### 1.4 FootBar

| 元素 | 类型 | 交互 |
|---|---|---|
| 位置提示 | 文本 | 「阶段 3 / 5 · 试听配音」 |
| 上一步 | 次级按钮 | `goStage(i-1)`；第 1 阶段禁用 |
| 确认点状态 | 徽章 | 「本阶段无确认点」/「脚本待确认」/「已确认 · R7」；点击弹出确认面板 |
| 下一步 | 主按钮 | **仅导航，不写入任何完成状态**；前置产物缺失时目标页顶部显示橙色缺失提示 |

### 1.5 TimelineBar（底部时间轨）

**配音确认后展开常驻**，可折叠，状态存工程。

```
┌────────────────────────────────────────────────────────────────────┐
│ ▸ ⏮ ▶ ⏭   00:12.480 / 02:34.120   缩放 ————●————   [48kHz · PCM] │
│ A轨 ▓▓▓▓░░░░░░▓▓▓▓▓▓░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  │
│ B轨 ░░░░░▓▓▓▓▓▓▓▓░░░░░░▓▓▓▓░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  │
│ 发言轨 [T01][T02][T03][T04]…    样片区间标记 ◄════►                │
└────────────────────────────────────────────────────────────────────┘
```

| 元素 | 交互 |
|---|---|
| A / B 轨 | 色块 = 发言单元实际波形区间；悬停显示发言 ID、文本摘要、单元时长；点击定位播放头 |
| 发言轨 | 每个 Turn 一格，双击跳转编辑对话中该发言 |
| 样片标记 | 显示样片覆盖区间（outputRange）；可拖动调整 |
| 播放控制 | `Space` / `←` `→` 句级跳转 |
| 规格说明 | 右侧固定显示「48kHz · PCM · N=样本数」，总时长由样本数导出 |

> 时间显示统一从 PCM 样本数换算（见 02 §5.3），前端不做独立时长估算。

### 1.6 公共组件

#### 1.6.1 卡片 Card

- 标题行：标题 + 版本徽章（`草稿` / `候选 n` / `已用 v3`）+ `⋯` 菜单（重新生成 / 查看历史版本 / 复制参数 / 导出产物）
- 状态徽章七态见 13.1

#### 1.6.2 上传槽 UploadSlot

- 空态：虚线框 + 主提示 + 规格副提示；点击选文件，支持拖拽
- 有内容：缩略图 + 文件名浮标 + 「替换 / 移除」
- 上传文件经 `/api/projects/{id}/assets` 复制进工程目录，返回相对路径；**不把二进制塞进 state**

#### 1.6.3 版本选择器 VersionPicker

- 「v3 ▾」展开历史：版本号、时间、关键参数摘要、缩略图
- 候选版本提供「采用」与「对比」；采用后下游按重跑矩阵标过期

#### 1.6.4 参数控件 ParameterField

- `标签 + 控件 + 当前值（等宽） + ?`；`?` 悬停显示含义、范围、影响
- **能力门控**：仅显示所选引擎 / 服务声明支持的选项；不支持但有明确降级解释的选项显示降级说明；未支持的强度等选项直接隐藏

#### 1.6.5 确认面板 ApprovalPanel

```
┌─────────────────────────────────────────────────┐
│  ✓ 确认 · 配音                     [×]          │
├─────────────────────────────────────────────────┤
│  将确认音轨版本 R9（总时长 2:48.120 ≤ 5:00）     │
│  ☑ 所有使用中的音频有效                           │
│  ☑ 必要读音校正已处理（2 处）                     │
│  ☑ 试听通过                                      │
├─────────────────────────────────────────────────┤
│  [打回并说明原因…]              [确认通过 →]      │
└─────────────────────────────────────────────────┘
```

- 全部勾选才能通过；确认写入 Approval 对象（类型 + 输入版本 + 规格）
- 打回需选择原因指向（脚本 / 配音 / 构图），写入日志并导航

#### 1.6.6 Toast

- 底部居中，2.2s 消失；成功 / 警告 / 错误（带「查看详情」→ 任务中心）；不阻断

---

## 2. 工程首页与节目模板

> 入口：启动工作台；任意制作页的「返回工程」。

### 2.1 布局

```
┌────────────────────────────────────────────────────────────────────┐
│  双声播客工坊                              [新建本期] [导入工程]    │
├────────────────────────────────────────────────────────────────────┤
│  最近工程                                                           │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                           │
│  │ EP003     │ │ EP002    │ │ EP001    │   [打开工程目录]          │
│  │ A·李雷 B·  │ │ 5:12·渲染 │ │ 草稿     │                          │
│  │ 阶段④     │ │  中(2/9) │ │ 阶段③    │                          │
│  │ ⚙后台任务2 │ │          │ │          │                          │
│  │ 09-10     │ │ 09-08    │ │ 09-05    │                          │
│  └──────────┘ └──────────┘ └──────────┘                           │
├────────────────────────────────────────────────────────────────────┤
│  节目模板                                          [保存为模板]    │
│  ┌────────┐ ┌────────┐ ┌────────┐                                 │
│  │科技周报 │ │人物访谈 │ │ + 新建 │                                 │
│  └────────┘ └────────┘ └────────┘                                 │
└────────────────────────────────────────────────────────────────────┘
```

### 2.2 元素明细

| 元素 | 类型 | 功能 | 交互与校验 |
|---|---|---|---|
| 新建本期 | 主按钮 | 创建新工程 | 选择节目模板或空白工程 → 进入阶段① |
| 工程卡片 | 卡片 | 打开工程 | 显示：名称、双方角色、最后编辑时间、**当前阶段**、**最新成片版本**、**是否仍有后台任务**（橙色徽章，点击直达任务中心） |
| 继续制作 | 卡片内主操作 | 进入上次工作位置 | 若后台任务仍在执行，同时展示任务入口，**不重复提交** |
| 节目模板卡 | 卡片 | 用模板新建 | 模板保存：角色搭配、对话风格、场景、声音绑定、镜头风格、导出规格、确认策略 |
| 保存为模板 | 次级按钮 | 从当前工程沉淀模板 | 只复制**可复用配置与已选素材引用**；不携带本期台词、时间码、任务状态、旧验收结果 |
| 复制工程 | `⋯` 菜单 | 建立新工程身份 | 素材可复制复用；**进行中的任务不复制** |
| 导入 / 打开目录 | 次级按钮 | zip 导入 / 资源管理器 | 导入校验目录结构与 schemaVersion，不匹配提示迁移 |

### 2.3 空工程入口

空工程提供三种起步方式（也在阶段①重复出现）：

| 入口 | 说明 |
|---|---|
| 从话题开始 | 输入话题，走完整生成链路 |
| 粘贴文章 | 以文章为素材生成 |
| 使用已有脚本 | 导入已有双人稿，仅做结构解析与校验，**不默认改写**（首版低成本能力，不强制再生成） |

---

## 3. 阶段一 · 创建本期

> 入口：新建工程；制作页切换到第一阶段。

### 3.1 布局

```
┌────────────────────────────────────────────────────────────────────┐
│ ① 创建本期                                                         │
├──────────────────────────┬─────────────────────────────────────────┤
│ 内容输入                  │ 内容要求（模板提供默认值）                 │
│ ○ 从话题开始              │ 目标听众 [____________]                  │
│ ● 粘贴文章                │ 讨论重点 [____________]                  │
│ ○ 使用已有脚本            │ 双方职责  A[主持]  B[嘉宾]               │
│ ┌──────────────────────┐ │ 风格     [轻松对谈 ▾]                   │
│ │ 粘贴文章或输入话题…    │ ├─────────────────────────────────────────┤
│ │                      │ │ 时长  ● 根据内容，最多 5 分钟            │
│ │                      │ │      ○ 大约 1 / 2 / 3 / 4 分钟          │
│ └──────────────────────┘ │ 比例  ● 横屏 16:9   ○ 竖屏               │
│ 字数：1,842              │ 角色  A [资产库选择▾]  B [资产库选择▾]     │
│                          │      （可换，模板可锁定）                 │
│ [生成对话]               ├─────────────────────────────────────────┤
│                          │ 视觉准备（可延后）                        │
│                          │ [资产抽屉：角色图/场景/上传同框底图]       │
└──────────────────────────┴─────────────────────────────────────────┘
```

### 3.2 元素明细

| 元素 | 类型 | 默认 / 取值 | 功能 | 交互与校验 |
|---|---|---|---|---|
| 起步方式 | 单选组 | 粘贴文章（按入口带入） | 决定后续生成路径 | 切换保留已输入内容；已有脚本模式不出现「生成对话」而出现「解析脚本」 |
| 内容输入框 | 多行文本 | 空 | 话题 / 文章 | 文章 ≤8000 字，超出橙色提示；话题建议 ≤200 字 |
| 目标听众 / 讨论重点 | 文本 | 模板默认值 | 内容结构生成的输入 | 可空 |
| 双方职责 | 文本 ×2 | 模板默认值 | 生成话轮时的角色意图约束 | 可空 |
| 风格 | 下拉 | 模板默认（无模板时「轻松对谈」） | 语气与节奏 | — |
| **时长设置** | 单选 | **根据内容，最多 5 分钟**（默认）/ 大约 1 / 2 / 3 / 4 分钟 | 上限 300 秒 | 「大约 N 分钟」是偏好不是承诺，不用字符数承诺精确时长 |
| **比例** | 单选 | **横屏 16:9（默认）** / 竖屏 | 交付比例 | 选竖屏时显示双人构图建议与「构图需独立检查」提示；不承诺横屏素材无损裁成竖屏 |
| 角色选择 | 下拉 ×2 | 从资产库 / 新建 | A、B 都可替换，**不把 A 永久固定为主持人** | 模板可锁定（锁定态显示 🔒 与解锁入口）；角色未就绪不阻塞写稿 |
| 生成对话 | 主按钮 | — | 触发两次生成（见 3.3） | 必须有有效输入**及可用文本 API**；API 不可用置灰并显示「去服务设置」；**角色形象与声音未就绪不阻塞** |
| 视觉准备 | 折叠区 | 收起 | 提前进入资产抽屉选角色图、场景或上传同框底图 | **与内容工作独立保存**，可随时回来；不做强制顺序 |

### 3.3 生成流程（两次生成）

```
第一次：内容结构（核心问题、主要观点、必要事实、段落目的、双方职责）
        简单输入（纯话题）可自动通过，不增加必点页面
第二次：结构化话轮（稳定 ID、说话人、显示文本、朗读文本、意图、语气、来源引用）
```

- 角色意图可为解释 / 追问 / 举例 / 质疑 / 总结——**不把所有 B 台词写成「对，你说得对」**
- 已有双人稿只做结构解析与校验，不默认改写
- 文章中的事实与模型补充的例子分开标识；来源锚点由程序按原文位置核对，不接受不存在的引用

### 3.4 完成条件与下游

| 项 | 说明 |
|---|---|
| 完成条件 | 已有确认的脚本版本（生成 → 编辑 → 确认脚本均在阶段②收口）；本阶段无确认点 |
| 下游 | 脚本版本 → 阶段②③④⑤；视觉准备产物 → 阶段④ |
| 失效规则 | 改起步输入 → 生成新候选脚本，不影响已确认版本直到重新采用 |

---

## 4. 阶段二 · 编辑对话

> 入口：首次生成完成；导入脚本完成；点击台词问题（来自配音或成片检查）进入。

### 4.1 布局

```
┌────────────────────────────────────────────────────────────────────┐
│ ② 编辑对话        草稿 R8（未确认）   [重新生成] [版本▾] [确认脚本]  │
├──────────────────────────────────────────┬─────────────────────────┤
│ 对话编辑器（话轮列表）                     │ 右侧面板                 │
│ ┌──────────────────────────────────────┐ │ ┌ 内容结构 ┐             │
│ │ #T07  B · 嘉宾              [追问▾]  │ │ │ 核心问题…│             │
│ │ 「我倒觉得关键在于成本结构…」          │ │ │ 观点…    │             │
│ │ 语气:质疑  语速:1.0  ⚠读音:3处       │ │ └──────────┘             │
│ ├──────────────────────────────────────┤ │ ┌ 所选发言属性 ┐         │
│ │ #T08  A · 主持人            [总结▾]  │ │ │ 说话人/意图/语气│       │
│ │ 「所以你的意思是…」                  │ │ │ 语速/朗读文本 │        │
│ └──────────────────────────────────────┘ │ └──────────────┘         │
│ [＋ 发言]  拖动排序                       │ ┌ 质量提示 ┐             │
│ 选区工具：改写 / 更口语化 / 增加追问 /     │ │ ⚠ B 连续三次重复│      │
│ 减少重复 / 精简至目标时长                 │ │   A 的结论      │      │
│                                          │ └──────────────┘         │
├──────────────────────────────────────────┴─────────────────────────┤
│ 统计：话轮 32 · A 17 / B 15 · 字数 1,842 · 预估 3:12（基于历史语速） │
└────────────────────────────────────────────────────────────────────┘
```

> **技术分层对用户的呈现**：句子（Line）、发言（Turn）、合成单元（Unit）三层在数据层存在；**用户默认编辑自然的话轮（Turn）**，句子与单元由系统派生并显示在需要的位置。

### 4.2 元素明细

| 操作 | 类型 | 成功结果与约束 |
|---|---|---|
| 编辑台词 | 行内编辑 | 自动保存新草稿；**已有配音保持可听**，并标明「不对应当前文字」 |
| 增删、拖动发言 | 列表操作 | 更新顺序；**稳定 ID（T07）不随显示序号改变** |
| 切换说话人 | 下拉 | 更新声音绑定与合成单元划分；显示涉及哪些现有配音（列出发言 ID） |
| 改写选中内容 | 选区操作 | 调文本 API 返回**候选改写及差异**，用户接受后替换；其他发言不自动重写 |
| 更口语化 / 增加追问 / 减少重复 | 选区操作 | 保留原来观点和上下文角色关系 |
| 精简至目标时长 | 选区操作 | **先给出删减建议，接受后才更新**；配音后仍以实长再次检查 |
| 情绪与语速 | 发言属性 | 默认自然语气；**只显示所选语音引擎能执行或已有明确降级解释的选项** |
| 读音词典 | 全局面板 | 维护专名、数字、缩写的读法；**显示文本与朗读文本分别保存**（displayText / spokenText） |
| 确认脚本 | 主按钮 | 绑定当前脚本版本；通过后可自动启动配音（可在模板关闭） |

### 4.3 质量提示

- 采用**具体问题**措辞：「B 连续三次重复 A 的结论」「这一段缺少来源」「连续独白较长」
- 可确定的长度和占比由本地程序计算；**语义判断由文本 API 提供建议，不作为事实保证**（UI 标注「AI 建议」）

### 4.4 流式与失败处理

| 情况 | 行为 |
|---|---|
| API 流式中断 | 部分内容作为草稿保留；**未完成 JSON 不作为有效脚本** |
| 结构无效 | 进行一次受限修复；仍失败则保留原输出与现有稿件，提供重试与手工导入 |

---

## 5. 阶段三 · 试听配音

> 入口：脚本确认；点击现有音频；视频检查发现读音问题。

### 5.1 布局

```
┌────────────────────────────────────────────────────────────────────┐
│ ③ 试听配音    音轨版本 R9 · 02:48.120     [确认配音]                │
├──────────────────────────────────────────┬─────────────────────────┤
│ 语音引擎                                   │ 检查器                   │
│ ○ 本地引擎（默认）  ● MiniMax             │ ┌ 发言 T07 ┐            │
│ A 音色 [voice_A ▾] [试听]                 │ │ 单元 U12 │            │
│ B 音色 [voice_B ▾] [试听]                 │ │ 引擎/时长│            │
│ [生成整期配音]                            │ │ [重新生成]│           │
├──────────────────────────────────────────┤ │ [拆出一句]│           │
│ 配音清单（按自然发言）                     │ │ [新旧对比]│           │
│ ┌──────────────────────────────────────┐ │ └──────────┘            │
│ │T07 B 「我认为…」 ✓ 12.4s ⚠读音       │ ├─────────────────────────┤
│ │T08 A 「所以…」    ✓  6.1s            │ │ 间隔调整                │
│ │T09 B ⏳ 生成中 3/9                   │ │ T07 ⟷ T08 [480ms] ⟷   │
│ └──────────────────────────────────────┘ │ 拖动改变 transitionGap  │
│ [▶ 连续试听] [独听A] [独听B]              ├─────────────────────────┤
│                                          │ 高级（折叠）             │
├──────────────────────────────────────────┤ · 引擎参数 / 对齐设置     │
│ 时长检查：02:48.120 / 05:00 ✓            │ · 混用语音引擎入口       │
└──────────────────────────────────────────┴─────────────────────────┘
```

### 5.2 三层对象与单元划分

| 层 | 用途 | 用户可见性 |
|---|---|---|
| 句子 Line | 编辑、字幕、读音修正 | 读音词典、字幕粒度 |
| 发言 Turn | 一次自然说话行为，承载角色与对话意图 | **默认编辑粒度**（配音清单行） |
| 合成单元 Unit | 实际送给一个语音引擎的一次请求，可覆盖多个句子 | 检查器、重新生成范围提示 |

**单元划分规则（权威定义见 02 §5.1）**：按说话人、语音提供方、模型、音色、明显情绪变化、语速变化、显式精确停顿与最大长度拆分；相邻同角色且参数一致的短句允许合并。

> 产品语义：默认以自然话轮为单位，允许用户拆分；**不宣称整段合成必定比逐句更保音色**。

### 5.3 元素明细

| 元素 | 类型 | 规则 |
|---|---|---|
| 语音引擎 | 单选 | 默认本地；可切 MiniMax。**先为双方绑定音色并做短试听，再合成整期** |
| 音色选择 | 下拉 + 试听 | 按引擎能力渲染（本地参考音 / MiniMax 系统音色 / 已有 voice_id）；切换引擎要求重新试听 |
| 配音清单 | 列表 | 按自然发言显示：说话人、文本、生成状态、**真实时长**、读音或对齐问题 |
| 连续试听 | 播放器 | 播放完整混音；可独听 A / B；点击台词跳到对应位置 |
| 重新生成发言段 | 按钮 | **默认只重生成对应单元**；相邻句受影响时直接显示范围（列出受影响单元 ID） |
| 拆出一句 | 按钮 | 把当前句变为独立合成单元再重新生成；提示「不承诺与原段完全一致的音色表现」 |
| 新旧对比 | 按钮 | **新结果先进入候选，用户采用后替换当前版本；失败不删除旧结果** |
| 调整间隔 | 拖动 | 拖动两次发言之间的停顿 → 重排时间轴，**通常不重新合成声音**（只改 `transitionGapMs`） |
| 改句内精确停顿 | 按钮 | 引擎支持时使用已验证语法；否则拆分单元后本地插入静音；显式间隔是**额外静音**（界面说明与模型自然停顿的关系） |
| 确认配音 | 主按钮 | 条件：所有使用中的音频有效、必要校正已处理、总时长未超上限；**生成新的音轨版本** |

### 5.4 时长超限处理（>300 秒）

- **保留音频并显示超出量**（如 `05:12.480 / 超出 12.5s`）
- 提供操作：「精简某段」「删除某段」「调整语速后试听」
- **不自动截尾、不强行倍速**
- 配音时长包括片头片尾发言及显式首尾停顿；后续新增片尾也要重新检查总时长

### 5.5 对齐与质量（页面语义）

- 优先使用提供方时间信息；缺失或不可靠时转本地对齐；**页面显示真实使用的方法**（「提供方时间戳」/「本地强制对齐」）
- 质量字段：文本覆盖率、无法匹配的词、时间范围异常、待人工处理片段——低质量句在配音清单标 ⚠，可定位、试听、人工修正
- 手工修改时间码**只更新标注，不改变实际音频**

---

## 6. 阶段四 · 预览画面

> 入口：配音确认；准备角色与场景；调整构图或镜头。
> 页面包含**「构图」「镜头」「样片」三个页内区域**（页内 Tab）。未有配音时允许准备构图，但**不能生成真实口型样片**。

### 6.1 布局

```
┌────────────────────────────────────────────────────────────────────┐
│ ④ 预览画面    [构图] [镜头] [样片]         视觉变体 V3 · 已用       │
├──────────────────────────────────────────┬─────────────────────────┤
│ 构图区                                     │ 检查器                   │
│ ┌────────────────────────┐               │ ┌ 底图来源 ┐             │
│ │        同框底图         │               │ │ [生成] [上传]│         │
│ │   ┌──A区域──┐┌──B区域─┐│               │ └──────────┘            │
│ │   │  矩形框  ││ 矩形框 ││  [生成同框底图] │ ┌ 人物区域 ┐            │
│ │   └─────────┘└───────┘│               │ │ 矩形框标注 │           │
│ └────────────────────────┘               │ └──────────┘            │
│ 场景 [资产库/生成/上传]                    │ ┌ 坐标信息 ┐            │
│ 角色 A [资产库] 角色 B [资产库]            │ │ 原图/规范/输入│        │
│ 竖屏提示：构图需独立检查                   │ └──────────┘            │
├──────────────────────────────────────────┴─────────────────────────┤
│ 镜头区：镜头预设 [稳定同框 ▾]  ☐轻微推拉 ☐发言人裁切                │
│ 轻量预览 ▶（构图预览，无动态口型）                                     │
├────────────────────────────────────────────────────────────────────┤
│ 样片区：区间 [01:12–01:28 · A→B→A] [调整] [生成样片]                 │
│ [▶ 边界预览]  检查：☐错误开口 ☐口型同步 ☐角色一致 ☐边界连续 ☐声音清晰│
└────────────────────────────────────────────────────────────────────┘
```

### 6.2 构图区

| 元素 | 规则 |
|---|---|
| 角色图与场景 | 从资产库选择、上传或通过图片 API 生成；**不要求每期重新生成** |
| 生成同框底图 | 明确引用 A/B 身份图、场景、目标比例；**候选图需由人检查身份和遮挡**；首选支持多参考图的编辑接口，不支持时提供「上传已完成同框图」或「文字生成 + 人工检查」，**不默默丢掉人物参考图** |
| 人物区域 | 在**最终送入视频工作流的图**上标注 A/B 区域；**矩形框为首版**，精细笔刷后置 |
| 坐标显示 | 三类坐标（原始图 / 规范化人物区域 / 模型实际输入）与确定变换；底图和 mask 使用同一几何变换 |
| 失败处理 | 图片 API 失败时**保存提示词和引用关系**，可重试或上传替代素材 |
| 修改构图后 | 需重新生成对应人物区域与样片；**原图和旧样片保留作比较** |

### 6.3 镜头区

| 元素 | 规则 |
|---|---|
| 镜头预设 | **默认稳定同框**；可选轻微推拉和发言人裁切；**不默认每次换人必切镜** |
| 轻量预览 | 用底图、真实音轨和裁切框预览节奏；明确标记「**构图预览，无动态口型**」 |
| 镜头与分段关系 | 镜头属于后处理（数字运镜 / 裁切 / 版式），改镜头**不触发**基础视频重渲染 |

### 6.4 样片区

| 元素 | 规则 |
|---|---|
| 生成样片 | **自动选双方都有发言、包含 A→B→A 的区间**（初始 10–20 秒，按台词确定）；用户可调整范围 |
| 边界预览 | 另提供相邻分段的边界预览（检查跨段连续性） |
| 样片检查 | 错误开口、口型同步、角色一致、边界连续、声音与清晰度——**不能只检查是否生成成功** |
| 接受样片 | 绑定本次输入版本、工作流与规格；**只证明已检查区间和配置，不保证整片无缺陷** |
| 复用条件 | 样片与正式片段具有完全相同的输入、参数、上下文和区间时才可复用；**低规格快速样片不能充当高规格正式片段** |
| 替代形式 | 动态模式效果不佳时显示替代候选（双窗 / 单人动态 + 静态全景 / 静态 + 配音 + 字幕）与预估，**用户选择后才生成对比样片，不自动改变节目形式** |

---

## 7. 阶段五 · 生成导出

> 入口：样片确认；任务中心；历史成片。

### 7.1 开始前摘要面板

```
┌────────────────────────────────────────────────────────────────┐
│ 开始生成前确认                                                  │
│ 实际时长 02:48.120 │ 横屏 16:9 │ 规格 480p→16:9 导出画布        │
│ 语音来源 本地引擎 │ 视频模式 双人动态同框                        │
│ 复用段 3 │ 待生成段 6 │ 预估 25–40 分钟（本机同规格实测）        │
│ 磁盘预算 ~2.1 GB（可用 180 GB）│ 当前版本 R9/V3/S2              │
└────────────────────────────────────────────────────────────────┘
```

- **没有测速数据时明确显示「尚未测定」**，不显示编造的预估

### 7.2 任务控制

| 操作或状态 | 行为 |
|---|---|
| 开始生成 | **冻结任务输入快照**；相同请求连续点击只创建一项任务 |
| 当前段完成后暂停 | 停止派发新段，当前段完成后保存 |
| 取消当前生成 | 停止派发，尝试中断本任务持有的本地推理；已完成段保留 |
| 编辑脚本或画面 | 进入新的草稿版本；**后台继续生成原快照**，界面明确标注版本差异 |
| 某段失败 | 保留其他段，显示错误位置；**默认暂停后续串行生成**，支持原位重试 |
| 导出失败 | **只重跑相应后处理或编码**，不重新生成全部口型视频 |
| 成片检查 | 可定位片段、回到对应台词或镜头，记录问题并局部重跑 |
| 关闭浏览器 | **不停止后台**；退出本地服务前提供「当前段完成后退出」与「立即取消并退出」，说明已完成内容已保存 |

### 7.3 导出

| 项 | 规则 |
|---|---|
| 导出物 | MP4、混音 WAV、A/B 分轨、SRT 字幕；**烧录字幕可选**；[打开输出目录] |
| 字幕 | SRT 独立从校正后的时间信息生成；**显示文本不包含引擎专用标签**；开关字幕只影响对应后处理 |
| 后处理顺序 | 基础视频 → 按保留区间合成 → 镜头裁切与版式 → 可选补帧 → 可选烧录字幕 → 合并权威音轨 → 编码 → 校验（权威定义见 02 §6.5） |
| 版本 | 正式产物使用**不可变版本路径**，不覆盖同名 final.mp4 |
| 竖屏导出 | 先展示裁切预览；人物无法同时保留时提供上下双窗或整幅嵌入竖屏画布；能后期实现的只重跑后处理 |

---

## 8. 资产库

> **角色库不再是每期必经步骤**，是独立入口（Sidebar / 阶段①的资产抽屉）。

### 8.1 布局与元素

```
┌────────────────────────────────────────────────────────────────────┐
│ 资产库                       [新建角色] [新建场景] [导入素材]        │
├────────────────────────────────────────────────────────────────────┤
│ 角色                                                                │
│ ┌────────┐ ┌────────┐ ┌────────┐                                  │
│ │ 李雷    │ │ 阿尔法  │ │ …      │   保存：形象、性格描述、          │
│ │ 形象缩略 │ │ (拟人猫)│ │        │   多个语音提供方的独立绑定        │
│ │ 🔊本地 🔊M│ │        │ │        │                                  │
│ └────────┘ └────────┘ └────────┘                                  │
│ 场景 / 同框底图 / 上传素材                                           │
│ ┌────────┐ ┌────────┐                                             │
│ │ 录音棚  │ │ 咖啡馆  │   角色形象、场景、同框图可上传或经图片 API 生成 │
│ └────────┘ └────────┘                                             │
└────────────────────────────────────────────────────────────────────┘
```

| 规则 | 说明 |
|---|---|
| 角色对象 | 保存形象、性格描述、**多个语音提供方的独立绑定**（本地参考音绑定 + MiniMax voice_id 绑定可并存，不保证声音完全相同） |
| 工程引用 | 工程复制角色的**已选版本**；资产库更新**不追溯改变历史工程** |
| 音色绑定 | 角色 A 本地切 MiniMax：保留脚本、形象、场景；要求选择可用音色并试听；重新生成受影响角色音频 |
| 引擎切换矩阵 | 权威定义见 02 §4.4（本地⇄MiniMax、只有 A 切换、修改未采用、请求失败五种场景） |
| 双人要求 | 优先清晰可见的脸部、少遮挡嘴部的麦克风与可分离的人物区域；背景适度简化 |

---

## 9. 服务设置

> 独立入口。**仅四块**：文本 API、图片 API、本地引擎、MiniMax 语音。

### 9.1 布局与元素

```
┌────────────────────────────────────────────────────────────────────┐
│ 服务设置                                                           │
├──────────────┬─────────────────────────────────────────────────────┤
│ ▸ 文本 API    │ 地址 [https://…    ] 模型 [______]                  │
│ ▸ 图片 API    │ 凭据 [机器级密钥库引用 ▾]  [测试连接]                │
│ ▸ 本地引擎    │ ─────────────────────────────────────────           │
│ ▸ MiniMax 语音│ 能力状态：                                         │
│              │  文本连通性        ● 已连接                         │
│              │  图片·文生图        ● 已连接                         │
│              │  图片·单图编辑      ● 已连接                         │
│              │  图片·多参考图编辑   ○ 不支持（同框图走上传替代）      │
│              │  本地 TTS          ● 就绪                            │
│              │  本地口型          ● 就绪                            │
│              │  MiniMax 音色可用   ● 2 个绑定有效                    │
└──────────────┴─────────────────────────────────────────────────────┘
```

| 规则 | 说明 |
|---|---|
| 四块配置 | 地址、模型 / 工作流、**凭据引用**与连接测试；**不增加产品账号系统** |
| 凭据存放 | **保存在机器设置，工程只引用配置 ID**；打包工程不含 API 密钥 |
| 能力分开登记 | 文本连通性、图片生成、图像编辑、多参考图编辑、本地 TTS、本地口型、MiniMax 音色可用性**分开显示**；连接成功不代表具备人物一致性或多参考图能力 |
| 离线影响 | 某一服务离线只影响需要它的操作；用户仍可编辑和播放已有素材 |
| 测试区分 | **付费试听与只读连通检查区分**；测试按钮标注是否产生费用 |
| API 预算 | 可选设置每期上限；缺价格配置时展示字符数、请求数和实际用量，**不伪造金额** |

---

## 10. 任务中心

> 独立入口（TopBar 任务入口 / Sidebar）。统一显示**本地与 API 任务**。

### 10.1 布局与元素

```
┌────────────────────────────────────────────────────────────────────┐
│ 任务中心                    [刷新] 从服务端读取完整状态              │
├────────────────────────────────────────────────────────────────────┤
│ 运行中                                                             │
│ ● 整期配音  R9 · 阶段3/9 · 已 40s · [当前段完成后暂停] [取消]       │
│ ● 样片渲染  V3 · 推理中 62% · 预计 +3:10（本机同规格实测）          │
│ 排队                                                               │
│ ○ 渲染段 #04  等待 GPU · 前置：段#03                               │
│ 已结束（今日）                                                     │
│ ✓ 底图生成 V3 · 51s · [查看产物]                                   │
│ ✗ 图片生成 V2 · 余额不足 · [去服务设置] [换上传]                    │
│ ? 文本生成 R8 · 结果待核实 · [查询] （超时，可能重复计费）          │
└────────────────────────────────────────────────────────────────────┘
```

| 规则 | 说明 |
|---|---|
| 显示字段 | 阶段、**版本**、耗时、失败信息、可执行操作（暂停 / 取消 / 重试 / 查询 / 查看产物） |
| 重连 | **从服务端读取完整状态，不依赖浏览器之前收到的进度消息**（事件带序号，断线补发或读快照） |
| 进度语义 | 进度分为准备 / 推理 / 下载落盘 / 校验 / 后处理；节点进度映射为阶段进度，**不当作精确剩余时间**；ETA 用同规格历史测量 |
| 取消语义 | 「正在取消」与「已取消」分开；没有可验证的定向取消能力时显示「取消待生效」 |
| 待核实 | 提交后超时且不知服务是否处理时标 `unknown`（结果待核实）；能查询就查询，不能则告知再次生成可能重复计费 |

---

## 11. 数据模型

> 权威定义见 04 §12 与 02 §7；此处为前端视角的 TypeScript 摘要。

### 11.1 核心对象

```typescript
type Speaker = 'A' | 'B';

interface Project {
  schemaVersion: number;
  id: string;
  currentDraftRevision: string;      // 当前草稿版本
  selectedOutputVersion?: string;    // 已选成片版本
  aspect: 'landscape' | 'portrait';
  stageProgress: Record<StageId, StageState>;   // 由产物推导（见 13.1）
  approvals: Approval[];
}

// ---- 脚本 ----
interface ScriptRevision {
  id: string;                        // R8
  sourceInput: { kind: 'topic' | 'article' | 'script'; content: string };
  contentBrief: ContentBrief;        // 核心问题、主要观点、必要事实、段落目的、双方职责
  turns: Turn[];
  textApiConfigRef: string;          // 生成配置引用
}

interface Turn {                     // 发言：一次自然说话行为
  id: string;                        // T07（稳定 ID，不随排序改变）
  speaker: Speaker;
  intent: 'explain' | 'probe' | 'example' | 'challenge' | 'summarize' | 'other';
  lines: Line[];
  tone: string;                      // 默认「自然」
  speedRatio: number;                // >1 更快
  sourceAnchors?: SourceAnchor[];    // 来源引用（程序核对）
}

interface Line {                      // 句子：编辑、字幕、读音修正
  id: string;                        // L019
  displayText: string;               // 显示文本
  spokenText: string;                // 朗读文本（读音词典展开后）
}

// ---- 声音 ----
interface VoiceBinding {             // 角色身份、形象、声音绑定是三个对象
  id: string;
  characterId: string;
  providerProfileId: string;         // 配置 ID（凭据在机器级设置）
  modelId: string;
  localRefAudio?: AssetRef;          // 本地参考音版本
  minimaxVoiceId?: string;           // MiniMax voice_id
  auditionState: 'none' | 'passed' | 'stale';
}

interface SynthesisUnit {            // 合成单元：一次引擎请求
  id: string;                        // U12
  turnId: string;
  lineIds: string[];                 // 可覆盖多个句子
  providerProfileId: string;
  modelId: string;
  voiceBindingId: string;
  emotion: { label: string; intensity?: number };
  speedRatio: number;
  pronunciationRevision: string;     // 读音词典版本
  adoptedAudioAssetId?: string;      // 已采用音频版本
  candidateAudioAssetIds: string[];  // 候选（未被采用）
}

// ---- 音轨 ----
interface AudioTimeline {
  sampleRate: number;                // 母轨统一 48000
  sampleCount: number;               // N：权威总长
  unitOffsets: Record<string, number>;   // 整数样本偏移
  transitionGapMs: number[];         // 单元间显式间隔（单一归属）
  leadInMs: number;                  // 首部显式静音
  tailOutMs: number;                 // 尾部显式静音
  annotationVersion: string;         // 标注（时间码）版本，可与音轨版本不同
}

// ---- 视觉 ----
interface VisualVariant {            // 一次底图+区域的组合
  id: string;                        // V3
  masterImage: AssetRef;
  aspect: 'landscape' | 'portrait';
  modelInputTransform: Transform;    // 缩放/裁切/补边（确定值）
  personRegions: { A: Rect; B: Rect };   // 规范化人物区域坐标
  imageApiConfigRef?: string;        // 生成配置（上传时缺省）
}

interface ShotPlan {
  id: string;                        // S2
  preset: 'stable' | 'pushPull' | 'speakerCrop';
  ranges: Array<{ startSample: number; endSample: number; crop?: Rect; move?: CameraMove }>;
  manualFixed: boolean;              // 人工固定镜头与自动镜头分开保存
}

// ---- 渲染 ----
interface RenderSegment {
  id: string;                        // #04（稳定，插入段不导致后续换种子）
  outputRange: [number, number];     // 保留区间（不与其他段重复）
  renderRange: [number, number];     // 实际生成区间（可含缓冲）
  contextRange?: { range: [number, number]; sourceAssetId: string };  // 续接上下文
  inputHash: string;
  effectiveOutputVersion?: string;
}

// ---- 产物 / 任务 / 确认 ----
interface Artifact {
  id: string;
  kind: 'audio' | 'image' | 'video' | 'subtitle' | 'mixed';
  path: string;                      // 不可变版本路径（相对工程）
  fileHash: string;
  dependencyHash: string;            // 缓存键（见 02 §7.3）
  paramsSnapshot: Record<string, unknown>;
  workflowVersion?: string;
  validation?: ValidationResult;     // 样片检查结果等
}

interface Job {
  id: string;
  inputSnapshot: Record<string, unknown>;
  stage: 'prepare' | 'infer' | 'download' | 'verify' | 'post';
  status: 'queued' | 'running' | 'pause_requested' | 'paused'
        | 'waiting_confirmation' | 'recovering' | 'succeeded' | 'failed'
        | 'cancel_requested' | 'cancelled' | 'unknown';
  providerTaskId?: string;           // ComfyUI prompt_id / 远程任务 ID
  attempt: number;
  result?: { artifactIds: string[] };
  createdAt: string;
}

interface Approval {                 // 确认是对版本与规格的接受
  kind: 'script' | 'voice' | 'sample' | 'character' | 'composition' | 'shot';
  inputRevisionId: string;           // 确认的输入版本
  spec?: Record<string, unknown>;    // 规格（时长 / 区间 / 工作流版本）
  decision: 'accepted' | 'rejected';
  reasonTarget?: StageId;            // 打回指向
  note?: string;
  at: string;
}
```

### 11.2 派生规则

| 派生量 | 来源 | 规则 |
|---|---|---|
| 节目总时长 T | AudioTimeline | `N / sampleRate`；**N = leadIn + Σ(unit.sampleCount + gap) + tailOut**（整数样本） |
| 句级全局位置 | 对齐标注 + unitOffsets | `单元局部位置 + offsetSamples` |
| 发言显示时长 | 其单元实际音频 | 样本数换算 |
| 阶段状态 | 产物版本 + 任务状态 | 见 13.1 |
| 过期标记 | 依赖哈希对比 | 依赖哈希变化 → 已过期（文件仍可播放） |

---

## 12. 本地接口契约

> 前端只与编排服务（`http://127.0.0.1:8000`）通信。错误码与通用约定见 02 §8。

### 12.1 业务接口

| 接口 | 方法 | 用途 |
|---|---|---|
| `/api/providers/capabilities` | GET | 返回各配置的实际能力和就绪状态（驱动 UI 能力门控） |
| `/api/providers/{id}/test` | POST | 按能力测试；**付费试听与只读连通检查区分** |
| `/api/projects/{id}` | GET / PATCH | 读取 / 保存草稿；**携带 `expectedRevision` 防止旧页面覆盖新状态** |
| `/api/projects/{id}/script/generate` | POST | 创建内容生成 Job，结果为**候选脚本版本** |
| `/api/projects/{id}/script/rewrite` | POST | 选区改写并返回差异 |
| `/api/projects/{id}/tts/preview` | POST | 指定声音绑定生成短试听 |
| `/api/projects/{id}/tts/synthesize` | POST | 指定合成单元及输入快照 |
| `/api/projects/{id}/audio/assemble` | POST | 构建音轨与校正时间信息 |
| `/api/projects/{id}/visual/generate` | POST | 图片 API 任务，明确生成或编辑能力 |
| `/api/projects/{id}/rehearsals` | POST | 创建样片任务 |
| `/api/projects/{id}/renders/plan` | POST | 返回**可复用与需重算产物、估算和阻塞项** |
| `/api/projects/{id}/renders` | POST | 提交计划版本及输入快照 |
| `/api/approvals` | POST | 确认明确的任务、关卡、产物版本 |
| `/api/jobs/{id}` | GET | 读取任务完整状态（重连以此为准） |
| `/api/jobs/{id}/pause` · `/cancel` | POST | 请求暂停或取消，返回**真实状态** |
| `/api/events` | GET (SSE/WS) | 订阅**带序号**的进度事件；重连可补发或读取完整快照 |

### 12.2 事件消息

```typescript
type ServerEvent =
  | { seq: number; type: 'job.stage'; jobId: string; stage: Job['stage']; message?: string }
  | { seq: number; type: 'job.done'; jobId: string; artifactIds: string[] }
  | { seq: number; type: 'job.failed'; jobId: string; error: string; retryable: boolean }
  | { seq: number; type: 'approval.requested'; approval: Approval['kind']; payload: unknown }
  | { seq: number; type: 'system.vram'; used: number; total: number };
```

> **渲染计划一致性**：`renders/plan` 生成后若输入发生变化，提交旧计划时必须明确「使用旧版本」或「重新规划」——不能显示「只重跑 2 段」而提交时实际用新输入重跑 10 段。

---

## 13. 状态机与全局交互规则

### 13.1 阶段状态（七态，由产物版本与任务状态推导）

| 状态 | 判定 | UI |
|---|---|---|
| 未准备 | 所需产物不存在 | 灰色数字 |
| 可执行 | 前置版本已确认或存在，本阶段无运行任务 | 可点击主操作 |
| 执行中 | 本阶段存在 running / queued 任务 | 转圈 + 进度 |
| 待确认 | 产物已就绪且确认点开启、未确认 | 橙色「待确认」 |
| 已就绪 | 所需产物已确认 / 已采用 | 绿勾 |
| 已过期 | 产物存在但依赖哈希已变化 | 橙点 + 产物「已过期」角标（**仍可播放 / 查看**） |
| 失败 | 最近任务 failed 且无有效产物 | 红点 + 重试入口 |

> **「下一步」仅导航**，不写入任何虚假完成状态；完成态永远由产物推导。

### 13.2 草稿 / 候选 / 已用

| 场景 | 行为 |
|---|---|
| 编辑 | 创建草稿；自动保存 |
| 生成 | 返回候选；**不自动覆盖已用版本** |
| 采用 | 用户显式操作（或自动模式按已保存策略）；更新当前使用版本；下游按重跑矩阵标过期 |
| 任务结果到达时草稿已变化 | 结果**只加入历史并标记所属版本**，不覆盖新草稿 |
| 旧任务完成 | 不覆盖新草稿；旧成片可独立播放和导出（版本隔离） |

### 13.3 全局交互约定

| 约定 | 说明 |
|---|---|
| 导航 | 阶段可自由前后跳转；不做硬性拦截，缺失前置时目标页顶部橙色提示 |
| 可调优先 | 校验只提示不硬拦（空数据与确认点除外） |
| 文件 | 上传先复制进工程目录；工程存**相对路径**；凭据存机器级设置 |
| 自动保存 | 2s 防抖；元数据由后端单一写入入口（临时文件 + 原子替换） |
| 版本保留 | 每类关键产物保留最近 N 版（默认 10）；正式产物不可变版本路径 |
| 服务断开 | 只禁用依赖该服务的操作；编辑与播放不受影响；提示去服务设置 |
| 键盘 | `Space` 播放 / `←→` 句跳转 / `Ctrl+Z` 撤销 / `1–5` 切阶段 |
| 无障碍 | 颜色 + 文字双通道；键盘可达；对比度 ≥4.5:1 |
| 退出 | 关浏览器不停后台；退出服务前提供「当前段完成后退出 / 立即取消并退出」 |

---

## 附录 A：页面清单速查

| 视图 | 层级 | 核心产出 | 确认点 |
|---|---|---|---|
| 工程首页 | 独立 | 工程列表 + 节目模板 | — |
| ① 创建本期 | 阶段 | 输入 + 内容结构 + 话轮候选 | （角色·可选） |
| ② 编辑对话 | 阶段 | 确认的脚本版本 | **脚本** |
| ③ 试听配音 | 阶段 | 确认的音轨版本（样本数权威） | **配音** |
| ④ 预览画面 | 阶段 | 底图+区域+镜头计划+样片 | （构图·可选）（镜头·可选）**样片** |
| ⑤ 生成导出 | 阶段 | 成片 + 音频 + SRT | — |
| 资产库 | 独立 | 角色 / 场景 / 素材 | — |
| 服务设置 | 独立 | 四块服务配置 + 能力状态 | — |
| 任务中心 | 独立 | 本地与 API 任务统一视图 | — |

## 附录 B：术语表

| 术语 | 含义 |
|---|---|
| **话轮（Turn）** | 一次自然说话行为，用户默认的编辑粒度，承载角色与对话意图 |
| **合成单元（Unit）** | 实际送给一个语音引擎的一次请求，可覆盖一个或多个句子 |
| **transitionGapMs** | 两个单元之间的显式停顿，唯一归属，是模型自然停顿之外的额外静音 |
| **leadInMs / tailOutMs** | 节目首部 / 尾部的显式静音 |
| **权威时长 T** | `N / sampleRate`；N 为最终 PCM 音轨样本数，不是时间码推算值 |
| **候选 / 已用** | 生成返回候选版本；用户采用后才成为当前使用版本 |
| **快照** | 任务提交时冻结的输入版本集合；后台运行不受后续编辑影响 |
| **三区间** | 每个渲染段的 outputRange（保留）/ renderRange（生成）/ contextRange（续接上下文） |
| **人物区域** | 在最终送入视频工作流的图上标注的 A/B 区域（首版矩形框），决定驱动绑定 |
| **视觉变体（V）** | 底图 + 比例 + 模型输入变换 + 人物区域的一次组合 |
| **确认（Approval）** | 对明确的输入版本与规格的接受记录，不是全局布尔值 |
| **结果待核实（unknown）** | 提交后超时且不知提供方是否已处理的状态；可能重复计费 |
| **替代形式** | 双窗版式 / 单人动态 + 静态全景 / 静态画面 + 配音 + 字幕等候选交付形式 |
