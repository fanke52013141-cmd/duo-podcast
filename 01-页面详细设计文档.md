# 双声播客工坊 DuoCast · 页面详细设计文档

> 版本 v2.0 ｜ 载体：本地 Web 工作台（SPA）｜ 运行：Windows 本地 ｜ 文档粒度：页面 / 布局 / 元素 / 交互 / 校验 / 数据契约
>
> **本文档的写作目标**：把每个页面的布局、元素、交互、状态、校验、数据流写到「可以直接据此生成页面」的粒度。

---

## 目录

- [0. 产品定义与核心机制](#0-产品定义与核心机制)
  - [0.1 产品定位](#01-产品定位)
  - [0.2 与 v1.0 方案的差异](#02-与-v10-方案的差异)
  - [0.3 三个核心机制：可调 / 确认关卡 / 一键生成](#03-三个核心机制可调--确认关卡--一键生成)
  - [0.4 完整生产链路](#04-完整生产链路)
  - [0.5 全局设计语言](#05-全局设计语言)
- [1. 全局框架与公共组件](#1-全局框架与公共组件)
- [2. P0 · 工程管理](#2-p0--工程管理)
- [3. P1 · 角色库](#3-p1--角色库)
- [4. P2 · 内容输入](#4-p2--内容输入)
- [5. P3 · 分角脚本](#5-p3--分角脚本)
- [6. P4 · 配音锁轨](#6-p4--配音锁轨)
- [7. P5 · 视觉设定](#7-p5--视觉设定)
- [8. P6 · 镜头表](#8-p6--镜头表)
- [9. P7 · 试镜](#9-p7--试镜)
- [10. P8 · 渲染与导出](#10-p8--渲染与导出)
- [11. P9 · 一键生成](#11-p9--一键生成)
- [12. 数据模型](#12-数据模型)
- [13. 本地接口契约](#13-本地接口契约)
- [14. 状态机与全局交互规则](#14-状态机与全局交互规则)

---

## 0. 产品定义与核心机制

### 0.1 产品定位

| 项 | 定义 |
|---|---|
| **一句话** | 面向 Windows 本地部署的双人播客视频生产工作台，编排「角色 → 内容 → 分角 → 配音 → 视觉 → 镜头 → 试镜 → 成片」八步流程，底层调用本机 ComfyUI 与 ffmpeg，全部数据不出本机 |
| **用户** | 单人创作者（同时是操作者），无登录、无多用户、无云端 |
| **产出** | ≤10 分钟（默认目标 1–3 分钟）的双人对谈视频，固定机位 + 切镜 + 轻微推拉 |
| **核心角色** | <span>A 主持人</span>：固定音色、固定形象，跨期复用<br><span>B 嘉宾</span>：每期可变（人 / 拟人动物 / 拟人物件） |
| **形态** | 单页应用，页内视图切换（非路由跳转）；左步骤轨 + 中央主视图 + 右检查器 + 底部时间轨 |
| **隐私** | 仅访问 127.0.0.1；图片 / 音频 / 视频全部落在本地工程目录 |

### 0.2 与 v1.0 方案的差异

| 维度 | v1.0（旧） | v2.0（本方案） | 原因 |
|---|---|---|---|
| 流程顺序 | 场景 → 角色 → 遮罩 → 音色 → 文案 → 试镜 → 成片 | **角色库 → 内容 → 分角 → 配音锁轨 → 视觉 → 镜头 → 试镜 → 成片** | 音频先行才能锁定时间轨；时间轨是所有画面工作的坐标系 |
| 时间轨来源 | 前端按 4.2 字/秒**估算** | **TTS 实际输出 + ASR 回填时间码** | 估算值会与真实音频差 10%+，导致切点错位 |
| 底图生成 | 角色图 + 场景图 → 合成 | **图生图融合**：角色设定图 + 场景图 → 模型生成「两人已在场景中」的同框底图 | 贴图会导致光照/透视/景深不匹配，且错误会放大到整片 |
| 遮罩 | 手动笔刷涂抹 A/B 区域 | **自动生成 mask**（左右站位 + bbox 校正），手涂降级为「精修」 | InfiniteTalk 多人物分支默认就是左右半幅 mask；手涂是伪工作量 |
| 说话人绑定 | 遮罩 + 双轨 | **`add` 模式零音频填充** + mask 跨注意力分配 | 官方实现：不说话的说话人收到全零音频特征，自然保持安静 |
| 镜头语言 | 分镜（同框/单拍/单拍） | **切镜点 + 数字运镜（推/拉/裁切）**，不做摇镜 | 播客语境下摇镜不真实；数字运镜零漂移、零额外算力 |
| 语音合成粒度 | 逐句 | **按说话人整段合成，ASR 回填句级时间码** | 整段合成音色更稳；回填后仍保留句级重录能力 |
| 模式 | 单一手动 | **手动可调 → 确认关卡 → 一键生成** 三级 | 用户明确要求的演进路径 |

### 0.3 三个核心机制：可调 / 确认关卡 / 一键生成

这是本产品的灵魂，必须在 UI 上贯穿始终。

#### 机制一：每步可调（Manual Mode）

**设计原则：每一个步骤都是一个「可独立重跑、可局部重跑、参数全暴露」的单元。**

| 能力 | 说明 | UI 体现 |
|---|---|---|
| **单步重跑** | 任意步骤可单独执行，不依赖后续步骤 | 每步主视图右上角有「重跑本步」按钮 |
| **局部重跑** | 只重跑被改动的最小单元 | 改一句话 → 只重合成那一句；改一个镜头段 → 只重渲染那一段 |
| **参数全暴露** | 所有推理参数在右检查器可见可改 | 高级参数默认折叠，展开后逐项可调 |
| **结果可对比** | 每次重跑保留历史版本，可 A/B 对比回退 | 产物卡片右上角「版本 n」下拉，支持回退 |
| **下游失效提示** | 上游改动后，明确标出哪些下游产物已过期 | 过期产物打橙色「已过期」角标 + 一键重算下游 |

**依赖失效规则（关键）**：

```
角色库 → 视觉设定（形象图）
内容   → 分角脚本
分角脚本 → 配音锁轨 → 时间轨 → 镜头表 → 试镜 → 成片
配音锁轨 → 视觉设定（不需要）  ← 注意：配音与视觉解耦
视觉设定 → 镜头表（底图）→ 试镜 → 成片
```

具体失效映射：

| 改动了 | 失效的下游 |
|---|---|
| 角色 A/B 的形象图 | 视觉设定的同框底图 → 镜头表 → 试镜 → 成片 |
| 角色 A/B 的参考音 | 配音锁轨 → 时间轨 → 镜头表 → 试镜 → 成片 |
| 分角脚本某一句文本 | 该句音频 → 时间轨 → 镜头表 → 试镜 → 成片 |
| 分角脚本的说话人归属 | 两条整段音频 → 时间轨 → 镜头表 → 试镜 → 成片 |
| 同框底图 | 镜头表 → 试镜 → 成片 |
| 镜头表某个段 | 该段渲染 → 成片 |
| 渲染参数（分辨率/步数） | 成片 |

#### 机制二：确认关卡（Checkpoint）

**设计原则：把「需要人眼判断」的环节从流程里拎出来，做成显式的关卡。关卡只有两种结果——通过 / 打回。**

| 关卡 | 名称 | 确认内容 | 通过条件 |
|---|---|---|---|
| **CP-1** | 角色确认 | A 的形象图 + 音色；B 的形象图 + 音色 | 两角色各有一张形象图 + 一条参考音，且已试听通过 |
| **CP-2** | 稿件确认 | 播客稿的分角结果：说话人分配、口语化程度、总时长 | 至少 1 句台词，且总时长在目标范围内 |
| **CP-3** | 底图确认 | 同框底图：两人站位、构图、风格、光照 | 底图已生成且两人左右分居 |
| **CP-4** | 配音确认 | 整期音频：音色、语气、语速、停顿、总时长 | A/B 两条整段音频已合成且试听通过 |
| **CP-5** | 镜头确认 | 镜头表：切镜点、镜头类型、数字运镜 | 所有镜头段已配置 |
| **CP-6** | 试镜确认 | 5 秒样片：口型、非说话人静止、画面稳定、风格统一 | 四项验收全勾 |

> **编号说明**：CP 编号按「确认内容」分组（角色 → 稿件 → 底图 → 配音 → 镜头 → 试镜），与执行顺序的对应关系是 **CP-1 → CP-2 → CP-3 → CP-4 → CP-5 → CP-6**（P4 配音之后是 CP-3 底图确认，P5 底图之后是 CP-4 配音确认）。编号一旦分配不再变动，避免与工程文件、日志中的引用冲突。

**两种工作方式**：

- **交互式**：跑到关卡时停下，弹出确认面板，等待用户操作。
- **前置式**：在一键生成开始前，先在 P9 把能提前定的关卡（CP-1、CP-3 的素材）预先锁定，跑到时自动通过。

> **关键设计**：关卡不是「锁」，是「暂停点」。用户可以配置每个关卡为「必须确认 / 自动通过 / 跳过」，这个配置保存在工程里。

#### 机制三：一键生成（Auto Mode）

**前提：所有环节已在手动模式下打磨通畅，参数已固化。**

| 配置项 | 说明 |
|---|---|
| **输入** | 一个话题，或一篇完整文章 |
| **前置资产** | 角色 A（形象 + 音色，锁定）；角色 B（从库中选，或新建） |
| **自动执行** | 内容 → 分角 → 配音 → 视觉 → 镜头 → 试镜 → 渲染 → 导出 |
| **中途停顿** | 仅在用户勾选的确认关卡处暂停 |
| **失败处理** | 任一步失败 → 停在失败步，保留已完成产物，可修复后「从失败步继续」 |
| **产物** | 与手动模式完全一致，且保留每一步的中间产物，可事后回到任意步微调 |

### 0.4 完整生产链路

```
┌─────────────────────────────────────────────────────────────────┐
│  P1 角色库（跨期复用资产）                                        │
│  A：形象图 + 参考音 + 音色参数（锁定）                             │
│  B：形象图 + 参考音 + 音色参数（每期可变）                          │
└────────────────────────┬────────────────────────────────────────┘
                         │ [CP-1 角色确认]
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│  P2 内容输入        话题 / 文章 ──LLM──▶ 播客稿（初稿）            │
└────────────────────────┬────────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│  P3 分角脚本        播客稿 ──LLM/人工──▶ 逐句 {说话人, 文本, 语气}  │
└────────────────────────┬────────────────────────────────────────┘
                         │ [CP-2 稿件确认]
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│  P4 配音锁轨        A 整段合成 + B 整段合成 ──ASR──▶ 句级时间码    │
│                     ▼ 产出：时间轨（唯一坐标系）                    │
└────────────────────────┬────────────────────────────────────────┘
                         │ [CP-3 底图确认]
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│  P5 视觉设定        A 形象图 + B 形象图 + 场景图 ──图生图融合──▶    │
│                     ▼ 产出：同框底图（唯一首帧）                    │
└────────────────────────┬────────────────────────────────────────┘
                         │ [CP-4 配音确认]
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│  P6 镜头表         时间轨静音间隙 ──▶ 可切点 ──▶ 镜头段            │
│                    {类型, 起止, 裁切框, 缩放, 缓动}                 │
└────────────────────────┬────────────────────────────────────────┘
                         │ [CP-5 镜头确认]
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│  P7 试镜            5 秒样片：底图 + 双音轨片段 ──▶ 四项验收       │
└────────────────────────┬────────────────────────────────────────┘
                         │ [CP-6 试镜确认]
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│  P8 渲染与导出      分段渲染（串行队列）──▶ 拼接 ──▶ RIFE 补帧 ──▶ 成片│
└─────────────────────────────────────────────────────────────────┘
```

### 0.5 全局设计语言

#### 色彩语义

| 用途 | 颜色 | 值 | 说明 |
|---|---|---|---|
| 角色 A | 青蓝 | `#0e7490` / 浅底 `#e2f2f6` | 全局一致，出现在所有涉及 A 的元素上 |
| 角色 B | 珊瑚粉 | `#be4d6e` / 浅底 `#fbe9ef` | 同上 |
| 品牌 / 主操作 | 焦糖 | `#a96a2c` / 浅底 `#f7ede2` | 主按钮、当前步骤高亮 |
| 成功 / 已就绪 | 绿 | `#2f9e6e` / `#e4f5ed` | 完成态、服务已连接 |
| 警告 / 待处理 / 已过期 | 橙 | `#b97e16` / `#fbf1dc` | 下游失效、参数越界 |
| 错误 / 删除 | 红 | `#c75052` / `#fcebeb` | 失败、危险操作 |
| 中性文字 | 三级灰 | `#1f2430` / `#4d5666` / `#858f9f` | 主文字 / 次要 / 提示 |
| 分割线 | 灰 | `#e3e7ee` / `#d3d9e3` | 一级 / 二级 |

> **无障碍**：颜色不作为唯一信息载体，A/B 与所有状态必须同时有文字标签。

#### 字体

| 用途 | 字体 | 字号 | 行高 |
|---|---|---|---|
| 正文 | `"Segoe UI", "Microsoft YaHei UI", system-ui` | 14px | 1.6 |
| 标题 | 同上 | 20 / 16 / 14 | 1.4 |
| 时间码 / 帧数 / 参数值 | `"Cascadia Code", Consolas, monospace` | 12px | 1.5 |
| 标签 / 徽章 | 同上 | 11px | 1.4 |

#### 间距与圆角

- 基础栅格 4px；组件内间距 8 / 12px；区块间距 16 / 24px
- 圆角：按钮 8px、卡片 12px、徽章 6px、弹窗 16px
- 卡片阴影：`0 1px 3px rgba(31,36,48,.06), 0 1px 2px rgba(31,36,48,.04)`

#### 响应式断点

| 断点 | 布局 |
|---|---|
| ≥1440px | 完整四区：步骤轨 232px + 主视图 + 检查器 340px + 底部时间轨 |
| 1024–1439px | 检查器折叠为抽屉（右侧浮层） |
| 768–1023px | 步骤轨变顶部横向滚动条，检查器全屏抽屉 |
| <768px | 单栏堆叠（本产品主要面向桌面，移动端仅保证可查看） |

---

## 1. 全局框架与公共组件

### 1.1 布局结构

```
┌──────────────────────────────────────────────────────────────────────┐
│ TopBar  56px  工程名 │ 服务状态 │ 显存 │ 模型芯片 │ 保存 │ 一键生成    │
├──────────┬───────────────────────────────────┬───────────────────────┤
│          │                                   │                       │
│ Sidebar  │        Stage（主视图）             │   Inspector           │
│ 232px    │        步骤视图区                   │   340px               │
│          │                                   │   上下文参数           │
│ 8 个步骤  │                                   │                       │
│          │                                   │                       │
│ 底注      │                                   │                       │
├──────────┴───────────────────────────────────┴───────────────────────┤
│ TimelineBar  底部时间轨（P4 之后常驻，可折叠） 高度 56 / 折叠 28       │
├──────────────────────────────────────────────────────────────────────┤
│ FootBar  48px  第 n 步 / 共 8 步 · 步骤名 │ 上一步 │ 检查点状态 │ 下一步 │
└──────────────────────────────────────────────────────────────────────┘
```

### 1.2 TopBar（顶部资源条）

高度 56px，吸顶，`z-index: 100`。

| 元素 | 类型 | 默认 / 取值 | 功能 | 交互与校验 |
|---|---|---|---|---|
| 产品标识 | 图标 + 文字 | 「双声播客工坊 / DuoCast Studio」 | 品牌识别 | 点击 → `goStep(0)`；若当前步骤有未保存改动，弹确认 |
| 工程名 | 可编辑文本 | 当前工程名，如「EP001·AI与创作」 | 显示与重命名 | 单击进入编辑，失焦保存；空值回退为「未命名工程」 |
| 本地运行徽章 | 状态标签 | 绿点 +「全本地运行 · 数据不出本机」 | 隐私承诺 | 只读，常亮 |
| 服务状态组 | 状态标签 ×2 | `ComfyUI 127.0.0.1:8188`、`编排服务 127.0.0.1:8000` | 后端连通性 | 每 5s 轮询 `/system_stats` 与 `/api/health`；已连接=绿描边，未连接=灰 + 悬停提示启动命令；**任一未连接时，所有推理类按钮禁用**，按钮下方显示「请先启动本地引擎」 |
| 显存条 | 进度条 + 数值 | 例 `9.2 / 16.0 GB` | 辅助判断能否跑高配 | 只读，来自 `/system_stats`；≥90% 转橙并在渲染页提示降配 |
| 模型芯片组 | 状态标签 ×4 | `IndexTTS-2.5` / `图生图模型` / `InfiniteTalk` / `RIFE` | 模型就绪状态 | 已就绪=绿描边；缺失=灰 + 悬停显示缺失文件与期望路径；缺模型时禁用依赖它的步骤按钮 |
| 保存状态 | 文本 | 「已保存」/「保存中…」/「有未保存改动」 | 工程持久化状态 | 自动保存：改动后 2s 防抖写入工程文件 |
| 一键生成 | 主按钮（焦糖） | — | 跳转 P9 | 无工程时禁用 |

### 1.3 Sidebar（左侧步骤轨）

宽度 232px，吸顶，独立滚动。

| 元素 | 类型 | 内容 | 交互 |
|---|---|---|---|
| 步骤项 ×8 | 纵向导航 | 见下表 | 点击切换视图；当前步=焦糖左竖条 + 浅底高亮；已完成=圆点变绿勾；有产物但已过期=圆点变橙；未开始=灰色数字 |
| 步骤副标题 | 小字 | 每步一句话说明 | 只读 |
| 依赖提示 | 橙色小字 | 如「需先完成 P4 配音」 | 前置未满足时显示在步骤项下方 |
| 底注 | 说明文本 | 「本地引擎：ComfyUI 8188 / 编排服务 8000」 | 只读 |

步骤定义：

| # | 步骤名 | 副标题 | 视图 ID |
|---|---|---|---|
| 1 | 角色库 | 锁定 A 与 B 的形象与音色 | `P1` |
| 2 | 内容输入 | 话题或文章 → 播客稿 | `P2` |
| 3 | 分角脚本 | 逐句分配说话人与语气 | `P3` |
| 4 | 配音锁轨 | 整段合成并回填时间码 | `P4` |
| 5 | 视觉设定 | 角色形象图与同框底图 | `P5` |
| 6 | 镜头表 | 切镜点与数字运镜 | `P6` |
| 7 | 试镜 | 5 秒样片验收 | `P7` |
| 8 | 渲染与导出 | 分段渲染与成片 | `P8` |

> P9（一键生成）不占步骤位，从 TopBar 与 FootBar 进入。

### 1.4 FootBar（底部操作条）

高度 48px，吸顶底部。

| 元素 | 类型 | 默认 | 功能 | 交互 |
|---|---|---|---|---|
| 位置提示 | 文本 | 「第 3 步 / 共 8 步 · 分角脚本」 | 位置感 | 随切步更新 |
| 上一步 | 次级按钮 | 第 1 步时禁用 | 回上一视图 | `goStep(i-1)` |
| 检查点状态 | 徽章 | 「本步无检查点」/「待确认」/「已通过」 | 关卡可见性 | 点击跳转到该检查点面板 |
| 下一步 | 主按钮 | 末步显示「已到流程末尾」并禁用 | 前进 | 写入 `done[current] = true` 后 `goStep(i+1)`；不做硬性拦截，未满足前置时目标页顶部显示橙色缺失提示 |

### 1.5 TimelineBar（底部时间轨）

**P4 配音锁轨完成后自动展开并常驻**，可折叠。

| 元素 | 类型 | 内容 | 交互 |
|---|---|---|---|
| 折叠开关 | 图标按钮 | 展开 56px / 折叠 28px | 点击切换，状态存工程 |
| 播放控制 | 按钮组 | 播放 / 暂停 / 跳到上一个句首 / 跳到下一个句首 | 键盘 `Space` / `←` / `→` |
| 当前时间码 | 等宽文本 | `00:12.480` | 只读 |
| 总时长 | 等宽文本 | `02:34.120` | 只读 |
| 缩放 | 滑块 | 10–200 px/秒，默认自适应 | 拖动重绘 |
| A 轨 | 音频轨 | 青蓝色块 = A 说话段；空白 = 静音 | 悬停显示句号与时长；点击定位播放头 |
| B 轨 | 音频轨 | 珊瑚粉色块 = B 说话段 | 同上 |
| 镜头段轨 | 分段轨 | 每个镜头段一个色块，标注段号 + 镜头类型图标 | 点击选中该段并高亮 P6 中对应行；拖动边界可调整切点（吸附到静音间隙） |
| 播放头 | 竖线 | 青绿色，带时间码气泡 | 拖动可 scrub；拖动时音视频跟随 |
| 切点标记 | 竖虚线 | 镜头段边界 | 吸附提示：靠近静音间隙时高亮 |

> **交互细节**：拖动镜头段边界时，若落点不在静音间隙内，显示橙色警告「切点落在说话中间，口型可能不连续」，但不阻止操作（可调优先）。

### 1.6 公共组件

#### 1.6.1 卡片 Card

```
┌────────────────────────────────────────┐
│ 标题              [状态徽章] [⋯菜单]    │  ← 标题行 40px
├────────────────────────────────────────┤
│ 内容区                                  │
│                                        │
├────────────────────────────────────────┤
│ [次要操作]                  [主操作]    │  ← 操作行（可选）
└────────────────────────────────────────┘
```

- `⋯菜单` 统一包含：重跑本步 / 查看历史版本 / 复制参数 / 导出产物
- 状态徽章取值：`待生成`(灰) / `生成中`(橙+进度) / `已完成`(绿) / `已过期`(橙) / `失败`(红)

#### 1.6.2 上传槽 UploadSlot

- 空态：虚线框 + 图标 + 主提示语 + 副提示语（规格要求）
- 点击打开系统文件选择；支持拖拽投放
- 有内容：缩略图铺满 + 左下角文件名浮标 + 右上角「替换 / 移除」
- **实现约定**：本地文件先经 `/api/project/{id}/assets` 复制进工程目录，返回相对路径；**不用 base64 塞进 state**（大文件会拖垮内存）

#### 1.6.3 版本选择器 VersionPicker

- 显示「v3 ▾」，点击展开历史版本列表
- 每项显示：版本号、生成时间、关键参数摘要、缩略图
- 选中历史版本 → 该版本变为当前，下游标记过期
- 支持「对比」：并排显示两个版本

#### 1.6.4 参数控件 ParameterField

- 统一结构：`标签` + `控件` + `当前值（等宽）` + `?` 提示图标
- `?` 悬停显示：参数含义、取值范围、推荐值、调大/调小的影响
- 越界时输入框转橙 + 下方橙色说明

#### 1.6.5 确认关卡面板 CheckpointPanel

```
┌─────────────────────────────────────────────────┐
│  ⏸ CP-3 · 底图确认                    [×]       │
├─────────────────────────────────────────────────┤
│  确认内容（清单，逐项勾选）                        │
│  ☑ 两人左右分居，居中留空                         │
│  ☑ 光照方向一致，无接缝                           │
│  ☑ 固定机位、16:9                                 │
│  ☐ 风格与角色设定图统一                            │
├─────────────────────────────────────────────────┤
│  [打回并说明]                    [确认通过 →]      │
└─────────────────────────────────────────────────┘
```

- 全部勾选才能点「确认通过」
- 「打回并说明」弹出文本框，说明写入工程日志，并高亮对应的上游步骤

#### 1.6.6 Toast

- 屏幕底部居中浮起，2.2s 自动消失，新提示覆盖旧提示
- 类型：成功（绿）/ 警告（橙）/ 错误（红，带「查看详情」）
- 不阻断操作

---

## 2. P0 · 工程管理

> P0 不是流程步骤，是进入工作台前的工程选择层。

### 2.1 布局

```
┌────────────────────────────────────────────────────────────┐
│  双声播客工坊                                    [新建工程]  │
├────────────────────────────────────────────────────────────┤
│  最近工程                                                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 缩略图    │ │ 缩略图    │ │ 缩略图    │ │  ＋      │       │
│  │ EP003    │ │ EP002    │ │ EP001    │ │ 新建     │       │
│  │ 2:34 · 已完成│ │ 5:12 · 渲染中│ │ 1:48 · 草稿│ │          │       │
│  │ 2026-09-08│ │ 2026-09-05│ │ 2026-09-01│ │          │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
├────────────────────────────────────────────────────────────┤
│  工程目录：C:\Users\...\DuoCast\projects\                  │
│  [打开目录]  [导入工程]                                     │
└────────────────────────────────────────────────────────────┘
```

### 2.2 元素与交互

| 元素 | 类型 | 功能 | 交互与校验 |
|---|---|---|---|
| 新建工程 | 主按钮 | 创建新工程 | 点击弹出表单：工程名（必填）、默认角色 A（从角色库选）、目标时长（1/3/5/10 分钟）；创建后在工程目录生成 `EPxxx/` 文件夹骨架 |
| 工程卡片 | 卡片 | 打开工程 | 点击进入工作台；右下角 `⋯` 菜单：重命名 / 复制 / 归档 / 删除（删除需二次确认并输入工程名） |
| 缩略图 | 图片 | 取工程内第一帧成片或底图 | 无产物时显示占位 |
| 进度标签 | 徽章 | 已完成 / 渲染中 / 草稿 / 失败 | 渲染中的工程卡片带进度环 |
| 打开目录 | 次级按钮 | 调用系统资源管理器 | — |
| 导入工程 | 次级按钮 | 从 zip 导入 | 校验目录结构，缺失则提示 |

### 2.3 工程目录结构

```
DuoCast/projects/EP001/
├── project.json           # 全局状态（见第 12 章）
├── assets/
│   ├── characters/        # 角色形象图、参考音
│   ├── scene/             # 场景图
│   └── imports/           # 用户上传的原始素材
├── script/
│   ├── article.md         # 输入的文章
│   ├── podcast.md         # 生成的播客稿
│   └── lines.json         # 分角后的逐句脚本
├── audio/
│   ├── A_full.wav         # A 整段音频
│   ├── B_full.wav         # B 整段音频
│   ├── A_track.wav        # 补静音后的 A 轨（与总时长等长）
│   ├── B_track.wav        # 补静音后的 B 轨
│   ├── mix.wav            # 混音轨（成片音轨）
│   └── lines/             # 逐句切片（用于重录）
│       ├── L001.wav
│       └── ...
├── images/
│   ├── charA.png          # 角色 A 形象图
│   ├── charB.png          # 角色 B 形象图
│   ├── scene.png          # 场景图
│   └── master.png         # 同框底图（唯一首帧）
├── masks/
│   ├── person1.png        # A 的 mask
│   ├── person2.png        # B 的 mask
│   └── background.png     # 背景 mask
├── shots/
│   └── shots.json         # 镜头表
├── render/
│   ├── queue.json         # 渲染队列状态
│   ├── seg_001.mp4        # 分段视频
│   └── ...
├── output/
│   └── final.mp4          # 成片
└── logs/
    └── run-YYYYMMDD-HHMMSS.jsonl
```

---

## 3. P1 · 角色库

> **页面目标**：建立并锁定角色资产。A 是跨期复用的固定角色，B 是每期可变的角色。本步产出的形象图与参考音，是后面所有推理的「身份锚点」。

### 3.1 布局

```
┌──────────────────────────────────────────────────────────────────────────────┐
│ 角色库                                    [从库中导入]  [导出角色包]           │
├──────────────────────────────────┬───────────────────────────────────────────┤
│ 角色 A 卡片（固定）                │ 角色 B 卡片（可变）                        │
│ ┌──────────────────────────────┐ │ ┌──────────────────────────────┐          │
│ │ [A] 主持人              🔒锁定 │ │ │ [B] 嘉宾               [换角色 ▾] │        │
│ ├──────────────────────────────┤ │ ├──────────────────────────────┤          │
│ │ 形象                            │ │ 形象                            │        │
│ │ ┌────────┐  形象类型：          │ │ ┌────────┐  形象类型：          │        │
│ │ │ 预览图  │  ○真人 ●拟人动物    │ │ │ 预览图  │  ●真人 ○拟人动物    │        │
│ │ │        │  ○卡通              │ │ │        │  ○卡通 ○拟人物件    │        │
│ │ └────────┘                     │ │ └────────┘                     │        │
│ │ [上传] [生成] [查看设定图]       │ │ [上传] [生成] [查看设定图]       │        │
│ ├──────────────────────────────┤ │ ├──────────────────────────────┤          │
│ │ 音色                            │ │ 音色                            │        │
│ │ 参考音：voice_A_01.wav (18.2s)  │ │ 参考音：voice_B_ep3.wav (15.6s) │        │
│ │ [试听] [替换]                   │ │ [试听] [替换]                   │        │
│ │ 情绪向量 ▾ [0,0,0,0,0,0,0,0.7]  │ │ 情绪向量 ▾ [0,0,0,0,0,0,0.2,0]  │        │
│ │ 语速 duration_factor  [1.00]    │ │ 语速 duration_factor  [0.95]    │        │
│ │ [合成试听句]                    │ │ [合成试听句]                    │        │
│ └──────────────────────────────┘ │ └──────────────────────────────┘          │
├──────────────────────────────────┴───────────────────────────────────────────┤
│ 角色一致性检查                                                                 │
│ ☑ A 与 B 音色有明显区分度（建议一高一低、一快一慢）                              │
│ ☑ A 与 B 形象风格统一（同一套光照与画风）                                       │
│ ☑ 两人形象均为正面或 3/4 侧，五官清晰                                           │
│ ☑ B 若为拟人物件，必须有可开合的嘴                                              │
│                                          [生成角色设定图（用于后续一致性）]      │
└──────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 元素明细

#### 3.2.1 角色卡片头部

| 元素 | 类型 | 默认 | 功能 | 交互与校验 |
|---|---|---|---|---|
| 角色标识 | 徽章 | `A`（青蓝）/ `B`（珊瑚粉） | 全局区分 | 只读 |
| 角色名 | 文本 | 「主持人」/「嘉宾」 | 自定义称呼 | 可编辑，用于文案生成时的称呼 |
| 锁定开关 | 图标按钮 | A 默认锁定 / B 默认解锁 | 锁定后不可编辑 | 点击切换；锁定态下所有输入控件置灰 + 提示「已锁定，点击解锁」 |
| 换角色 | 下拉 | 仅 B 有 | 从角色库切换 | 切换后形象图、参考音、参数全部替换，下游标过期 |

#### 3.2.2 形象区

| 元素 | 类型 | 默认 / 取值 | 功能 | 交互与校验 |
|---|---|---|---|---|
| 形象预览 | 图片 | 空态：占位人形图标 +「尚未设定形象」 | 显示当前形象图 | 点击放大查看（支持缩放、对比模式） |
| 形象类型 | 单选组 | A：真人（默认）/ 拟人动物 / 卡通<br>B：真人（默认）/ 拟人动物 / 卡通 / 拟人物件 | 界定视觉风格 | 单选互斥；切换时若已有形象图，提示「切换类型后建议重新生成形象图」 |
| 上传 | 次级按钮 | — | 上传已有形象图 | 接受 `image/*`；建议正面或 3/4 侧、肩部以上、五官无遮挡；上传后立即写入工程 `assets/characters/` |
| 生成 | 主按钮 | — | 调图生图/文生图生成形象图 | 打开生成面板（见下） |
| 查看设定图 | 文字按钮 | 生成后可用 | 查看用于一致性的「角色设定图」 | 设定图 = 正面 + 3/4 侧 + 表情三视图，用于后续底图融合时约束身份 |

#### 3.2.3 形象生成面板（点击「生成」后展开）

| 参数 | 类型 | 默认 | 说明 |
|---|---|---|---|
| 提示词 | 多行文本 | 预置模板 | 描述外貌、发型、服装、年龄、气质 |
| 负面提示词 | 多行文本 | 预置模板 | 排除变形、多手多指等 |
| 参考图 | 上传槽 | 可选 | 有参考图时走图生图，身份一致性更好 |
| 分辨率 | 下拉 | 1024×1024 | 形象图用方图 |
| 采样步数 | 数值 | 28 | 越低越快越糙 |
| CFG | 数值 | 4.5 | — |
| 种子 | 数值 + 骰子 | 随机 | 固定种子便于复现 |
| 批量张数 | 数值 | 4 | 一次出多张供挑选 |
| 生成 | 主按钮 | — | 提交后显示 4 张候选网格，点击选中即替换当前形象 |

#### 3.2.4 音色区

| 元素 | 类型 | 默认 / 取值 | 功能 | 交互与校验 |
|---|---|---|---|---|
| 参考音频槽 | 上传槽 | 空态「WAV/MP3 · 10–20 秒 · 无背景噪 · 无混响」 | 载入零样本克隆的参考音 | 接受 `audio/*`；载入后显示文件名 + 时长 + 波形缩略图；时长 <3s 或 >30s 时橙色提示但不阻止 |
| 试听 | 图标按钮 | — | 播放参考音 | 播放中按钮变暂停；再次点击停止 |
| 替换 | 文字按钮 | — | 换参考音 | 替换后下游全部标过期 |
| 情绪向量 | 8 维滑块组 | 全 0 | 控制情绪强度 | 8 维顺序固定：`[happy, angry, sad, afraid, disgusted, melancholic, surprised, calm]`；每维 0–1，步进 0.05；右侧实时显示当前向量数组 |
| 情绪预设 | 下拉 | 「平静」 | 快速套用 | 预设：平静 / 好奇 / 严肃 / 开心 / 惊讶 / 俏皮 / 委屈 / 激动；选中后自动填充 8 维 |
| `use_random` | 开关 | **关（默认且建议保持关）** | 随机采样 | 打开时显示橙色警告「会降低音色克隆保真度」 |
| `duration_factor` | 数值 | 1.00，范围 0.5–2.0 | 语速控制 | 步进 0.05；>1 变慢，<1 变快；显示「1.20 = 时长 ×1.2」 |
| 合成试听句 | 次级按钮 | — | 用固定示例句合成并播放 | 点击后按钮转「合成中…」，完成后内嵌播放器；失败显示错误条 + 重试 |
| 音色标签 | 文本 | 自动生成，如「低沉稳重·男」 | 便于区分 | 可手动改；用于提醒「A/B 音色要区分」 |

#### 3.2.5 角色一致性检查

四项勾选（可自动检测 + 人工确认）：

| 检查项 | 自动检测方式 | 未通过提示 |
|---|---|---|
| A/B 音色区分度 | 比较两条参考音的基频均值与语速 | 「两人音色接近，建议更换 B 的参考音」 |
| A/B 形象风格统一 | 人工确认（无法可靠自动判断） | — |
| 两人均为正面/3-4 侧 | 调用本地人脸/主体检测 | 「B 的形象角度偏侧，口型驱动效果可能不佳」 |
| B 有可开合的嘴 | 人工确认（拟人物件必需） | 「物件 B 必须有明确的嘴，否则无法驱动口型」 |

#### 3.2.6 生成角色设定图

- 点击后，把 A、B 的形象图与风格描述一起送入图生图模型，产出**一张包含两人的设定图**（正面 + 3/4 侧 + 表情），用于后续 P5 底图融合时作为身份约束参考。
- 这一步是「角色一致性」的关键：**后续所有出图都以设定图为参考图**，而不是各自的原图。

### 3.3 完成条件与下游

| 项 | 说明 |
|---|---|
| **本步完成条件** | A、B 各有一张形象图 + 一条参考音 |
| **检查点** | CP-1 角色确认 |
| **下游依赖** | 形象图 → P5 视觉设定；参考音 → P4 配音锁轨 |
| **失效规则** | 换形象图 → P5 及之后全部过期；换参考音 → P4 及之后全部过期 |

---

## 4. P2 · 内容输入

> **页面目标**：从「话题」或「完整文章」出发，生成一份可直接配音的播客稿。

### 4.1 布局

```
┌──────────────────────────────────────────────────────────────────────────────┐
│ 内容输入                                                                      │
├────────────────────────────┬─────────────────────────────────────────────────┤
│ 输入区                      │ 播客稿编辑区                                     │
│ ┌────────────────────────┐ │ ┌─────────────────────────────────────────────┐ │
│ │ 输入方式                │ │ │ 播客稿                        [重新生成] [版本▾]│ │
│ │ ● 话题  ○ 完整文章      │ │ ├─────────────────────────────────────────────┤ │
│ ├────────────────────────┤ │ │ # EP001 · AI 会取代创作者吗                   │ │
│ │ ┌────────────────────┐ │ │ │                                             │ │
│ │ │ 输入话题或粘贴文章  │ │ │ │ 【开场】                                     │ │
│ │ │                    │ │ │ │ A：欢迎来到《…》，我是主持人…                  │ │
│ │ │                    │ │ │ │                                             │ │
│ │ └────────────────────┘ │ │ │ 【第一段 · …】                               │ │
│ │ 字数：0 / 建议 ≤8000    │ │ │ A：…                                         │ │
│ ├────────────────────────┤ │ │ B：…                                         │ │
│ │ 生成参数                │ │ │                                             │ │
│ │ 目标时长  [3 分钟 ▾]    │ │ └─────────────────────────────────────────────┘ │
│ │ 风格      [轻松对谈 ▾]  │ │                                                 │
│ │ 口语化程度 ●──────○ 中  │ │ 结构大纲                                        │ │
│ │ 是否含开场白  [✓]       │ │ ┌─────────────────────────────────────────────┐ │
│ │ 是否含结束语  [✓]       │ │ │ 开场 (12s) · 3 段 · 结束语 (15s)             │ │
│ │ 禁用词      [输入…]     │ │ │ [拖动调整段落顺序]                            │ │
│ │ 参考视角    [第三人称▾] │ │ └─────────────────────────────────────────────┘ │
│ ├────────────────────────┤ │                                                 │
│ │ [生成播客稿]            │ │ 统计：字数 1,842 · 预估 3:12 · A 句 24 · B 句 21  │
│ └────────────────────────┘ │                                                 │
└────────────────────────────┴─────────────────────────────────────────────────┘
```

### 4.2 元素明细

#### 4.2.1 输入区

| 元素 | 类型 | 默认 / 取值 | 功能 | 交互与校验 |
|---|---|---|---|---|
| 输入方式 | 单选组 | 话题（默认）/ 完整文章 | 决定 prompt 模板 | 切换时保留已输入内容，仅换模板 |
| 输入框 | 多行文本 | 空 | 输入话题或粘贴文章 | 话题模式建议 ≤200 字；文章模式 ≤8000 字，超出转橙提示「超长文本会降低结构质量」 |
| 字数计数 | 数值 | 实时 | 辅助判断 | 只读 |
| 目标时长 | 下拉 | 1 / 3（默认）/ 5 / 10 分钟 | 控制稿件长度 | 按中文 4.2 字/秒反推目标字数并显示提示「约需 756 字」 |
| 风格 | 下拉 | 轻松对谈（默认）/ 深度访谈 / 犀利辩论 / 知识科普 / 故事叙述 | 控制语气与节奏 | — |
| 口语化程度 | 滑块 | 中 | 控制书面语→口语的转换强度 | 三档：低（接近原文）/ 中 / 高（大量口语词、语气词） |
| 含开场白 / 结束语 | 开关 | 均开 | 是否生成固定结构 | — |
| 禁用词 | 标签输入 | 空 | 生成时避免的词 | 回车添加标签；支持粘贴批量导入 |
| 参考视角 | 下拉 | 第三人称（默认）/ 第一人称 / 拟人化视角 | 影响 B 的发言立场 | 「拟人化视角」适用于拟人物件 B |
| 生成播客稿 | 主按钮 | — | 调 LLM 生成 | 输入为空时禁用；生成中显示流式输出 |

#### 4.2.2 播客稿编辑区

| 元素 | 类型 | 功能 | 交互 |
|---|---|---|---|
| 稿件正文 | 富文本编辑器 | 显示与编辑播客稿 | 支持 Markdown；段落前有 `【】` 结构标签；可直接改文字 |
| 重新生成 | 次级按钮 | 用当前参数重新生成 | 覆盖前二次确认「将覆盖当前稿件」 |
| 版本 | 下拉 | 历史版本 | 每次生成存一版，可回退与并排对比 |
| 结构大纲 | 可拖拽列表 | 显示段落结构 | 拖动调整段落顺序；每项显示段落名 + 预估时长 |
| 统计栏 | 文本 | 字数 / 预估时长 / A 句数 / B 句数 | 实时更新 |

> **注意**：本步的播客稿是「散文式」的，尚未分句标角色。分角在 P3 完成。

### 4.3 完成条件与下游

| 项 | 说明 |
|---|---|
| **完成条件** | 播客稿正文非空且已人工审阅 |
| **下游依赖** | 稿件文本 → P3 分角脚本 |
| **失效规则** | 改动稿件 → P3 及之后全部过期 |

---

## 5. P3 · 分角脚本

> **页面目标**：把播客稿拆成逐句的对话脚本，每句标注说话人（A/B）、语气、语速，形成后续配音与镜头的唯一数据源。

### 5.1 布局

```
┌──────────────────────────────────────────────────────────────────────────────┐
│ 分角脚本                     [自动分角] [合并选中] [拆分] [插入 A 句] [插入 B 句]│
├──────────────────────────────────────────────┬───────────────────────────────┤
│ 逐句脚本                                       │ 句属性检查器（选中句）         │
│ ┌──────────────────────────────────────────┐ │ ┌───────────────────────────┐ │
│ │ #01 [A] 欢迎来到《AI 会取代创作者吗》。  ▾ │ │ │ 句号：L003                 │ │
│ │ #02 [B] 我觉得这个问题问得挺好。        ▾ │ │ │ 说话人 [A ▾]               │ │
│ │ #03 [A] 那你先说，你觉得会吗？          ▾ │ │ │ 文本 [多行文本域]           │ │
│ │ #04 [B] 会，也不会。                    ▾ │ │ │ 语气 [平静 ▾]              │ │
│ │ ...                                       │ │ │ 局部语速 [1.00]            │ │
│ │                                           │ │ │ 前置停顿 [0.30 s]          │ │
│ │                                           │ │ │ 后置停顿 [0.35 s]          │ │
│ │                                           │ │ │ 预估时长 [2.4 s]           │ │
│ └──────────────────────────────────────────┘ │ │ [重录本句] [删除本句]       │ │
│                                               │ └───────────────────────────┘ │
│ 统计：A 24 句 / B 21 句 · 总字数 1,842 · 预估 3:12                            │
└──────────────────────────────────────────────┴───────────────────────────────┘
```

### 5.2 元素明细

#### 5.2.1 脚本列表

| 元素 | 类型 | 功能 | 交互与校验 |
|---|---|---|---|
| 句行 | 行组件 | 句号 + 说话人标签 + 文本 + 展开箭头 | 点击选中并在右检查器显示；双击直接编辑文本 |
| 说话人标签 | 可点击徽章 | A（青蓝）/ B（珊瑚粉） | **点击在 A↔B 之间切换**；切换后该句及之后的时间轨标过期 |
| 文本 | 内联文本域 | 编辑台词 | 自动增高；为空时行变橙 |
| 展开箭头 | 图标 | 展开该句的高级属性 | 展开后内联显示语气 / 语速 / 停顿 |
| 拖动把手 | 图标 | 调整句序 | 拖拽排序，带插入位置指示线 |
| 删除 | 图标按钮 | 删除该句 | 至少保留 1 句；删除后自动重新编号 |

#### 5.2.2 顶部工具条

| 按钮 | 功能 | 交互 |
|---|---|---|
| 自动分角 | 调 LLM 把播客稿拆成逐句并分配说话人 | 覆盖前二次确认；生成后保留原稿可回退 |
| 合并选中 | 把选中的连续句合并为一句 | 需选中 ≥2 句；合并后说话人取第一句 |
| 拆分 | 把光标处拆成两句 | 光标必须在文本中间 |
| 插入 A 句 / B 句 | 在当前句后插入空句 | 自动聚焦 |
| 批量操作 | 下拉 | 全部设为 A / 全部设为 B / 重新自动分角 |

#### 5.2.3 句属性检查器

| 参数 | 类型 | 默认 | 说明 |
|---|---|---|---|
| 说话人 | 下拉 | 该句当前值 | A / B |
| 文本 | 多行文本 | 该句文本 | — |
| 语气 | 下拉 | 平静 | 平静 / 好奇 / 严肃 / 开心 / 惊讶 / 俏皮 / 委屈 / 激动；**会映射到情绪向量** |
| 局部语速 | 数值 | 1.00，0.5–2.0 | 覆盖角色级 `duration_factor`；显示「继承角色设定」的快捷复位 |
| 前置停顿 | 数值 | 0.30s，0–2.0 | 该句前插入的静音；**这是切点的天然位置** |
| 后置停顿 | 数值 | 0.35s，0–2.0 | 该句后插入的静音 |
| 预估时长 | 只读 | 按字数与语速估算 | 合成后由真实时长覆盖 |
| 重录本句 | 次级按钮 | — | 只重合成这一句（需 P4 已执行过） |
| 删除本句 | 危险按钮 | — | 二次确认 |

### 5.3 分角规则（自动分角）

LLM 分角时的约束（写进 prompt）：

1. **一句一个说话人**，不出现「A 和 B 同时说」（若需要，单独标记为 `both`，走 `para` 模式）。
2. 每句 ≤60 字，超过则拆分。
3. A 负责提问、串联、总结；B 负责主体内容。
4. 保留口语词与语气词（视口语化程度）。
5. 每句输出结构化 JSON：`{who, text, tone}`。

### 5.4 完成条件与下游

| 项 | 说明 |
|---|---|
| **完成条件** | ≥1 句台词，且无空句、无未分配说话人的句 |
| **检查点** | CP-2 稿件确认 |
| **下游依赖** | `lines[]` → P4 配音锁轨 |
| **失效规则** | 改动任一句 → P4 及之后全部过期（改说话人 → 两条整段音频都需重合成） |

---

## 6. P4 · 配音锁轨

> **页面目标**：**按说话人整段合成音频**，然后用 ASR 把句级时间码回填到时间轨。本步产出的时间轨是后续所有画面工作的**唯一坐标系**。

### 6.1 布局

```
┌──────────────────────────────────────────────────────────────────────────────┐
│ 配音锁轨                                        [开始合成] [重新锁轨] [导出音轨]│
├──────────────────────────────────────────────┬───────────────────────────────┤
│ 合成区                                        │ 合成参数（右检查器）           │
│ ┌──────────────────────────────────────────┐ │ ┌───────────────────────────┐ │
│ │ [A] 主持人  24 句 · 1,024 字              │ │ │ 合成粒度                   │ │
│ │ 参考音 voice_A_01.wav · 情绪 [平静]        │ │ │ ● 按说话人整段（推荐）      │ │
│ │ [▶ 试听整段] [波形缩略] 时长 1:48.320      │ │ │ ○ 逐句合成                 │ │
│ ├──────────────────────────────────────────┤ │ ├───────────────────────────┤ │
│ │ [B] 嘉宾  21 句 · 818 字                  │ │ │ 采样参数                   │ │
│ │ 参考音 voice_B_ep3.wav · 情绪 [好奇]       │ │ │ 温度        [0.8]          │ │
│ │ [▶ 试听整段] [波形缩略] 时长 1:24.600      │ │ │ 停顿插入    [自动]         │ │
│ ├──────────────────────────────────────────┤ │ ├───────────────────────────┤ │
│ │ 对齐结果                                   │ │ │ 时间码回填                 │ │
│ │ ┌────────────────────────────────────────┐│ │ │ ASR 模型 [faster-whisper ▾]│ │
│ │ │ 句号 说话人 开始     结束     时长      ││ │ │ 对齐方式 [强制对齐 ▾]      │ │
│ │ │ L001  A     00:00.000 00:02.480 2.480  ││ │ │ 最小可信度 [0.6]          ││ │
│ │ │ L002  B     00:02.980 00:05.120 2.140  ││ │ └───────────────────────────┘ │
│ │ │ L003  A     00:05.620 00:08.040 2.420  ││ │                               │
│ │ │ ...                                    ││ │ 对齐质量                       │
│ │ └────────────────────────────────────────┘│ │ ┌───────────────────────────┐ │
│ │ 对齐质量：38/45 句高可信 · 7 句需人工确认  │ │ │ 低可信句列表                │ │
│ └──────────────────────────────────────────┘ │ │ L017  0.42  [跳转] [重录]  │ │
│                                               │ │ L029  0.51  [跳转] [重录]  │ │
│                                               │ └───────────────────────────┘ │
└──────────────────────────────────────────────┴───────────────────────────────┘
```

### 6.2 元素明细

#### 6.2.1 合成区

| 元素 | 类型 | 功能 | 交互与校验 |
|---|---|---|---|
| 角色合成卡 | 卡片 | 显示该角色待合成文本量、参考音、情绪、试听、时长 | 点击「试听整段」内嵌播放；合成中显示进度条 |
| 开始合成 | 主按钮 | 触发 A、B 两条整段合成 | 无脚本时禁用；合成中显示「A 合成中 3/2…」；失败显示错误条 + 重试 |
| 重新锁轨 | 次级按钮 | 重跑 ASR 对齐 | 音频不变只重对齐时用，成本低 |
| 导出音轨 | 次级按钮 | 导出 `A_track.wav` / `B_track.wav` / `mix.wav` | — |

#### 6.2.2 对齐结果表

| 列 | 说明 |
|---|---|
| 句号 | `L001` 形式，可点击跳转时间轨 |
| 说话人 | A/B 徽章 |
| 开始 / 结束 | 等宽时间码 `mm:ss.mmm` |
| 时长 | 秒，等宽 |
| 置信度 | 0–1，<0.6 转橙 |
| 操作 | 跳转 / 播放该句 / 重录该句 / 手动改时间码 |

#### 6.2.3 合成参数

| 参数 | 类型 | 默认 | 说明 |
|---|---|---|---|
| 合成粒度 | 单选 | **按说话人整段（推荐）** | 整段合成音色更稳；逐句合成为备选 |
| 温度 | 数值 | 0.8 | 越高越有变化，但音色稳定性下降 |
| 停顿插入 | 下拉 | 自动 | 自动 / 固定 / 按标点；控制句间静音 |
| ASR 模型 | 下拉 | faster-whisper large-v3 | 用于回填句级时间码 |
| 对齐方式 | 下拉 | 强制对齐 | 强制对齐（准）/ 分段检测（快） |
| 最小可信度 | 数值 | 0.6 | 低于此值列入人工确认列表 |

### 6.3 关键算法：整段合成 + 句级回填

```
输入：lines[] = [{who, text, tone, pre_pause, post_pause, rate}]

1. 分组
   groupA = lines 中 who==A 的连续段落合并成一段文本（保留句间标点）
   groupB = lines 中 who==B 的连续段落合并成一段文本

   ⚠️ 注意：不是把 A 的所有台词无视顺序拼一起，
      而是「同一说话人的连续段落」各成一段。
      若 A 的台词被 B 打断成 5 段，则 A 有 5 个合成段。

2. 逐段合成
   for seg in groupA:
       audio = IndexTTS.infer(
           spk_audio_prompt = 角色A参考音,
           text             = seg.text,
           emo_vector       = 角色A情绪向量 ⊕ 该段语气映射,
           emo_alpha        = 1.0,
           duration_factor  = 角色A语速 × 该段局部语速,
           use_random       = False,
           lang             = "ZH"
       )
   拼接所有段 → A_full.wav（段间插入 post_pause + pre_pause 的静音）

3. 句级时间码回填（ASR 强制对齐）
   align(A_full.wav, A的逐句文本) → 每句的 [start, end]
   置信度 < 阈值 → 标记人工确认

4. 构造两条等长轨
   总时长 T = A_full 时长 + B_full 时长 + 全部停顿
   A_track = 在 T 的坐标系里，A 的每句放在其 [start, end]，其余补零
   B_track = 同理
   （二者长度严格相等，这是 add 模式的前置条件）

5. 混音
   mix = A_track + B_track（两人不会重叠，因为脚本保证不抢话）

6. 写入工程
   audio/lines/Lxxx.wav  ← 逐句切片，供单句重录
```

### 6.4 完成条件与下游

| 项 | 说明 |
|---|---|
| **完成条件** | A、B 两条音频已合成，且时间码已回填（低可信句可人工确认或接受） |
| **检查点** | CP-4 配音确认 |
| **下游依赖** | 时间轨 → P6 镜头表；音频 → P7/P8 渲染 |
| **失效规则** | 改动任一句 → 该说话人的整段需重合成 → 时间轨 → 镜头表 → 试镜 → 成片全部过期 |

---

## 7. P5 · 视觉设定

> **页面目标**：用**图生图融合**生成一张「两人已在场景中」的同框底图。这是全片唯一的首帧，决定所有后续渲染的一致性。

### 7.1 布局

```
┌──────────────────────────────────────────────────────────────────────────────┐
│ 视觉设定                              [生成同框底图] [重新生成] [版本 ▾]       │
├──────────────────────────────┬───────────────────────────────────────────────┤
│ 素材区                        │ 同框底图                                        │
│ ┌──────────────────────────┐ │ ┌───────────────────────────────────────────┐ │
│ │ 角色 A 形象                │ │ │                                           │ │
│ │ ┌────┐  设定图 [查看]     │ │ │            [同框底图 16:9]                 │ │
│ │ │    │                   │ │ │                                           │ │
│ │ └────┘                   │ │ │   A ◀──── 居中留空 ────▶ B               │ │
│ ├──────────────────────────┤ │ │                                           │ │
│ │ 角色 B 形象                │ │ └───────────────────────────────────────────┘ │
│ │ ┌────┐  设定图 [查看]     │ │ 站位标注                                        │
│ │ │    │                   │ │ ┌───────────────────────────────────────────┐ │
│ │ └────┘                   │ │ │ 左侧 A 区 [x,y,w,h]  右侧 B 区 [x,y,w,h]  │ │
│ ├──────────────────────────┤ │ │ [自动检测] [手动框选] [重置]                │ │
│ │ 场景图                    │ │ └───────────────────────────────────────────┘ │
│ │ ┌──────────────────────┐ │ │ Mask 预览                                       │
│ │ │  [场景预览]           │ │ │ ┌────────┐ ┌────────┐ ┌────────┐              │
│ │ │                      │ │ │ │ A mask │ │ B mask │ │ 背景    │              │
│ │ └──────────────────────┘ │ │ └────────┘ └────────┘ └────────┘              │
│ │ [上传] [生成场景]         │ │ [导出 mask] [手动精修]                          │
│ └──────────────────────────┘ │                                                 │
└──────────────────────────────┴───────────────────────────────────────────────┘
```

### 7.2 元素明细

#### 7.2.1 素材区

| 元素 | 类型 | 功能 | 交互与校验 |
|---|---|---|---|
| 角色 A/B 形象 | 缩略图 + 查看 | 显示来自 P1 的形象图与设定图 | 点击「查看」放大；下方显示「来自 P1 · 已锁定」 |
| 场景图 | 上传槽 / 生成 | 播客场景底图（不含人） | 上传或生成；生成参数见下 |
| 生成场景 | 主按钮 | 调图生图生成场景 | 提示词预置：「固定机位、16:9、为两位角色预留左右站位、不含人物、暖光」 |

#### 7.2.2 场景生成面板

| 参数 | 类型 | 默认 | 说明 |
|---|---|---|---|
| 场景模板 | 单选 | 暖光播客客厅（默认）/ 双人沙发对谈 / 极简演播室 / 户外咖啡馆 | 选中后替换提示词（手改过则确认） |
| 提示词 | 多行文本 | 模板 | **必须含「预留左右站位、居中留空、不含人物」** |
| 分辨率 | 下拉 | 1280×720 | 16:9 |
| 采样步数 / CFG / 种子 | 数值 | 28 / 4.5 / 随机 | — |

#### 7.2.3 同框底图生成（核心）

| 参数 | 类型 | 默认 | 说明 |
|---|---|---|---|
| 参考图 | 只读列表 | A 形象图 + B 形象图 + 设定图 | **融合输入**，不是贴图 |
| 提示词 | 多行文本 | 模板 | 描述「A 在左、B 在右、两人对谈、固定机位、16:9、居中留空」 |
| 融合强度 | 数值 | 0.75，0–1 | 越高越贴近参考图身份，越低越自由 |
| 保留结构 | 开关 | 开 | 是否保留场景图的空间结构 |
| 采样步数 / CFG / 种子 | 数值 | 30 / 4.5 / 固定 | **种子必须固定并记录**，用于后续所有分段 |
| 生成 | 主按钮 | — | 提交后出 4 张候选，点击选中 |

#### 7.2.4 站位标注与 Mask

| 元素 | 类型 | 功能 | 交互与校验 |
|---|---|---|---|
| 站位标注 | 可拖拽矩形 ×2 | 标注 A、B 在图中的包围盒 | 「自动检测」调本地主体检测给出建议框；「手动框选」在图上拖拽；坐标以原图分辨率为基准 |
| A/B 区域坐标 | 等宽文本 | 显示 `[x, y, w, h]` | 可手动输入微调 |
| Mask 预览 | 三张缩略图 | A mask / B mask / 背景 mask | 点击放大 |
| 导出 mask | 次级按钮 | 写入 `masks/*.png` | 尺寸与底图严格一致 |
| 手动精修 | 次级按钮 | 打开笔刷编辑器 | 降级手段：仅当自动 mask 不理想时使用 |

> **重要约束（来自 InfiniteTalk 实现）**：多人物模式下默认 mask 是**硬切的左半/右半**。因此底图必须让两人**左右分居、居中留空**。若两人前后错开或偏中，必须用 bbox 校正。UI 需在生成前就把这个约束显示出来。

### 7.3 完成条件与下游

| 项 | 说明 |
|---|---|
| **完成条件** | 同框底图已生成/上传，且 A/B 站位标注已确认 |
| **检查点** | CP-3 底图确认 |
| **下游依赖** | 底图 + mask → P6 镜头表 → P7 试镜 → P8 渲染 |
| **失效规则** | 换底图 → 镜头表、试镜、成片全部过期 |

---

## 8. P6 · 镜头表

> **页面目标**：基于时间轨的静音间隙，生成可切点，为每个镜头段配置镜头类型与数字运镜。**不做摇镜**，只做切镜 + 轻微推拉。

### 8.1 布局

```
┌──────────────────────────────────────────────────────────────────────────────┐
│ 镜头表                  [自动生成镜头表] [添加段] [合并段] [导出滤镜链]       │
├──────────────────────────────────────────────────┬───────────────────────────┤
│ 镜头段列表                                        │ 段属性（选中段）           │
│ ┌──────────────────────────────────────────────┐ │ ┌───────────────────────┐ │
│ │ 段  时间范围        时长   类型      运镜      │ │ │ 段号：S03             │ │
│ │ S01 00:00.000–00:12.4 12.4 同框      推近     │ │ │ 起止 00:24.6–00:41.2  │ │
│ │ S02 00:12.4–00:24.6  12.2 单拍 A    静止     │ │ │ 时长 16.6s            │ │
│ │ S03 00:24.6–00:41.2  16.6 同框      静止     │ │ │ 类型 [同框 ▾]         │ │
│ │ S04 00:41.2–00:55.0  13.8 单拍 B    轻微推近 │ │ │ 运镜 [轻微推近 ▾]     │ │
│ │ ...                                          │ │ │ 强度 [0.15]           │ │
│ └──────────────────────────────────────────────┘ │ │ 缓动 [线性 ▾]         │ │
│                                                   │ │ 裁切框 [x,y,w,h]      │ │
│ 切点提示                                          │ │ [预览运镜]            │ │
│ ┌──────────────────────────────────────────────┐ │ └───────────────────────┘ │
│ │ 12 个切点 · 10 个落在静音间隙 ✓               │ │                           │
│ │ 2 个落在说话中间 ⚠  [查看并调整]              │ │ 全局参数                   │
│ └──────────────────────────────────────────────┘ │ ┌───────────────────────┐ │
│                                                   │ │ 最短段长 [3.0s]       │ │
│                                                   │ │ 最短静音 [0.25s]      │ │
│                                                   │ │ 裁切缩放 [1.6×]       │ │
│                                                   │ │ 推拉强度上限 [0.25]   │ │
│                                                   │ └───────────────────────┘ │
└──────────────────────────────────────────────────┴───────────────────────────┘
```

### 8.2 镜头类型

| 类型 | 说明 | 实现 |
|---|---|---|
| **同框** | 完整底图，两人都在画面里 | 无裁切，可叠加轻微推拉 |
| **单拍 A** | 裁切到 A 的区域并放大 | 裁切框 = A 站位框向外扩一定比例 |
| **单拍 B** | 裁切到 B 的区域并放大 | 同上 |

### 8.3 运镜类型（全部为数字运镜）

| 运镜 | 说明 | 参数 |
|---|---|---|
| **静止** | 不动 | — |
| **轻微推近** | 缓慢放大（zoom in） | 强度 0.05–0.25（最终缩放 = 1 + 强度） |
| **轻微拉远** | 缓慢缩小（zoom out） | 同上 |
| **轻微平移** | 画面内横向微移 | 强度 0.05–0.25（位移占画幅比例） |
| **推近 + 平移** | 组合 | 两项参数 |

> **为什么不摇镜**：播客语境下摇镜不真实；且生成式运镜会导致背景几何漂移。数字运镜是后期在已渲染视频上做，零漂移、零额外算力。

### 8.4 元素明细

#### 8.4.1 镜头段列表

| 元素 | 类型 | 功能 | 交互 |
|---|---|---|---|
| 段行 | 行组件 | 段号 / 时间范围 / 时长 / 类型 / 运镜 | 点击选中；双击时间范围可编辑；右侧拖拽把手调边界 |
| 类型下拉 | 下拉 | 同框 / 单拍 A / 单拍 B | 切换后自动建议裁切框 |
| 运镜下拉 | 下拉 | 静止 / 推近 / 拉远 / 平移 / 组合 | 切换后显示对应强度参数 |
| 添加段 | 次级按钮 | 在播放头处切一刀 | 新段继承前一段类型 |
| 合并段 | 次级按钮 | 合并选中段 | 需选中 ≥2 段 |
| 导出滤镜链 | 次级按钮 | 生成 ffmpeg 滤镜表达式 | 写入 `shots/filters.json`，可在 P8 前人工审阅 |

#### 8.4.2 自动生成镜头表

算法（见 8.5），产出后：
- 落在静音间隙的切点标绿勾
- 落在说话中间的切点标橙警告 + 一键吸附到最近静音间隙

#### 8.4.3 段属性检查器

| 参数 | 类型 | 默认 | 说明 |
|---|---|---|---|
| 段号 | 只读 | 自动 | `S01`… |
| 起止 / 时长 | 只读 | 自动 | 等宽时间码 |
| 类型 | 下拉 | 自动建议 | 同框 / 单拍 A / 单拍 B |
| 运镜 | 下拉 | 静止 | 见 8.3 |
| 强度 | 数值 | 0.12 | 0.05–0.25 |
| 缓动 | 下拉 | 线性 | 线性 / ease-in / ease-out / ease-in-out |
| 裁切框 | 数值组 | 自动 | `x, y, w, h`，以底图分辨率为基准 |
| 预览运镜 | 次级按钮 | — | 用底图静态图模拟运镜效果（快速预览，不渲染） |

#### 8.4.4 全局参数

| 参数 | 类型 | 默认 | 说明 |
|---|---|---|---|
| 最短段长 | 数值 | 3.0s | 低于此长度不切镜 |
| 最短静音 | 数值 | 0.25s | 静音长于此值才认为可切 |
| 裁切缩放 | 数值 | 1.6× | 单拍时裁切区域放大到原尺寸的倍数 |
| 推拉强度上限 | 数值 | 0.25 | 防止运镜过猛 |

### 8.5 关键算法：自动生成镜头表

```
输入：时间轨（句级 [start,end]）、镜头全局参数

1. 找出所有静音间隙
   gaps = []
   cursor = 0
   for line in lines（按时间排序）:
       if line.start - cursor >= min_silence:
           gaps.append((cursor, line.start))
       cursor = line.end

2. 找候选切点
   切点必须落在某个 gap 内。对每个 gap，取其中点作为候选切点。

3. 按最短段长过滤
   从起点开始，累积时长 >= min_shot_len 时，在最近的下一个 gap 中点切一刀。
   末尾不足 min_shot_len 的段并入前一段。

4. 为每段判定类型（启发式）
   - 若段内 A 的发言占比 >= 70% → 单拍 A
   - 若段内 B 的发言占比 >= 70% → 单拍 B
   - 否则 → 同框
   （用户可逐段覆盖）

5. 为每段分配运镜（启发式，保守）
   - 连续同类型段超过 2 段 → 第 3 段起加轻微推近/拉远，制造变化
   - 段长 > 20s → 允许一次推近
   - 默认静止

6. 输出 shots[]
   [{id, start, end, type, camera:{move, strength, easing, crop}}]
```

### 8.6 完成条件与下游

| 项 | 说明 |
|---|---|
| **完成条件** | 所有镜头段均已配置类型与运镜；切点警告已处理或已知接受 |
| **检查点** | CP-5 镜头确认 |
| **下游依赖** | `shots[]` → P7 试镜 → P8 渲染 |
| **失效规则** | 改时间轨 → 镜头表需重算；改镜头表 → 成片过期 |

---

## 9. P7 · 试镜

> **页面目标**：用 5 秒样片低成本验证口型、非说话人静止、画面稳定、风格统一。**通过后才投入长片渲染。**

### 9.1 布局

```
┌──────────────────────────────────────────────────────────────────────────────┐
│ 试镜                                                      [生成试镜样片]        │
├──────────────────────────────────────┬───────────────────────────────────────┤
│ 样片预览                              │ 验收清单                               │
│ ┌──────────────────────────────────┐ │ ┌───────────────────────────────────┐ │
│ │                                  │ │ │ ☐ ① 嘴型跟得上音频                │ │
│ │       [16:9 视频播放器]           │ │ │ ☐ ② 不说话的人不抢戏（保持安静）   │ │
│ │                                  │ │ │ ☐ ③ 脸/物体不变形、背景不扭曲      │ │
│ │                                  │ │ │ ☐ ④ 风格与设定图统一              │ │
│ └──────────────────────────────────┘ │ └───────────────────────────────────┘ │
│ 规格：5 秒 / 125 帧 · 832×480 · 种子 20260908 │ 四项全勾才能进入长片               │
├──────────────────────────────────────┤                                       │
│ 试镜设置                              │ 回退路径                               │
│ ┌──────────────────────────────────┐ │ ┌───────────────────────────────────┐ │
│ │ 样片区间 [自动选一段 ▾]           │ │ │ 嘴不动/乱动 → 回 P5 收紧 mask     │ │
│ │ 起始时间 00:24.600                │ │ │ 画面崩    → 回 P5 换更标准底图     │ │
│ │ 时长     [5 s ▾]                  │ │ │ 音色不对  → 回 P1 换参考音         │ │
│ │ 分辨率   [832×480 ▾]              │ │ │ 不抢戏    → 回 P6 调整段类型       │ │
│ │ 步数     [8]                      │ │ └───────────────────────────────────┘ │
│ │ 种子     [20260908]  [🔒固定]     │ │                                       │
│ │ 模式     ●add（交替）○para（并行）│ │                                       │
│ │ 音频 CFG [4.0]                    │ │                                       │
│ │ motion_frame [9]                  │ │                                       │
│ └──────────────────────────────────┘ │                                       │
└──────────────────────────────────────┴───────────────────────────────────────┘
```

### 9.2 元素明细

| 元素 | 类型 | 默认 / 取值 | 功能 | 交互与校验 |
|---|---|---|---|---|
| 样片区间 | 下拉 | 自动选一段（优先选「A 说完 B 接话」的段） | 选择试镜片段 | 可选「自动选一段 / 手动指定 / 选最长段」 |
| 时长 | 下拉 | 2s / 5s（默认）/ 10s | 样片长度 | 帧数 = 时长 × 25fps |
| 分辨率 | 下拉 | 832×480（默认）/ 480p / 720p | 样片分辨率 | — |
| 步数 | 数值 | 8 | 蒸馏后的采样步数 | 越低越快 |
| 种子 | 数值 + 锁 | 20260908，默认锁定 | 固定种子 | **锁定后与长片分段一致**，保证连贯 |
| 模式 | 单选 | `add`（交替，默认）/ `para`（并行） | 多音频模式 | 交替对话用 add；抢话用 para |
| 音频 CFG | 数值 | 4.0 | 口型同步强度 | 官方推荐 3–5；越高越准但可能不自然 |
| motion_frame | 数值 | 9 | 运动幅度 | 官方默认 9 |
| 生成试镜样片 | 主按钮 | — | 提交渲染 | 生成中显示进度条（来自 WebSocket）；失败显示错误 + 重试 |
| 验收清单 | 勾选 ×4 | 全未勾 | 人工验收 | 勾中变绿；**四项全勾才解锁 P8 的「开始队列渲染」** |

### 9.3 完成条件与下游

| 项 | 说明 |
|---|---|
| **完成条件** | 样片已生成且四项验收全勾 |
| **检查点** | CP-6 试镜确认 |
| **下游依赖** | 通过 → P8 解锁长片渲染 |
| **注意** | 未通过不硬锁，但 P8 会显示橙色提示「建议先通过试镜」 |

---

## 10. P8 · 渲染与导出

> **页面目标**：把整片按镜头段切成 15–30 秒分段逐段渲染（串行、可续跑），拼接后用 ffmpeg 应用数字运镜，RIFE 补帧，导出成片。

### 10.1 布局

```
┌──────────────────────────────────────────────────────────────────────────────┐
│ 渲染与导出                          [开始队列渲染] [暂停] [从失败段继续] [导出]│
├──────────────────────────────┬───────────────────────────────────────────────┤
│ 渲染设置                      │ 渲染队列                          总进度 34%   │
│ ┌──────────────────────────┐ │ ┌───────────────────────────────────────────┐ │
│ │ 单段时长 [20 s ▾]         │ │ │ #01 00:00–00:20 20.0s 1250帧 ███████ 100% ✓│ │
│ │ 分辨率   [480p ▾]         │ │ │ #02 00:20–00:41 21.0s 1313帧 ███████ 100% ✓│ │
│ │ 步数     [8]              │ │ │ #03 00:41–01:02 21.0s 1313帧 ████░░░  62% │ │
│ │ 音频 CFG [4.0]            │ │ │ #04 01:02–01:23 21.0s 1313帧 ░░░░░░░   0%  │ │
│ │ motion_frame [9]          │ │ │ ...                                        │ │
│ │ 种子     [20260908] 🔒    │ │ └───────────────────────────────────────────┘ │
│ │ 段间重叠 [25 帧]          │ │                                               │
│ ├──────────────────────────┤ │ 后处理                                        │
│ │ ☑ RIFE 补帧 → 50fps       │ │ ┌───────────────────────────────────────────┐ │
│ │ ☑ 应用数字运镜            │ │ │ 拼接   ✓ 完成  final_raw.mp4              │ │
│ │ ☑ 交叉融合拼接            │ │ │ 运镜   ⏳ 等待                            │ │
│ │ ☑ 音画合并                │ │ │ 补帧   ⏳ 等待                            │ │
│ ├──────────────────────────┤ │ │ 导出   ⏳ 等待                            │ │
│ │ 预算（只读）              │ │ └───────────────────────────────────────────┘ │
│ │ 总帧数   3,853            │ │                                               │
│ │ 分段数   12               │ │ 成片预览                                       │
│ │ 预计耗时 2h 14m           │ │ ┌───────────────────────────────────────────┐ │
│ └──────────────────────────┘ │ │ [播放器]  输出：output/final.mp4           │ │
│                               │ └───────────────────────────────────────────┘ │
└──────────────────────────────┴───────────────────────────────────────────────┘
```

### 10.2 元素明细

#### 10.2.1 渲染设置

| 参数 | 类型 | 默认 | 说明 |
|---|---|---|---|
| 单段时长 | 下拉 | 15 / 20（默认）/ 30 秒 | 切换后重算分段与预算 |
| 分辨率 | 下拉 | 480p（默认）/ 720p | 720p 提示显存需求显著增加 |
| 步数 | 数值 | 8 | 蒸馏后步数 |
| 音频 CFG | 数值 | 4.0 | 3–5 |
| motion_frame | 数值 | 9 | 运动幅度 |
| 种子 | 数值 + 锁 | 固定 | 与试镜一致 |
| 段间重叠 | 数值 | 25 帧 | 保证拼接连贯 |
| RIFE 补帧 | 开关 | 开 | 25fps → 50fps |
| 应用数字运镜 | 开关 | 开 | 按 P6 镜头表执行 |
| 交叉融合拼接 | 开关 | 开 | 重叠区交叉淡化 |
| 音画合并 | 开关 | 开 | 合入 `mix.wav` |

#### 10.2.2 渲染队列

| 元素 | 类型 | 内容 | 交互 |
|---|---|---|---|
| 段行 | 队列行 | 段号 / 时间范围 / 时长 / 帧数 / 进度条 / 状态 | 状态：`待渲染`(灰) → `渲染中`(橙+进度) → `已完成`(绿勾) → `失败`(红+重试) |
| 总进度 | 百分比 | 已完成段 / 总段数 | 100% 时启用导出 |
| 暂停 | 次级按钮 | 暂停队列 | 当前段完成后暂停 |
| 从失败段继续 | 次级按钮 | 跳过已完成段 | 失败时出现 |

#### 10.2.3 后处理状态

四步流水线，每步显示状态：`等待` / `进行中` / `完成` / `失败`。

| 步骤 | 输入 | 输出 | 工具 |
|---|---|---|---|
| 拼接 | `seg_*.mp4` | `final_raw.mp4` | ffmpeg concat + 交叉融合 |
| 运镜 | `final_raw.mp4` + `shots.json` | `final_cam.mp4` | ffmpeg zoompan / crop 滤镜链 |
| 补帧 | `final_cam.mp4` | `final_60fps.mp4` | RIFE |
| 导出 | 视频 + `mix.wav` | `output/final.mp4` | ffmpeg mux |

### 10.3 完成条件

| 项 | 说明 |
|---|---|
| **完成条件** | 全部分段已完成，后处理四步完成，成片可播放 |
| **下游** | 无（流程终点） |
| **产物** | `output/final.mp4` + `logs/run-*.jsonl` |

---

## 11. P9 · 一键生成

> **页面目标**：把已打磨通畅的流程串成自动流水线。只在用户勾选的确认关卡处暂停。

### 11.1 布局

```
┌──────────────────────────────────────────────────────────────────────────────┐
│ 一键生成                                                     [开始] [取消]    │
├──────────────────────────────────────┬───────────────────────────────────────┤
│ 前置资产                              │ 确认关卡配置                           │
│ ┌──────────────────────────────────┐ │ ┌───────────────────────────────────┐ │
│ │ 角色 A  🔒 已锁定                 │ │ │ ● 必须确认                         │ │
│ │   形象 ✓ 音色 ✓                  │ │ │ ○ 自动通过                         │ │
│ ├──────────────────────────────────┤ │ │ ○ 跳过                             │ │
│ │ 角色 B                            │ │ ├───────────────────────────────────┤ │
│ │   [从角色库选择 ▾] 或 [新建]      │ │ │ ☑ CP-1 角色确认                    │ │
│ │   形象 ✓ 音色 ✓                  │ │ │ ☑ CP-2 稿件确认                    │ │
│ ├──────────────────────────────────┤ │ │ ☑ CP-3 底图确认                    │ │
│ │ 固定参数                          │ │ │ ☑ CP-4 配音确认                    │ │
│ │   目标时长 [3 分钟 ▾]             │ │ │ ☑ CP-5 镜头确认                    │ │
│ │   风格 [轻松对谈 ▾]               │ │ │ ☑ CP-6 试镜确认                    │ │
│ │   分辨率 [480p ▾]                 │ │ └───────────────────────────────────┘ │
│ └──────────────────────────────────┘ │                                       │
│                                       │ 执行进度                               │
│ 输入                                  │ ┌───────────────────────────────────┐ │
│ ┌──────────────────────────────────┐ │ │ ① 角色库        ✓ 已完成           │ │
│ │ 话题或文章                        │ │ │ ② 内容输入      ✓ 已完成           │ │
│ │ [                                  │ │ │ ③ 分角脚本      ✓ 已完成           │ │
│ │                                    │ │ │ ④ 配音锁轨      ✓ 已完成           │ │
│ │                                  ] │ │ │ ⑤ 视觉设定      ✓ 已完成           │ │
│ └──────────────────────────────────┘ │ │ ⑥ 镜头表        ⏸ 等待确认 CP-5    │ │
│                                       │ │ ⑦ 试镜          ○ 待执行           │ │
│                                       │ │ ⑧ 渲染与导出    ○ 待执行           │ │
│                                       │ └───────────────────────────────────┘ │
└──────────────────────────────────────┴───────────────────────────────────────┘
```

### 11.2 三种工作模式

| 模式 | 说明 | 适用 |
|---|---|---|
| **全手动** | 逐步执行，每步参数全暴露 | 首次打磨、调试 |
| **关卡确认** | 自动执行到关卡停下等确认 | 日常生产 |
| **全自动** | 全部关卡自动通过 | 批量生产（前提是已充分验证） |

> 模式选择保存在工程里，可在任意时刻切换。

### 11.3 执行规则

1. **前置校验**：开始前检查角色资产、服务连通性、模型就绪。任一缺失则禁用「开始」并说明。
2. **逐步执行**：按 ①→⑧ 顺序执行，每步完成后写入 `state.done`。
3. **关卡暂停**：到达配置为「必须确认」的关卡时暂停，弹出 `CheckpointPanel`。
4. **失败处理**：任一步失败 → 停止，保留已完成产物，显示失败原因 + 「从失败步继续」。
5. **产物保留**：一键生成的每一步产物与手动模式完全一致，**可事后回到任意步微调并局部重跑**。
6. **日志**：每步的参数、耗时、产物路径写入 `logs/run-*.jsonl`。

---

## 12. 数据模型

### 12.1 全局状态 `state`

```typescript
interface ProjectState {
  // ---- 元信息 ----
  id: string;                    // EP001
  name: string;                  // 工程名
  createdAt: string;             // ISO
  updatedAt: string;
  version: string;               // 文档版本，用于迁移

  // ---- 导航 ----
  step: StepId;                  // 'P1'|'P2'|...|'P8'
  done: Record<StepId, boolean>;
  stale: Record<StepId, boolean>;      // 下游过期标记

  // ---- 模式 ----
  mode: 'manual' | 'checkpoint' | 'auto';
  checkpoints: Record<CheckpointId, 'ask' | 'auto' | 'skip'>;

  // ---- P1 角色库 ----
  characters: {
    A: Character;
    B: Character;
  };
  characterSetSheet?: AssetRef;        // 角色设定图

  // ---- P2 内容输入 ----
  content: {
    inputType: 'topic' | 'article';
    rawInput: string;
    targetDuration: number;            // 秒
    style: string;
    colloquial: 0 | 1 | 2;             // 低/中/高
    includeIntro: boolean;
    includeOutro: boolean;
    bannedWords: string[];
    perspective: string;
    podcastDraft: string;              // 播客稿正文
    outline: OutlineSection[];
    history: ContentVersion[];
  };

  // ---- P3 分角脚本 ----
  lines: Line[];

  // ---- P4 配音锁轨 ----
  audio: {
    granularity: 'per-speaker' | 'per-line';
    temperature: number;
    pauseMode: 'auto' | 'fixed' | 'punctuation';
    asrModel: string;
    alignMode: 'forced' | 'segment';
    minConfidence: number;
    A_full?: AssetRef;
    B_full?: AssetRef;
    A_track?: AssetRef;
    B_track?: AssetRef;
    mix?: AssetRef;
    alignments: Alignment[];           // 句级时间码
    lowConfidence: string[];           // 低可信句号
  };

  // ---- 时间轨（派生） ----
  timeline: {
    totalDuration: number;             // 秒
    lines: TimelineLine[];             // 带 start/end 的句
  };

  // ---- P5 视觉设定 ----
  visual: {
    sceneImage?: AssetRef;
    masterImage?: AssetRef;
    fusionStrength: number;
    keepStructure: boolean;
    seed: number;
    stations: {                         // 站位框（原图坐标系）
      A: Rect;
      B: Rect;
    };
    masks?: {
      person1?: AssetRef;
      person2?: AssetRef;
      background?: AssetRef;
    };
    history: VisualVersion[];
  };

  // ---- P6 镜头表 ----
  shots: Shot[];
  shotConfig: {
    minShotLength: number;             // 秒
    minSilence: number;                // 秒
    cropScale: number;                 // 1.6
    maxCameraStrength: number;         // 0.25
  };

  // ---- P7 试镜 ----
  rehearsal: {
    range: { start: number; end: number };
    resolution: string;
    steps: number;
    seed: number;
    seedLocked: boolean;
    audioType: 'add' | 'para';
    audioCfg: number;
    motionFrame: number;
    result?: AssetRef;
    checks: [boolean, boolean, boolean, boolean];
  };

  // ---- P8 渲染 ----
  render: {
    segLength: 15 | 20 | 30;
    resolution: '480p' | '720p';
    steps: number;
    audioCfg: number;
    motionFrame: number;
    seed: number;
    overlapFrames: number;
    rife: boolean;
    applyCamera: boolean;
    crossFade: boolean;
    muxAudio: boolean;
    segments: RenderSegment[];
    post: {
      concat: PostStatus;
      camera: PostStatus;
      rife: PostStatus;
      export: PostStatus;
    };
    output?: AssetRef;
  };

  // ---- 日志 ----
  logs: LogEntry[];
}
```

### 12.2 子类型

```typescript
type StepId = 'P1'|'P2'|'P3'|'P4'|'P5'|'P6'|'P7'|'P8';
type CheckpointId = 'CP-1'|'CP-2'|'CP-3'|'CP-4'|'CP-5'|'CP-6';
type Speaker = 'A' | 'B';

interface AssetRef {
  path: string;          // 相对工程目录
  type: 'image' | 'audio' | 'video';
  width?: number;
  height?: number;
  duration?: number;
  size?: number;
  createdAt: string;
}

interface Character {
  id: string;
  name: string;                    // 主持人 / 嘉宾
  locked: boolean;
  appearanceType: 'real' | 'anthropomorphic' | 'cartoon' | 'object';
  image?: AssetRef;                // 形象图
  setSheet?: AssetRef;             // 设定图
  voice: {
    reference?: AssetRef;          // 参考音
    emoVector: number[];           // 8 维
    durationFactor: number;        // 0.5–2.0
    useRandom: boolean;            // 建议 false
    label: string;                 // 音色标签
  };
}

interface Line {
  id: string;                      // L001
  who: Speaker;
  text: string;
  tone: string;                    // 平静/好奇/...
  rate: number;                    // 局部语速倍率
  prePause: number;                // 秒
  postPause: number;               // 秒
  estDuration?: number;            // 预估
  actual?: { start: number; end: number };  // 真实时间码
}

interface TimelineLine extends Line {
  start: number;
  end: number;
  duration: number;
}

interface Alignment {
  lineId: string;
  start: number;
  end: number;
  confidence: number;
}

interface Rect { x: number; y: number; w: number; h: number; }

interface Shot {
  id: string;                      // S01
  start: number;
  end: number;
  type: 'wide' | 'soloA' | 'soloB';
  camera: {
    move: 'static' | 'pushIn' | 'pullOut' | 'panLeft' | 'panRight' | 'pushPan';
    strength: number;              // 0.05–0.25
    easing: 'linear' | 'easeIn' | 'easeOut' | 'easeInOut';
  };
  crop: Rect;                      // 裁切框
}

interface RenderSegment {
  id: string;                      // #01
  start: number;
  end: number;
  frames: number;
  status: 'pending' | 'rendering' | 'done' | 'failed';
  progress: number;
  output?: AssetRef;
  error?: string;
}

interface PostStatus {
  status: 'waiting' | 'running' | 'done' | 'failed';
  progress: number;
  output?: AssetRef;
  error?: string;
}
```

### 12.3 派生规则

| 派生量 | 来源 | 公式 |
|---|---|---|
| `timeline.totalDuration` | 对齐结果 | `max(最后一句.end)` |
| `timeline.lines` | `lines[]` + `alignments[]` | 合并 |
| `render.segments` | `timeline` + `segLength` | 按镜头段边界优先切分 |
| 镜头段候选切点 | `timeline` 的静音间隙 | 见 8.5 |
| `stale` 标记 | 依赖图 | 见 0.3 失效映射表 |

---

## 13. 本地接口契约

> 前端不直接调模型，只与**编排服务**（`http://127.0.0.1:8000`）通信；编排服务再调 ComfyUI（`http://127.0.0.1:8188`）。

### 13.1 服务与健康检查

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/health` | GET | 编排服务健康检查 |
| `/api/system` | GET | 返回 GPU 名、显存、模型就绪状态 |
| `/api/models` | GET | 返回各模型可用性（IndexTTS / 图生图 / InfiniteTalk / RIFE） |

### 13.2 工程与素材

| 接口 | 方法 | 入参 | 返回 |
|---|---|---|---|
| `/api/projects` | GET | — | 工程列表 |
| `/api/projects` | POST | `{name, characterAId, targetDuration}` | 新工程 |
| `/api/projects/{id}` | GET | — | 完整 `ProjectState` |
| `/api/projects/{id}` | PUT | 部分状态 | 保存后的状态 |
| `/api/projects/{id}/assets` | POST | multipart 文件 | `AssetRef` |
| `/api/projects/{id}/assets/{path}` | GET | — | 文件流 |

### 13.3 各步骤接口

| 步骤 | 接口 | 方法 | 入参 | 返回 |
|---|---|---|---|---|
| P2 | `/api/projects/{id}/content/generate` | POST | `{inputType, rawInput, targetDuration, style, ...}` | SSE 流式文本 |
| P3 | `/api/projects/{id}/script/split` | POST | `{podcastDraft, style}` | `Line[]` |
| P4 | `/api/projects/{id}/audio/synthesize` | POST | `{granularity, temperature, ...}` | `{jobId}` |
| P4 | `/api/projects/{id}/audio/align` | POST | `{asrModel, alignMode, minConfidence}` | `{jobId}` |
| P4 | `/api/projects/{id}/audio/line/{lineId}/resynth` | POST | — | `{jobId}` |
| P1 | `/api/projects/{id}/character/{role}/image` | POST | 生成参数 | `{jobId}` |
| P1 | `/api/projects/{id}/character/{role}/voice/preview` | POST | `{text}` | `{jobId}` |
| P5 | `/api/projects/{id}/visual/scene` | POST | 生成参数 | `{jobId}` |
| P5 | `/api/projects/{id}/visual/master` | POST | `{prompt, fusionStrength, seed, ...}` | `{jobId}` |
| P5 | `/api/projects/{id}/visual/mask` | POST | `{stations}` | `{masks}` |
| P6 | `/api/projects/{id}/shots/auto` | POST | `{config}` | `Shot[]` |
| P6 | `/api/projects/{id}/shots/filters` | GET | — | ffmpeg 滤镜链 |
| P7 | `/api/projects/{id}/rehearsal` | POST | 试镜参数 | `{jobId}` |
| P8 | `/api/projects/{id}/render/queue` | POST | 渲染参数 | `{jobId, segments}` |
| P8 | `/api/projects/{id}/render/segment/{segId}/retry` | POST | — | `{jobId}` |
| P8 | `/api/projects/{id}/render/post` | POST | 后处理开关 | `{jobId}` |
| P9 | `/api/projects/{id}/auto/run` | POST | `{mode, checkpoints}` | `{jobId}` |
| 通用 | `/api/jobs/{jobId}` | GET | — | 任务状态 |
| 通用 | `/api/jobs/{jobId}/cancel` | POST | — | — |
| 通用 | `/api/ws` | WS | — | 实时进度推送 |

### 13.4 WebSocket 消息格式

```typescript
// 服务端 → 客户端
type ServerMessage =
  | { type: 'job.progress'; jobId: string; step: string; progress: number; message: string }
  | { type: 'job.done'; jobId: string; result: unknown }
  | { type: 'job.failed'; jobId: string; error: string }
  | { type: 'render.segment'; jobId: string; segId: string; status: string; progress: number }
  | { type: 'checkpoint.reached'; checkpoint: CheckpointId; payload: unknown }
  | { type: 'system.vram'; used: number; total: number };

// 客户端 → 服务端
type ClientMessage =
  | { type: 'subscribe'; jobId: string }
  | { type: 'checkpoint.respond'; checkpoint: CheckpointId; action: 'pass' | 'reject'; note?: string }
  | { type: 'job.cancel'; jobId: string };
```

---

## 14. 状态机与全局交互规则

### 14.1 步骤完成判定

| 步骤 | 完成判定 | 未完成时的表现 |
|---|---|---|
| P1 角色库 | A、B 各有形象图 + 参考音 | 禁用 P4、P5 的生成按钮 |
| P2 内容输入 | 播客稿正文非空 | P3 显示「请先生成播客稿」 |
| P3 分角脚本 | ≥1 句且无空句 | P4 显示「请先完成分角」 |
| P4 配音锁轨 | A、B 音频已合成 + 时间码已回填 | P6 显示「请先完成配音锁轨」；时间轨不展开 |
| P5 视觉设定 | 底图已生成 + 站位已确认 | P6、P7、P8 相关按钮禁用 |
| P6 镜头表 | 所有段已配置 | P7、P8 显示「请先完成镜头表」 |
| P7 试镜 | 四项验收全勾 | P8 显示橙色提示（不硬锁） |
| P8 渲染 | 全部段完成 + 后处理完成 | 导出按钮禁用 |

### 14.2 全局交互约定

| 约定 | 说明 |
|---|---|
| **导航** | 步骤可自由前后跳转，允许前跳；点「下一步」才写入已完成；侧栏用编号/绿勾/橙点区分状态 |
| **可调优先** | 任何校验只提示不硬拦（除空数据外），保证「打磨」不被流程打断 |
| **文件读取** | 上传文件先复制进工程目录，用相对路径引用；不把二进制塞进 state |
| **自动保存** | 改动后 2s 防抖写入 `project.json` |
| **过期标记** | 上游改动后，下游步骤侧栏圆点变橙 + 产物卡片打「已过期」角标 + 提供「重算下游」 |
| **版本管理** | 每一步的关键产物保留最近 10 个版本，可对比与回退 |
| **服务断开** | 所有推理按钮禁用，显示「请先启动本地引擎」+ 具体启动命令 |
| **错误处理** | 统一 Toast；失败任务保留上下文，可原位重试；错误详情写入日志 |
| **键盘快捷键** | `Space` 播放/暂停、`←/→` 前后句、`Ctrl+S` 保存、`Ctrl+Z` 撤销、`1–8` 切步骤 |
| **无障碍** | 颜色 + 文字双通道；键盘可达；焦点可见；正文对比度 ≥4.5:1 |

### 14.3 撤销与历史

- 所有会改变产物的操作进入操作栈，支持 `Ctrl+Z` / `Ctrl+Shift+Z`
- 产物级操作（生成、重跑）不进撤销栈，改为「版本回退」
- 撤销栈上限 50 步，跨步骤保留

---

## 附录 A：页面清单速查

| 页面 | 视图 ID | 核心产出 | 检查点 | 主要参数数 |
|---|---|---|---|---|
| 工程管理 | P0 | 工程目录 | — | 3 |
| 角色库 | P1 | 形象图 + 参考音 + 设定图 | CP-1 | 16 |
| 内容输入 | P2 | 播客稿 | — | 10 |
| 分角脚本 | P3 | `lines[]` | CP-2 | 7/句 |
| 配音锁轨 | P4 | 时间轨 + 音轨 | CP-4 | 6 |
| 视觉设定 | P5 | 同框底图 + mask | CP-3 | 11 |
| 镜头表 | P6 | `shots[]` + 滤镜链 | CP-5 | 4 全局 + 6/段 |
| 试镜 | P7 | 样片 + 验收 | CP-6 | 8 |
| 渲染与导出 | P8 | 成片 | — | 12 |
| 一键生成 | P9 | 全流程 | 6 关卡 | 模式配置 |

## 附录 B：术语表

| 术语 | 含义 |
|---|---|
| **时间轨** | 由 TTS 实际音频 + ASR 回填构成的句级时间坐标系，是全片唯一基准 |
| **同框底图（master）** | 唯一首帧，两人已在场景中，后续所有渲染都基于它 |
| **图生图融合** | 用角色设定图 + 场景图作为参考，由模型重新生成一张「两人已在场景中」的图，而非贴图 |
| **数字运镜** | 在已渲染视频上做裁切/缩放/平移，零漂移、零额外算力 |
| **`add` 模式** | 多音频串行模式：不说话的说话人收到全零音频特征，自然保持安静 |
| **`para` 模式** | 多音频并行模式：两条等长音轨相加，用于两人同时说话 |
| **mask 跨注意力分配** | mask 决定「哪条音轨驱动哪块像素」，不决定「谁动谁不动」 |
| **确认关卡** | 需要人眼判断的暂停点，可配置为必须确认/自动通过/跳过 |
| **过期标记** | 上游改动导致下游产物失效的标记 |
