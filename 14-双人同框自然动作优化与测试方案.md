# 双人同框自然动作优化与测试方案

## 1. 结论

主路线使用现有 InfiniteTalk / MultiTalk 多人权重，将一张双人同框图、两条等长互补音轨和两个人物区域遮罩一次性输入模型。遮罩用于绑定人物与音轨，不用于冻结静音人物。这条路线已在 RTX 5080 16GB 上生成 5 秒有效样片。

现有的“单人裁窗生成后贴回静态底图”保留为降级路线，用于多人生成失败或需要强口型稳定的镜头。

## 2. 开源方案选型

| 项目 | 多人分轨 | 倾听动作 | 16GB 本机可行性 | 结论 |
|---|---:|---:|---:|---|
| InfiniteTalk / MultiTalk | 是 | 可由整帧生成 | 已实测跑通 | 主方案 |
| LongCat-Video-Avatar 1.5 | 是 | 官方展示轮流互动 | 官方 13.6B，需 INT8/社区 GGUF 另行验证 | 第二阶段对照 |
| HunyuanVideo-Avatar | 论文支持 | 支持动态多人 | 官方最低 24GB，10GB 需 Wan2GP 社区路径 | 不作首轮 |
| INFP | 面向说话/倾听状态过渡 | 强 | 偏互动头部研究，不直接替代当前全景生产链 | 可借鉴状态设计 |

## 3. 输入规范

1. 双人底图采用闭嘴、双手放稳、表情中性的姿态。两人脸部不重叠，人物之间留出空间。
2. A/B 音轨必须等采样率、等长、与母轨共用同一时间零点。A 发言时 B 轨填数字静音，反之亦然。输入类型使用 `para`，不使用会串行拼接的 `add`。
3. 每个遮罩覆盖对应人物的头部和上身，两个遮罩不重叠；边缘羽化 8–16 像素。遮罩按 A、B 顺序与 `audio_1`、`audio_2` 一一对应。
4. 提示词明确规定：发言者准确口型和克制头动；静音者闭嘴、呼吸、眨眼、看向说话者，偶尔轻微点头；固定机位和背景。

## 4. 基线参数

- 输出：864×480，25 fps。
- 模型：`Wan2_1-InfiniteTalk-Multi_fp8_e4m3fn_scaled_KJ.safetensors`。
- 模式：`multitalk`；双轨 `para`；`audio_scale=1`；`audio_cfg_scale=1`。
- 采样：4 steps，`flowmatch_distill`，shift 11，seed 1。
- 显存：`blocks_to_swap=38`，TeaCache 先关闭，排除多人路径的缓存干扰。
- 色彩匹配：`disabled`。Reinhard 在本次输出上产生 NaN 并导致全黑视频。
- 启动环境：Windows 必须设置 `PYTHONIOENCODING=utf-8` 和 `PYTHONUTF8=1`，避免节点 emoji 日志触发 GBK 编码错误。

## 5. 分阶段测试

### T1：单说话者，5 秒

A 轨为真实语音，B 轨为等长静音。验证 A 口型、B 闭嘴动作、背景稳定和身份一致。本次 `duo_listener_v2.mp4` 已通过技术可行性验证。

### T2：轮流接话，10 秒

0–4.5 秒 A 说，4.5–5.0 秒双方静音，5.0–9.5 秒 B 说。验证角色交换时嘴部不串轨，转换期没有跳帧或人脸漂移。

### T3：参数 A/B

固定素材和 seed，只改一项：

- `audio_cfg_scale`: 1.0 / 2.0 / 3.0，找口型准确与静音者闭嘴的平衡。
- 动作提示：“blink and breathe” / 再加 “subtle nod”，避免频繁机械点头。
- 遮罩：脸部 / 头肩 / 上身，选择串轨最少、动作最自然的范围。

### T4：20–30 秒分段连续性

每段保留上一段末尾的 motion frames，检查色偏、人脸累积漂移、背景变形和段边界跳变。通过后再扩展到完整播客。

## 6. 验收门槛

- 发言者嘴部持续跟随语音，无连续 0.5 秒以上明显脱节。
- 静音者的嘴在 95% 以上时间保持闭合，不出现连续两帧以上的误开口。
- 静音者在每 3–8 秒内至少有一次自然眨眼或微小姿态变化，但不得持续点头。
- 人脸、服装和背景无明显闪烁；手部不突然增减；麦克风不变形。
- 段边界不出现人物位置跳变或整体色调突变。

## 7. 降级策略

若多人整帧在长镜头中不稳定，先切为单人近景；同框必须保留时，使用“发言者动画窗 + 倾听者循环待机素材”的双动态合成，倾听素材必须是同一底图和同一构图生成，并在切近景时替换循环点。

## 8. 本轮实测记录

- 硬件：RTX 5080 16GB。
- 第一轮：运行时未设 UTF-8，图像节点 emoji 日志触发 GBK `UnicodeEncodeError`。
- 第二轮：已生成 5 秒，但 Reinhard 色彩匹配产生 NaN，输出全黑。
- 第三轮：关闭色彩匹配，470 秒生成 4.92 秒视频，文件约 1.08MB。A 发言，B 闭嘴且有微动作，背景与身份稳定。
- 逐帧差分：A 区域平均 1.593，B 区域 0.994，B 脸部 1.515。相比旧回贴方案的近静止编码残差，倾听者已有真实生成动作。

### 倾听语义增强测试

- 只加“看向说话者、轻微偏头、点头”提示词的视频测试不足：右侧人物仍然大致正对镜头。结论：不应将倾听资讯交给提示词单独解决。
- 已生成专用底图 `duo_stage_listener_b_v1.png`：B 闭嘴、目光和头部转向 A，双手维持在桌面。同时重算对应的独立 A/B 遮罩，避免图像尺寸变化时绑定偏移。
- 产出 `duo_listener_pose_v1.mp4`：4.92 秒、864×480、470 秒渲染。抽帧验证：A 发言；B 全程闭嘴、朝向 A、微小偏头与眸眼。这是首个可用的“发言—倾听”同框镜头模板。

## 9. 生产中的镜头资产组合

为每个双人机位维护至少三张同构图底图：A 说 / B 听、B 说 / A 听、双方中性。轮到谁发言，就以对应的“一说一听”底图启动整帧多人生成；停顿时使用中性底图或切近景。每张底图的遮罩单独保存并与图像同名关联。

## 10. 首帧与色彩稳定处理

多人模型的 start_image 是条件参考，不是逐像素复制到输出首帧。4 步蒸馏采样的前约 0.5 秒可能包含去噪残留，且模型会将底图重新渲染为自己的色彩分布。

生产处理固定为：

1. 在每段音频前加 0.9 秒数字静音；画面同期展示对应的干净底图。
2. 生成视频的 0.00–0.55 秒完全用底图覆盖；0.55–0.90 秒从底图平滑淡出到生成画面。语音从淡出结束时才进入，避免嘴形与底图不一致。
3. 4.8–5.0 秒以内作为本机多人模型的优先分段长度。8 秒实测在第三个生成窗口后出现人物重绘和色彩退化，须在该边界前切镜头或使用下一段重新锚定。
4. 当前 flowmatch_distill 调度器只允许 4 步，不能直接提高至 6 步；若要做高步数质量对照，必须更换为非蒸馏调度器和匹配的 LoRA/参数组合。

本轮女声样片使用用户提供的 29.47 秒 MP3 作为 IndexTTS 2.5 的参考音频，合成“认真倾听，本身就是一种回应。”；右侧女主播发言、左侧主播倾听。female_voice_listener_short_clean_v1_fixed.mp4 为首帧修复后的可播放成片，4.84 秒、864×480。

### 窗口色闪修复验证

- 调查发现：默认 81 帧窗口在 25fps 下约等于 3.24 秒。短片的第二窗口开始时，模型会重新解码并重估色彩，素材的 RGB 均值从约 [143,95,56] 跳为 [139,100,67]。这是全屏色调闪变的直接原因，与分辨率无关。
- 已实测单窗口方案：将此 4.8 秒镜头设为 121 帧一次生成（峰值显存约 15.3GB）。首帧修复后的 `female_voice_listener_singlewindow_v1_fixed.mp4` 在 0.9–4.4 秒的 RGB 均值稳定在约 [138,89,52]，消除了 3.24 秒的色闪。
- 生产规则：若镜头时长≤4.8 秒，优先单窗口生成；若更长，在 4.8 秒前切换镜头或重新以底图锚定下一段，不直接依赖跨窗口延续。
