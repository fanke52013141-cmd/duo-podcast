# DuoCast 双声播客工坊 · 代码 / UI / 交互审查报告

| 项 | 值 |
|---|---|
| 审查对象 | `apps/server`（Python 22 文件 / 约 1.5k 行）+ `apps/web/src`（React 17 文件 / 约 4.5k 行） |
| 审查时间 | 2026-09-10 |
| 审查基线 | 仓库当前工作区（HEAD 未提交改动）；权威需求：[04-详细优化方案.md](04-详细优化方案.md)、[01-页面详细设计文档.md](01-页面详细设计文档.md)、[05-UI设计系统与页面规划.html](05-UI设计系统与页面规划.html) |
| 审查方式 | 通读全部源码 + **实际启动服务复现**（临时存储 `8123` 端口）+ 静态检查（ruff）+ 对比度计算 + 构建产物类名覆盖核对 |
| 结论 | **骨架设计良好、分层清晰，但存在 4 个会丢数据或让结果不可信的缺陷（P0），以及 11 个与设计文档明显不符的实现缺口（P1）**。当前"流程真、内容假"的定位成立，但"流程真"这一半也有若干处并未真正做到 |

---

## 0. 结论速览

### 0.1 严重度分布

| 级别 | 数量 | 含义 |
|---|---|---|
| **P0** | 4 | 会导致编辑内容被覆盖/丢失、任务状态永久错误、中断任务凭空消失 —— 必须在对外演示前修掉 |
| **P1** | 11 | 功能与文档承诺不符、并发/时长等核心机制失真、可访问性硬伤 |
| **P2** | 19 | 一致性、可维护性、空实现控件、误导性文案 |

### 0.2 必须先修的 4 件事（按修复顺序）

| # | 缺陷 | 位置 | 后果 |
|---|---|---|---|
| 1 | 选中历史版本编辑，内容写进**当前草稿** | `EditScriptPage.tsx:134-142` + `lib/api.ts:153-179` | 当前草稿被历史版本内容覆盖，用户以为在改历史版本 |
| 2 | 自动保存 409 冲突**静默丢弃** | `lib/api.ts:181-188`（无 `onError`） | 编辑丢失且界面无任何提示 |
| 3 | pause / cancel 后任务**永久卡在非终态**，并发出与状态矛盾的 `job.failed` | `jobs/manager.py:87-117, 120-147` | 任务中心无法结束该任务；成功任务被报"失败" |
| 4 | 重启后已落盘任务**不再被加载** | `jobs/recovery.py` 结果未写回 `JobManager` | 任务中心空白；中断任务在 UI 中消失 |

---

## 1. 审查范围与方法

### 1.1 覆盖范围

| 层 | 覆盖内容 |
|---|---|
| 后端 | `core`（config/eventbus/logging）、`domain`（5 个域模型）、`storage`（project_store/artifacts/machine）、`jobs`（manager/recovery）、`adapters`（base/mock）、`services`（5 个编排）、`api`（10 个路由）、`main.py` 装配 |
| 前端 | `App`/`AppShell`/`wu` 组件库、`lib`（api/sse/types）、`stores`、9 个页面、3 份 CSS、`vite.config`/`tsconfig`/`package.json` |
| 交叉核对 | 与 01 / 04 / 05 / README / HANDOFF 中的明确承诺逐条比对 |

### 1.2 复现手段（脚本保留在 `.review/`，可直接作为回归用例）

| 脚本 | 作用 |
|---|---|
| `.review/verify_backend.py` | 直连模块复现：别名覆盖、修订号重号、暂停/取消状态机、队列串行、启动恢复、时长权威、事件总线 |
| `.review/verify_http.py` | HTTP 层复现：全链路冒烟、悬空资产引用、409 冲突、SSE 帧格式 |
| `.review/verify_http2.py` | 暂停真实 RUNNING 任务、SSE 原始帧与 `Last-Event-ID` 污染 |
| `.review/fe-check.ts` | 直接 import 前端真实模块，验证草稿 upsert 的版本语义 |

复现统一使用临时存储（`DUOCAST_STORAGE=.review/rt`、端口 `8123`），**未触碰 `apps/storage` 下的真实数据**。

### 1.3 基线验证（先说好消息）

- `pnpm build`（`tsc --noEmit && vite build`）**零错误通过**，产物哈希与已提交的 `dist/` 完全一致 —— 前端类型与构建是干净且同步的。
- 全链路冒烟真实通过：创建 → 脚本生成 → 配音合成 → 画面生成 → 渲染 → 资产登记 → 机器设置 → 提供方测试。
- 后端分层纪律执行得不错：`api` 不碰文件、`services` 通过 `store.apply()` 单点写入、`adapters` 不 import 业务概念，与 06 §2 的依赖规则一致。

---

## 2. P0 级缺陷

### P0-1 选中历史版本编辑，内容会覆盖当前草稿

**位置**：`apps/web/src/pages/EditScriptPage.tsx:134-142`、`apps/web/src/lib/api.ts:153-179`

`upsertDraftTurns(proj, turns)` 内部**只认 `proj.currentDraftRevision`**，完全不接收"当前查看的是哪个版本"：

```ts
// api.ts:154-155
const cur = proj.scriptRevisions.find((r) => r.id === proj.currentDraftRevision)
  ?? proj.scriptRevisions[proj.scriptRevisions.length - 1];
```

而页面的自动保存把**查看中的版本内容**直接喂进去：

```ts
// EditScriptPage.tsx:138
const { patch, revId } = upsertDraftTurns(proj, nextTurns);   // nextTurns 来自 viewRevId 版本
```

**实测（`.review/fe-check.ts`）**：

```
界面当前查看版本（viewRevId） = R1；工程当前草稿 = R2
upsertDraftTurns 返回 revId  = R2 （写进了当前草稿，而不是基于 R1 新建）
R2 的台词是否被 R1 视图的内容覆盖 = 在 R1 视图上改出来的新内容
```

**更糟的是界面文案在明确承诺相反的行为**（`EditScriptPage.tsx:291-293`）：

> "正在查看历史版本 {revision.id}（非当前草稿）；编辑将基于该版本创建新草稿。"

**修复方向**：把"查看版本"作为显式参数传入（`upsertDraftTurns(proj, turns, baseRevId)`），当 `baseRevId !== currentDraftRevision` 时强制走"新建草稿版本"分支；或暂时在历史版本视图下禁用编辑并给出说明。

---

### P0-2 自动保存的 409 冲突被静默丢弃

**位置**：`apps/web/src/lib/api.ts:181-188`（`useSaveDraft` 无 `onError`）、`EditScriptPage.tsx:88, 139`（`saveDraft` 的状态从未被读取）

后端冲突保护本身是对的，实测有效：

```
PATCH#1 → 200
PATCH#2（同 revision 重放）→ 409 {"detail":"expectedRevision=3 != current=4"}
```

但前端拿到 409 之后**什么都不做**：`saveDraft.mutate(...)` 没有 `onError`，页面也没有渲染 `saveDraft.isError`。触发路径非常日常：

1. 用户在阶段②改一句台词 → 2 秒防抖计时器启动；
2. 计时器触发前，用户点了「确认脚本」（或后台 `script.generate` 完成，两者都会 bump `revision`）；
3. 自动保存用旧的 `proj.revision` 提交 → 409 → **这一句编辑凭空消失，且没有任何提示**。

同一个问题也存在于 `confirm.mutate`（`EditScriptPage.tsx:203`、`VoicePage.tsx:113`、`VisualPage.tsx:63`）和 `patchProject.mutate`（`CreateEpisodePage.tsx:82, 89`）—— 全部没有错误处理，409 时按钮点下去"像没反应"。

**修复方向**：给所有 `mutate` 加 `onError`；对 409 做专门处理（自动重取最新 `revision` 后重放 patch，或弹出"内容已在别处更新，请确认"对话框并保留本地草稿）。

---

### P0-3 pause / cancel 后任务永久卡在非终态，并发出矛盾的失败事件

**位置**：`apps/server/duocast/jobs/manager.py:107-117`（pause/cancel）、`:87-100`（transition）、`:120-147`（调度循环）

三个问题叠在一起：

**① 状态机没有"暂停执行"的实现。** `pause()` 只把状态改成 `PAUSE_REQUESTED`，此后**没有任何代码路径**把它推进到 `PAUSED`；正在运行的 runner 也收不到任何取消信号。`cancel()` 同理。

**② runner 完成后回写终态必然失败。** `TRANSITIONS[PAUSE_REQUESTED] = {PAUSED, FAILED, CANCEL_REQUESTED}`，不含 `SUCCEEDED`；`TRANSITIONS[CANCEL_REQUESTED]` 同样不含。于是：

```
[pause] 派发前状态 = running
[pause] 调用 pause() 后 = pause_requested
[pause] 任务体早已返回、2 秒后状态 = pause_requested   ← 期望 paused/succeeded
[pause] job.result 是否已写入 = True（结果被丢弃，状态卡死）
[cancel] cancel() 后 = cancel_requested；2 秒后 = cancel_requested ← 期望 cancelled
[cancel] 真实计算是否被中断 = 否（runner 未收到取消信号）
```

HTTP 层同样复现（`.review/verify_http2.py`）：对真正 `running` 的 GPU 合成任务调 `POST /pause`，5 秒后仍是 `pause_requested`，而**工程的 `audioTimeline` 已被写入**（暂停没有阻止落盘）。

**③ `transition()` 在非法转换时发布 `job.failed`。** 这是设计缺陷 —— 状态转换失败 ≠ 任务失败：

```python
# manager.py:88-91
if not job.can_transition(target):
    self.bus.publish("job.failed", jobId=job.id, error=f"illegal transition {job.status}->{target}", ...)
    return False
```

实测产生的两条噪音事件：

```
{'jobId': 'JFE3BA58E08', 'error': 'illegal transition PAUSE_REQUESTED->SUCCEEDED', 'retryable': False}
{'jobId': 'J2400D9BDC1', 'error': 'illegal transition CANCEL_REQUESTED->SUCCEEDED', 'retryable': False}
```

对**已经成功**的任务调用 pause（接口层没有终态守卫，UI 只是把按钮禁用了）同样会发出这条事件 —— 前端 `App.tsx:76-81` 收到 `job.*` 就 invalidate 并提示，用户看到"失败"，而任务状态其实是 `succeeded`。

**连带后果**：前端 `stores/index.ts:45-52` 的 `touch()` 只在 `succeeded/failed/cancelled` 时把任务移出 `activeJobIds`，`pause_requested` / `cancel_requested` 会**永久留在"活动任务"集合里**；`TasksPage.tsx:12-14` 把它们归入"运行中"页签，用户永远看到一个卡住的任务。

**修复方向**：① `pause()` 只对 `RUNNING` 生效，并真正向 runner 传递取消信号（`asyncio.Task.cancel()` + 适配器 `cancel` 能力）；② 在 runner 的 `finally` 中根据实际结果选择合法终态，或为 `PAUSE_REQUESTED/CANCEL_REQUESTED` 补 `SUCCEEDED` 分支（"已完成，取消来不及"是合法结局）；③ 非法转换只记日志/发独立的 `job.invalid_transition` 事件，不要复用 `job.failed`。

---

### P0-4 重启后已落盘的任务不再被加载，中断任务在 UI 中消失

**位置**：`apps/server/duocast/jobs/recovery.py:16-44`、`main.py:156-159`

`recover_on_startup()` 逻辑本身写得挺克制（无查询能力时标 `unknown` 而不是盲目重跑，符合 02 §7.2 纪律），但它**只返回一个列表，`main.py` 只用了它的长度打日志**：

```python
# main.py:156-159
recovered = recover_on_startup(settings.storage_root / "jobs")
log("startup", ..., recovered_jobs=len(recovered))
job_manager.start()
```

而 `JobManager.__init__` 只把 `self._jobs` 初始化成空字典，**从不扫描磁盘**。实测：

```
磁盘上落盘的任务目录 = ['JF4FE308B5B']
recover_on_startup 扫描到 = 1 个中断任务（状态写为 unknown）
但新 JobManager 是否加载它们 = 0 个 ← 期望 ≥1
```

`Job.list()` / `get()` 全部只反映本进程内存。后果：

- 重启后**任务中心一片空白**，用户看不到任何历史任务（而 README 把"启动恢复扫描"列为已完成项）；
- 处于 `QUEUED` 状态的任务永远不会被重新入队（`recovery.py:41-42` 保留了状态，但没人消费）；
- `RECOVERING → UNKNOWN` 的改写结果无人读取，等于白做。

**修复方向**：`JobManager` 增加 `load_from_disk()`（或让 `recover_on_startup` 接收 manager 实例），把非终态任务注册进 `_jobs`，并把 `QUEUED` 任务重新 `put_nowait` 入队。

---

## 3. P1 级缺陷

### P1-1 时间权威不含转场间隔与首尾停顿 —— 与 README「三条技术事实」直接矛盾

**位置**：`apps/server/duocast/services/voice_svc.py:59-92`

README 第 121 行明确写：**`T = 首尾样本 + 全部单元样本 + 全部显式间隔样本`**，而实现里：

```python
# voice_svc.py:75-84
for unit in units:
    res = await tts.synthesize(...)
    timeline.unit_offsets[unit.id] = offset
    offset += sample_count          # ← 只累加单元长度，没有加间隔
timeline.sample_count = offset      # ← 没有加 leadIn / tailOut
```

实测（4 个单元、3 段 320ms 间隔）：

```
单元样本合计      = 506880
显式间隔样本合计  = 46080（3 × 320ms @48kHz）
规范要求的总长    = 552960
实际 sampleCount  = 506880  ← 期望 552960
差额 = 46080 样本 (0.96 秒)
unit_offsets 是否计入间隔： [0, 126720, 253440, 380160] （等差 = 仅累加单元长度）
leadInMs/tailOutMs 是否参与总长： 0 0 → 未参与
```

HTTP 层复现一致（2 段间隔 → 少 30720 样本 = 0.64 秒）。

影响链：`sample_count` 是渲染分段计划（`RenderPage.tsx:27-49` 的 `planSegments`）、字幕对齐、总时长校验（"真实总时长 ≤ 300 秒"）的输入，也是 README 强调的"唯一时间权威"。**间隔越长、单元越多，误差越大** —— 一个 20 单元、320ms 间隔的节目会少算约 6 秒。`unitOffsets` 同样失真，意味着按偏移定位句子的功能从根上就偏了。

**修复方向**：在累加循环中显式加间隔样本（`offset += gapSamples`），并把 `leadInMs` / `tailOutMs` 折算后计入 `sample_count`；补一条断言测试（`sum(units) + sum(gaps) + leadIn + tailOut == sample_count`）。

---

### P1-2 三队列并发模型名存实亡

**位置**：`apps/server/duocast/jobs/manager.py:120-159`

设计（02 §7、`core/config.py:21-23`）是 GPU=1 / API=16 / CPU=4 三队列**并行**调度。但派发循环是"取出一个任务 → 内联 `await` 执行完 → 再取下一个"：

```python
# manager.py:133-141
self.transition(job, JobStatus.RUNNING)
try:
    async with self._gpu_lock if queue_class == "gpu" else _null_ctx():
        result = await self._runner(job)     # ← 整个循环在这里阻塞
```

实测：

```
完成顺序（队列, 相对秒）： [('gpu', 0.82), ('api', 0.88), ('api', 0.94), ('api', 1.01)]
API 任务最早完成时间 = 0.88 → GPU 任务占满 0.9s，API 队列被串行阻塞
_gpu_lock 是否存在实际作用 = 否（派发循环内联 await，天然串行）
```

即：**一个 GPU 渲染任务运行期间，所有 API 任务（脚本生成、图片生成）全部排在后面等**。mock 阶段延迟是毫秒级所以看不出来，一旦接上真实提供方（渲染 5 分钟 / 图片几十秒），"三队列"退化成单队列，`queue_cap_api=16` 毫无意义。

附带的次生问题：`submit()` 用 `put_nowait` 入队（`manager.py:69`），队列满时抛 `asyncio.QueueFull` 且无捕获 → 接口直接 500。

还有一处小浪费：`_dispatch_loop` 里 `await self._wait_any()` 本身已经 `await asyncio.sleep(0.05)`（`manager.py:158`），返回 `None` 后循环又 sleep 0.05 —— 空转时是 10Hz 轮询 + 双倍延迟。

**修复方向**：改成"每个队列一个消费协程 + `asyncio.Semaphore` 限流"的标准模型，或把 `await self._runner(job)` 包成 `asyncio.create_task` 并登记到活动任务表。

---

### P1-3 修订 ID 重启后重号，且没有去重校验

**位置**：`apps/server/duocast/adapters/mock.py:86-99`、`services/script_svc.py:36-44`

```python
_counter = [0]
def mock_script_counter() -> int:
    _counter[0] += 1
    return _counter[0]
```

修订号来自**进程内自增计数器**，重启后归零：

```
同一进程连续两次生成 → revisionId = R1 / R2（全局自增，与工程无关）
进程重启后再次生成 → revisionId = R1  ← 与历史版本重复
```

而 `apply_script_revision` 是**无条件追加**：

```python
# script_svc.py:41-44
"script_revisions": [...现有...] + [revision.model_dump(by_alias=True)],
"current_draft_revision": revision.id,
```

于是 `script_revisions` 里会出现两条 `R1`。后果是双重的：`voice_svc.find_revision()`（`voice_svc.py:18-23`）返回**第一条匹配**，配音会合成到旧版本内容上；前端 `nextRevisionId()`（`api.ts:139-146`）算 `R{max+1}` 又和后端计数器是两套编号来源。

**修复方向**：修订号由工程内在场版本推导（`R{max+1}`，与前端统一），或在 `apply_script_revision` 里检测冲突并换号；mock 的计数器只应作为兜底。

---

### P1-4 阶段间失效传播（stale）完全没有实现

**位置**：`apps/server/duocast/services/stage_svc.py:20-49`

`StageState` 定义了 7 个状态，README 与 01 §13.1 把它当作核心卖点（"产物版本 + 任务状态七态"、"改脚本不必重跑全部"）。但实现只产出 4 个：

```python
def _stage(has_product, confirmed, running):
    if running:              return StageState.RUNNING
    if has_product and confirmed:     return StageState.DONE
    if has_product and not confirmed: return StageState.PENDING_CONFIRM
    if has_product:          return StageState.READY     # ← 不可达
    return StageState.NOT_READY
```

- **`STALE` 从未被返回**，`FAILED` 也从未被返回 → 前端 `STAGE_META` 里的"已过期"是死状态；
- 没有任何服务检查"时间轨的 `revisionId` 是否还等于 `currentDraftRevision`"。实测路径：确认配音（`approvals` 绑 `R1`）→ 回阶段②重新生成脚本得到 `R2` → 阶段③仍然显示"已确认"，`voiceApproved` 依旧为真，"下一步"按钮照常可点。

同类问题：**重新合成后"回到待确认"没有实现**。`VoicePage.tsx:169-171` 的文案承诺"重新合将覆盖当前时间轨并回到待确认"，但确认记录是按 `inputRevisionId`（= revision id）匹配的，revision id 不变 → 旧的 `voiceApproved` 永远成立。样片确认（`VisualPage.tsx:44-46`）同理。

**修复方向**：确认记录应绑定"输入版本 + 规格指纹"（04 文档里本来就写了"确认记录绑定的输入版本与规格，不只是全局布尔值"），例如给 `AudioTimeline` 加 `timelineVersion` 并纳入 approval 的 `spec`；`stage_svc` 增加 stale 推导规则表。

---

### P1-5 资产的 `artifactIds` 系统性悬空

**位置**：`main.py:41-52`（脚本）、`:85-96`（画面）、`:97-113`（渲染）、`:67-84`（配音）

`job.result.artifactIds` 与 `ArtifactStore` 登记的实际 ID 对不上，实测四类任务全中：

```
visual.generate  J4F8B602233  声明=['V01']                        未登记=['V01']
script.generate  J795AC77774  声明=['R1']                         未登记=['R1']
tts.synthesize   JE53C1E899F  声明=['audio-JE5...','audio-b22d1f75','audio-4c33f21c','audio-0ba6b768']  未登记=3 个
manifest 实际登记 = ['audio-J96D3DF2CCC', 'audio-JE53C1E899F', 'image-a1ae1722', 'video-J4821836A1A']
```

具体成因：

- `script.generate` 分支**根本没有调用 `artifacts.register`**，却把 `revision.id`（形如 `R1`）当 artifactId 返回；
- `visual.generate` 返回变体 ID（`V01`）而非 `master_image.artifact_id`（实际登记的是 `image-a1ae1722`）；
- `tts.synthesize` 给每个单元伪造了 `candidate_audio_asset_ids`（`voice_svc.py:80`），这些 ID 从未登记。

后果：`job.done` 事件（`manager.py:97`）向 SSE 宣告一批不存在的产物，前端若据此跳转/展示会踩空；资产库与任务结果无法互证。

**修复方向**：统一"返回的就是已登记 ID"，并加一条断言（`set(result.artifactIds) <= set(manifest.ids)`）。

---

### P1-6 SSE 的 `Last-Event-ID` 会被服务端自己污染

**位置**：`apps/server/duocast/api/events.py:26-33`、`core/eventbus.py:19, 66`

两条路径写入了非法的 `id`：

```python
# events.py:31-33  无补发时
yield {"id": "0", "data": '{"type":"system.resync"}'}
# eventbus.py:19  慢消费者时（RESYNC_EVENT 的 seq = -1），经 events.py:42 发出 id: -1
```

浏览器（原生 `EventSource`）会把 `id:` 记成 `lastEventId` 并在重连时回传。实测：

```
Last-Event-ID:  -1 → 服务端立即补发的帧数 = 1，前 3 个 id = ['id: 1\r']
Last-Event-ID:   0 → 服务端立即补发的帧数 = 1，前 3 个 id = ['id: 1\r']
```

`replay(-1)` / `replay(0)` 会从头补发**整个内存缓冲（上限 2 万条）**。也就是说：一旦发生过一次慢消费者或一次"无事可补"的重连，之后每次断线重连都会全量重放事件日志。此外 `int(last_seq)` 对非数字头会抛 `ValueError`（`events.py:27`），直接 500。

顺带一个正确性小问题：`subscribe()` 在 `replay()` 之前执行，两段之间没有队列排空，重连瞬间可能重复投递同一批事件（前端靠 `seq` 只更新本地游标、不做去重，只是多了几次无效刷新）。

**修复方向**：resync 帧不要带 `id`（或带一个大于当前最大序号的哨兵）；`Last-Event-ID` 解析加 `try/except`；重连时先 subscribe → 记录 `latest_seq` → replay 到该序号 → 再排空队列。

---

### P1-7 跨工程状态泄漏：切换工程后上一期的数据留在界面上

**位置**：`App.tsx:22-56`（`WorkspacePage` 无 `key`）、`VoicePage.tsx:44-49`、`EditScriptPage.tsx:95-103`、`VisualPage.tsx:28-30`

`WorkspacePage` 用 `switch(view)` 渲染页面组件，**没有给页面加 `key={projectId}`**。当 `currentProjectId` 变化而 `view` 不变时，React 按"同类型同位置"复用组件实例 → 所有 `useState` 保留：

- `VoicePage` 的 `bindings`（声音绑定）、`refNames`、`selectedUnitId`；
- `EditScriptPage` 的 `viewRevId`、`turns`、`selected`；
- `VisualPage` 的 `selectedId`。

由于 `bs = bindings ?? defaultBindings(proj)`（`VoicePage.tsx:57`），只要用户在新工程里没触发过 `setBindings`，界面就会显示**上一个工程的提供方/模型/参考音频**。`EditScriptPage` 的 reload effect 依赖 `revision?.id`（`:112-118`），而两个工程的草稿版本很可能都叫 `R1`，effect 不触发 → 直接显示上一个工程的话轮。

**修复方向**：`<VoicePage key={projectId} ... />`，或让相关页面在 `projectId` 变化时显式重置本地状态。

---

### P1-8 「AI 建议」等文字色对比度不达标（2.15–2.4:1）

**位置**：`styles/duo-shell.css:89`（`.hf-ai`）、`:106`（`.hf-cand .t`）

这两处把 `--wu-semantic-brand-hover`（`#FA875D`）用作**文字颜色**。实测对比度：

| 前景 | 背景 | 对比度 | 判定 |
|---|---|---|---|
| `#FA875D` brand-hover | `#FFFEFA` surface | **2.40** | FAIL |
| `#FA875D` brand-hover | `#F4F1EC` muted | **2.15** | FAIL |
| `#F67748` brand | `#FFFEFA` | **2.72** | FAIL（同样作文字用） |

而 05 §4.4/§1.1 明确要求"文字/浅底组合对比度全部 ≥4.5:1"，且专门规定了 `-text / -strong` 深阶用于文字场景。这是**违反自己设计系统的**可访问性硬伤，且落在"AI 建议"这种需要被注意的标签上。

其余令牌全部达标（success 5.75 / warning 5.88 / danger 5.29 / info 5.82 / speaker-b 5.22 / caption 4.92–5.17），说明配色体系本身是好的，只是这两处用错了色阶。

**修复方向**：为 brand 增加 `brand-text` 深阶（如 `#B4562A` 一类 ≥4.5:1 的值）并替换这两处。

---

### P1-9 `Alert tone="muted"` 没有任何样式，渲染为裸文本块

**位置**：`styles/components.css:51-58`（只定义了 success/warning/danger/info 四种），使用点：`VoicePage.tsx:243`、`VisualPage.tsx:129`、`RenderPage.tsx:152`

核对构建产物确认规则不存在：

```
grep 'wu-alert\[data-tone=' dist/assets/*.css
→ danger / info / success / warning   （没有 muted）
```

`.wu-alert` 基类只有 `padding` + `border-radius`，所以 muted 提示渲染成"一段带内边距的普通文字"，与周围卡片不成体系。这三处恰好都是"还没有时间轨/主图"这类重要的空态引导。

**修复方向**：补 `.wu-alert[data-tone="muted"]` 规则，或把这三处改用 `Card`/`Empty` 组件。

---

### P1-10 响应式行为完全缺失

**位置**：`styles/duo-shell.css:31`（`.hf-rail` 固定 208px）、`:55`（`.hf-inspector` 固定 320px）；`AppShell.tsx`

05 §4.4 规定三档：**≥1440 完整四区 / 1024–1439 检查器抽屉化 / <1024 单栏（阶段轨收纳为顶部横向条）**。实际实现里：

- 没有任何 `@media` 或 `@container` 查询（`.hf-app` 上声明了 `container-type: inline-size`，但**没有任何查询使用它**）；
- 侧轨 208 + 检查器 320 全部为固定宽度、不允许收缩。

窗口宽度小于约 1200px 时，主视图被挤到无法阅读；小于 900px 基本不可用。技术选型里写的 Tailwind 也没接入（实际全是手写 CSS + 内联 style）。

**修复方向**：至少补上 1024–1439 的检查器抽屉化与 <1024 的单栏两个断点。

---

### P1-11 TimelineBar（底部时间轨）整体缺失

**位置**：01 §1.5 `242-244`、01 §0.4 `198`、05 §1.1 `537`、05 §4.4

设计文档把它列为应用壳的固定分区之一："**配音确认后展开常驻，可折叠（56 / 28px），状态存工程**"。而实现中**没有任何对应组件**：`components/` 下只有 `AppShell.tsx`（TopBar / StageRail / StageHead / FootBar）和 `wu.tsx`。`FootBar`（48px）被复用成了操作栏，阶段③的时间轨被塞进主视图的普通卡片里。

HANDOFF 的"下一步"只提到"时间轨波形（WaveSurfer）"待接入 —— 但实际上**连容器壳都没有**，WaveSurfer 接进来无处安放，且"折叠状态存工程"这一条完全没做。

**修复方向**：在 `AppShell` 内新增 `TimelineBar` 分区（56 / 28px + 折叠状态写入 `project`），再把阶段③的波形接进去。

---

## 4. P2 级问题

### 4.1 后端

| # | 问题 | 位置 |
|---|---|---|
| 1 | **`Any` 未导入，12 处 F821。** `mock.py` 用了 `dict[str, Any]` 但从未 `import`；`from __future__ import annotations` 让运行时侥幸不报错，一旦有工具调用 `get_type_hints()`（pydantic/FastAPI 内省）即 `NameError` | `adapters/mock.py:34,40,70,119,143,156,174` |
| 2 | 5 处未使用导入：`asyncio`、`Path`、`Field`、`StreamingResponse`、`Any` | `main.py:9,11`、`domain/render.py:7`、`api/events.py:9`、`services/voice_svc.py:10` |
| 3 | 两处 `# noqa: SLF001` 是**无效抑制**（ruff 报 "Invalid noqa directive"），等于没写 | `api/machine.py:24`、`api/artifacts.py:15` |
| 4 | `asyncio.get_event_loop()` 已弃用，应用 `get_running_loop()` | `jobs/manager.py:48` |
| 5 | `ArtifactStore.register` 每次读-改-写整个 manifest，**无去重、无锁**；`by_hash` / `by_id` 是 O(n) 线性扫描并反复读全文件。`dependencyHash` 里带了 `job.id`（`main.py:77-79`），**永远不可能命中**，缓存复用实际未实现 | `storage/artifacts.py:27-66` |
| 6 | `CacheStore` 装配了却从未被任何服务调用（死代码），而资产库页脚却写着"缓存命中即复用，不重复推理与计费" | `storage/machine.py:39-62`、`AssetsPage.tsx:60` |
| 7 | `PATCH /api/projects/{id}` 对未知字段**静默丢弃**（pydantic 默认 `extra='ignore'`），字段名写错不会报错；`expectedRevision` 缺失时 `None != revision` → 直接 409 而非 400 | `api/projects.py:53-64` |
| 8 | `stage_progress` 字段被持久化进 `project.json`，但每次读取都被 `_inject_states` 的派生值覆盖（死字段）；`list_projects` 里对同一工程 `store.load()` 调了两次 | `domain/project.py:94`、`api/projects.py:33` |
| 9 | `job.attempt` 从不自增，"第 N 次尝试"永远显示第 1 次；重试机制未实现；`retryable` 恒为 `False` | `jobs/manager.py:146`、`TasksPage.tsx:135` |
| 10 | uvicorn 访问日志未纳入结构化日志（`setup_logging` 只配置了 `duocast` logger），JSON 日志与裸文本混排 | `core/logging.py:35-40` |
| 11 | `app.mount("/", StaticFiles(...))` 注册在 `@app.get("/")` 之前，导致 `root()` 是**不可达的死代码** | `main.py:209-218` |
| 12 | 事件总线只在内存里保留缓冲，进程重启后 `replay()` 无从补发（`events.jsonl` 写了但不读）；`RESYNC_EVENT` 是模块级可变 dict，被多订阅者共享 | `core/eventbus.py:31,79-83` |
| 13 | `docstring` 声称 `publish` "可在任意线程调用"，实现却对 `asyncio.Queue` 做 `put_nowait`（非线程安全 API）。**本次未复现异常**（子线程发 10 条、含队列满分支，均正常），但属潜在风险，建议改用 `loop.call_soon_threadsafe` | `core/eventbus.py:24,49-69` |
| 14 | `ArtifactStore` 不记录工程归属，`audio-*` / `video-*` 无法追溯到工程（只能靠 `paramsSnapshot` 里的 revisionId 反推） | `main.py:77,103` |

### 4.2 前端逻辑

| # | 问题 | 位置 |
|---|---|---|
| 15 | 候选改写结果靠"**列表里第一条成功的 rewrite 任务**"回填；若新一次改写失败，会把上一次的旧候选重新弹出来 | `EditScriptPage.tsx:121-131` |
| 16 | 自动保存计时器**未在卸载时清理**，切页后仍会发出 PATCH | `EditScriptPage.tsx:134-142` |
| 17 | `useConfirm` 从 query 缓存里读 `approvals` 再整体回传（`api.ts:203-217`）。缓存缺失/过期时会上报**不完整的 approvals 数组**，导致历史确认记录丢失 | `lib/api.ts:199-224` |
| 18 | `voiceBindings` 里的 `localRefAudio` 被 `bindingsPayload` 剥掉（`VoicePage.tsx:74-81`），文件选择器选中的参考音频**从未发给后端**，界面却显示"已绑定（本地引用）" | `VoicePage.tsx:92-96, 216` |
| 19 | 绑定编辑（模型名等）只存本地 state，未合成前离开页面即丢失；页面也没有"从工程重新载入绑定"的 effect | `VoicePage.tsx:45, 87-90` |
| 20 | `useJobStore.setActiveJobs` / `activeJobIds` 从未被任何页面读取（死代码）；`App.tsx:78` 用 `as never` 绕过类型检查，把 `String(ev.status)` 塞进 `JobStatus` 参数 | `stores/index.ts:36`、`App.tsx:78` |
| 21 | `touch()` 把 `cancel_requested` 当未结束状态，任务会永久留在活动集合 | `stores/index.ts:47` |
| 22 | 渲染不需要样片确认即可开始（`RenderPage.tsx:78` 的 `canRender` 不含 `sampleApproved`，只给黄色警告），而阶段④的"下一步"却强制要求 —— 两道门禁口径不一致 | `RenderPage.tsx:71,78` |
| 23 | `TasksPage` 把 `cancel_requested` 归入"已结束"页签，但它其实还在跑 | `TasksPage.tsx:14` |
| 24 | `Progress` 的进度值是按 `stage` 枚举映射的**假进度**（20/50/70/85/95），与真实任务进度无关 | `wu.tsx:241-248`、`TasksPage.tsx:43-45` |

### 4.3 UI / 交互 / 可访问性

| # | 问题 | 位置 |
|---|---|---|
| 25 | **假控件 / 空实现**：HomePage 的「打开工程目录」弹 `alert()`、「保存为模板」无 `onClick`、「新建模板」只是打开新建弹层；`CreateEpisodePage` 生成中的「取消」按钮点击后只 `setError(...)`，而错误提示渲染在**另一个分支**里，用户根本看不到；EditScriptPage 质量提示的「拆分」按钮执行 `setView('script')`（已在当前页，等于无操作）；VoicePage「试听例句」永久 `disabled` | 多处 |
| 26 | `HomePage` 的新建弹层允许选模板（`templateId`），但 `handleCreate` 完全忽略它（`HomePage.tsx:63-72`）—— 选"科技周报"和选"空白"结果一样 | `HomePage.tsx:63-72` |
| 27 | 底栏宣称"**Ctrl+Z 撤销**"，代码里没有任何撤销/历史栈实现 | `EditScriptPage.tsx:522` |
| 28 | TopBar 硬编码 `VRAM 17%`，与"服务设置"里的 VRAM 分配、`/api/machine` 无任何关联；`ServicesPage` 的"已连接"徽章取 `cap ? ...` 恒为真（mock 一定返回 dict），永远显示"已连接/可用"，无法表达真实的未连通态 | `AppShell.tsx:30`、`ServicesPage.tsx:104-106` |
| 29 | **键盘可达性**：`.hf-turn`（`EditScriptPage.tsx:339-357`）与 `.hf-task-row`（`TasksPage.tsx:108-118`）是 `div + onClick`，无 `role` / `tabIndex` / 键盘事件，鼠标专用；`.hf-link` 是无 `href` 的 `<a onClick>`（多处），无法 Tab 聚焦、无法回车触发 | 多处 |
| 30 | 拖拽排序：整张话轮卡 `draggable`，与"点击卡片选中"手势冲突；拖拽没有落点指示器，也没有键盘替代方案（设计指定的 dnd-kit 未接入） | `EditScriptPage.tsx:343-356` |
| 31 | 点击台词文本进入编辑时 `stopPropagation`，导致**编辑这句的同时不会选中它**，检查器仍停留在上一句 | `EditScriptPage.tsx:399-404` |
| 32 | 阶段① 的「节目名」输入框初值是空字符串，从不与 `proj.title` 同步，已有工程打开时输入框显示的是 placeholder 而非真实标题；重命名只在 `blur`/回车时提交 | `CreateEpisodePage.tsx:41, 187-195` |
| 33 | 阶段① 的所有表单值（sourceKind / content / durationMode / 标题）都不落库，离开页面即丢；已生成脚本时回到阶段① 只看到空输入框 | `CreateEpisodePage.tsx:36-42` |
| 34 | `Alert tone="muted"` 无样式（见 P1-9）；`hf-grid3` / `hf-pcard` / `hf-sec-head` 三个类名在 CSS 中不存在（靠内联样式撑着，属无效类名） | 见 P1-9、`HomePage.tsx:103,110,116` |
| 35 | 任务/资产卡片选中态用 `outline: 1px solid` 内联实现，未用设计系统定义的 focus ring；`wu-*` 基线的 `:focus-visible` 规则要求祖先有 `.wu` 类，虽然 `main.tsx:22` 包了，但页面里大量元素是裸 `div`，焦点依赖默认 UA 样式 | 多处 |
| 36 | 事件流的 `startSse` 被 `App.tsx:72-89` 放在以 `currentProjectId` 为依赖的 effect 里 —— **每次切换工程都会断开重连 SSE** | `App.tsx:72-89` |

### 4.4 工程与流程

| # | 问题 |
|---|---|
| 37 | **前端零 lint 配置**：没有 eslint / prettier / biome，`package.json` 无 lint 脚本；后端也没有被配置为 CI 门禁的 ruff/black（本次审查是临时装的） |
| 38 | 测试只有脚本式冒烟（`tests/smoke.py`、`tests/smoke_full.py`、`smoke_e2e.py`）。`tests/` 下没有一个是 pytest 用例（`smoke_full.py` 全是裸语句 + `print`，**零断言**），`pyproject.toml` 里配了 `testpaths = ["tests"]` 但 `pytest` 跑起来收集不到任何测试 |
| 39 | `apps/storage/projects/EP001/project.json` 里的中文已损坏为字面量 `"?????"`（`"title": "?????"`、`"content": "????\n????"`）。其余工程（EP002/EP005/EP3487/EP3517）编码正常，判断是早期某个客户端以非 UTF-8 提交所致，非代码缺陷，但建议清理该脏数据 |
| 40 | `.review/` 审查脚本目录未加入 `.gitignore`（如不需要可删除；本次审查后保留作为回归用例） |

---

## 5. 与设计文档的偏差清单

| 文档承诺 | 出处 | 实现状态 |
|---|---|---|
| 页面状态七态（含**已过期**） | README / 01 §13.1 | ❌ 只实现 4 态，`stale` / `failed` 从未产出 |
| 内容改动后自动标记下游失效 | 04 重跑矩阵 | ❌ 无失效传播，改脚本后配音仍显示"已确认" |
| 时间权威 = PCM 样本数（含间隔与首尾） | README §技术事实 3 / 04 | ❌ 少算全部间隔与 leadIn/tailOut |
| 三队列 GPU/API/CPU 并行（1/16/4） | 02 §7、06 §6 | ❌ 串行执行，并发上限失效 |
| 启动恢复扫描 | README「已实现」 | ❌ 扫描了但结果不写回，任务中心重启后为空 |
| 确认记录绑定输入版本**与规格** | 04 / 01 §13.2 | ⚠️ 只绑了 revisionId，未绑规格 → 重跑不失效 |
| 缓存命中即复用 | 02 §5.9 / 资产库文案 | ❌ `CacheStore` 未接线，hash 永不相同 |
| TimelineBar 常驻可折叠（56/28） | 01 §1.5 / 05 §1.1 | ❌ 组件不存在 |
| 1024–1439 检查器抽屉化、<1024 单栏 | 05 §4.4 | ❌ 无任何断点 |
| 文字对比度 ≥4.5:1 | 05 §1.1 / §4.4 | ❌ brand 系文字 2.15–2.72:1 |
| 高级参数折叠区（默认收起） | 01 §1.3、05 §3 | ❌ 未实现 |
| dnd-kit 排序 / WaveSurfer / Konva | README「未做」已声明 | ✅ 声明一致（但容器壳也缺，见 P1-11） |
| 真实提供方适配器 | README「未做」已声明 | ✅ 声明一致 |
| mock 能力声明需与真实一致 | HANDOFF「已知坑」 | ❌ `dualTrackInput: true` 与 R1 未验证矛盾；`timestamps: true` 却不返回时间戳 |

---

## 6. 做得好的地方（建议保留的设计）

- **分层与依赖纪律**：`api → services → storage/domain`，`adapters` 不 import 业务概念，`ProjectStore.apply()` 是唯一写入口 —— 代码结构与 06 §2 的规划高度一致，新人上手成本低。
- **revision 乐观锁**：`apply()` 用 `expectedRevision` + 深拷贝 + 原子替换（tmp → fsync → `os.replace`），并在同步上下文里降级为立即落盘（`project_store.py:89-94`），考虑到了线程池端点没有事件循环的情况。
- **适配器契约**：`Protocol` + `CapabilityRegistry` 作为能力门控唯一数据源，替换真实提供方时改动面很小。
- **恢复策略的克制**：无查询能力时标 `unknown` 而不是盲目重跑（`recovery.py:35-40`），这条纪律是对的（虽然结果没被使用）。
- **设计令牌体系**：`tokens.css` 的 global → semantic → component 三层派生干净，除 brand 文字色外其余 11 组对比度全部 ≥4.5:1。
- **前端类型与构建**：`strict` + `noUnusedLocals` 全开，`tsc --noEmit && vite build` 零错误，`dist` 与源码同步。
- **文档密度**：01/04/05/06 + HANDOFF 对"哪些是真、哪些是假"交代得很清楚，审查时几乎不需要猜意图。

---

## 7. 建议的修复路线

### 第一批（对外演示前必须完成）

1. P0-1 历史版本编辑覆盖草稿 —— 传 `baseRevId`，跨版本强制新建。
2. P0-2 自动保存 409 静默失败 —— 统一 `onError` + 冲突重放/提示。
3. P0-3 pause/cancel 状态机 —— 补终态收敛 + 真正的取消信号；`transition` 不再发 `job.failed`。
4. P0-4 启动恢复 —— `JobManager` 加载磁盘任务并重新入队。
5. P1-8 / P1-9 —— 品牌文字色阶 + `muted` alert 样式（改动量极小、收益直观）。

### 第二批（核心机制对齐文档）

6. P1-1 时间权威补间隔与首尾，加断言测试。
7. P1-2 三队列真并发（每队列一个消费协程 + 信号量）。
8. P1-4 stale 推导 + 确认记录绑规格（这是产品最有价值的卖点，也是当前最大的"文档-实现"落差）。
9. P1-5 artifactIds 与 manifest 对齐，加一致性断言。
10. P1-3 修订号生成收敛到一处并去重。
11. P1-6 SSE 事件 id 修正 + 重连去重。
12. P1-7 页面加 `key={projectId}`。

### 第三批（体验与工程化）

13. P1-10 / P1-11 响应式断点 + TimelineBar 壳。
14. P2-25 清理假控件（要么实现、要么隐藏），特别是阶段①的「取消」按钮与底栏的「Ctrl+Z」文案。
15. 接入 eslint + ruff 到 `build`/CI 门禁；把 `.review/` 下 4 个复现脚本改写成 pytest/vitest 用例。
16. 删除死代码：`CacheStore`、`setActiveJobs`、`stage_progress` 字段、`root()` 死路由、未使用导入。

---

## 附录 A · 复现命令

```bash
# 依赖（隔离环境，不污染系统）
python -m venv ~/.workbuddy/binaries/python/envs/duocast
~/.workbuddy/binaries/python/envs/duocast/Scripts/pip install fastapi "uvicorn[standard]" pydantic sse-starlette ruff

# 后端缺陷复现（临时目录，不触碰 apps/storage）
~/.workbuddy/binaries/python/envs/duocast/Scripts/python .review/verify_backend.py

# HTTP 层复现（需先起临时实例）
cd apps/server && DUOCAST_STORAGE=../../.review/rt DUOCAST_PORT=8123 \
  python -m uvicorn duocast.main:app --host 127.0.0.1 --port 8123 &
python .review/verify_http.py && python .review/verify_http2.py

# 前端草稿逻辑复现
npx esbuild .review/fe-check.ts --bundle --platform=node --format=esm --outfile=.review/fe-check.mjs
node .review/fe-check.mjs

# 静态检查
python -m ruff check apps/server --select F,E9,B,SIM,C4
cd apps/web && pnpm build      # 已通过，零错误
```

## 附录 B · 静态检查原始结果

```
ruff check apps/server --select F,E9,B,SIM,C4   → 19 errors
  12 × F821  Undefined name `Any`            adapters/mock.py
   5 × F401  未使用导入                        main.py / render.py / events.py / voice_svc.py
   2 × SIM105 try-except-pass                 core/eventbus.py:61,65
  +2 × 无效 noqa 指令（SLF001）               api/machine.py:24 / api/artifacts.py:15

ruff check apps/server --select UP                → 27 errors（全为 Optional→X|None 等风格项，可 --fix）
```
