# 双声播客工坊 DuoCast · 项目实施方案

> 版本 v2.0 ｜ 目标：Windows 本地部署的双人播客视频自动化生产工作台
>
> **本文档的写作目标**：说清「用什么技术、为什么这么选、怎么落地、哪里会踩坑」，让一个开发者能据此开工。

---

## 目录

- [1. 项目目标与范围](#1-项目目标与范围)
- [2. 技术选型](#2-技术选型)
- [3. 系统架构](#3-系统架构)
- [4. 本地环境与依赖](#4-本地环境与依赖)
- [5. 关键算法实现](#5-关键算法实现)
- [6. 数据与工程目录规范](#6-数据与工程目录规范)
- [7. 接口契约](#7-接口契约)
- [8. 性能预算与容量规划](#8-性能预算与容量规划)
- [9. 风险清单与应对](#9-风险清单与应对)
- [10. 开发路线图](#10-开发路线图)
- [11. 验收标准](#11-验收标准)
- [12. 运维与故障排查](#12-运维与故障排查)
- [附录 A：一键部署清单](#附录-a一键部署清单)
- [附录 B：参数速查表](#附录-b参数速查表)

---

## 1. 项目目标与范围

### 1.1 目标

让一位创作者在**本地 Windows 机器**上，通过一个 Web 工作台，产出一段 ≤10 分钟（默认 1–3 分钟）的双人对谈视频，全程数据不出本机。

### 1.2 核心能力

| 能力 | 说明 |
|---|---|
| 角色资产化 | A 主持人固定复用，B 嘉宾每期可变；形象与音色都作为可复用资产 |
| 内容自动生成 | 话题 / 文章 → 播客稿 → 逐句分角脚本 |
| 音频先行 | 按说话人整段合成，ASR 回填句级时间码，形成唯一时间坐标系 |
| 图生图融合 | 角色设定图 + 场景图 → 融合生成「两人已在场景中」的同框底图 |
| 镜头编排 | 基于静音间隙自动切镜，配数字运镜（推/拉/平移），不做摇镜 |
| 分段渲染 | 串行队列、可续跑、段间重叠、固定种子 |
| 三级模式 | 手动可调 → 确认关卡 → 一键生成 |

### 1.3 明确不做（本期边界）

- ❌ 多人（>2）对话
- ❌ 真实摇镜 / 复杂运镜 / 多机位切换（只做切镜 + 数字推拉）
- ❌ 云端推理、多用户、账号体系
- ❌ 实时预览（渲染都是离线的）
- ❌ 移动端完整支持（仅保证可查看）
- ❌ 字幕 / 多语言配音 / 后期特效

---

## 2. 技术选型

### 2.1 选型总览

| 层 | 选型 | 版本建议 | 理由 |
|---|---|---|---|
| 前端框架 | **React 18 + TypeScript** | 18.3+ / 5.4+ | 生态最全，AI 生成页面命中率最高 |
| 构建 | **Vite** | 5+ | 启动快，HMR 好 |
| 样式 | **Tailwind CSS** | 3.4+ | 原子类，配合设计 token 一致性好 |
| 组件库 | **Radix UI / shadcn-ui** | 最新 | 无样式、可访问性好、可完全自定义 |
| 状态管理 | **Zustand** | 4+ | 轻量，适合单一 state 对象 + 派生 |
| 数据请求 | **TanStack Query** | 5+ | 缓存、重试、乐观更新 |
| 音频波形 | **WaveSurfer.js** | 7+ | 时间轨、播放头、缩放 |
| 画布编辑 | **Konva + react-konva** | 9+ | 站位框、mask 精修、拖拽 |
| 拖拽排序 | **dnd-kit** | 6+ | 句序、段落排序 |
| 后端 | **Python 3.11 + FastAPI** | 3.11 / 0.110+ | AI 生态原生；WebSocket 成熟 |
| 异步任务 | **Celery + Redis** 或 **arq** | — | 渲染队列、长任务；单机可用 arq（更轻） |
| 实时通信 | **WebSocket**（FastAPI 原生） | — | 进度回传、关卡暂停 |
| 推理编排 | **ComfyUI** | 最新 | 图生图 / TTS / 口型视频的工作流宿主 |
| 视频后处理 | **ffmpeg** | 6.0+ | 拼接、运镜滤镜、音画合并 |
| 补帧 | **RIFE**（ComfyUI 节点或独立） | — | 25fps → 50fps |
| ASR 对齐 | **faster-whisper** | large-v3 | 句级时间码回填 |
| 数据库 | **无（JSON 文件）** | — | 单机、单用户，文件即数据库 |

### 2.2 关键选型说明

#### 为什么用 FastAPI + ComfyUI 双层，而不是纯 ComfyUI？

ComfyUI 擅长「一次推理」，不擅长「编排」。本项目的复杂度几乎全在编排：

- 分段调度、串行队列、断点续跑
- 音轨对齐、补静音、拉平两条等长轨
- 镜头表 → ffmpeg 滤镜链
- 关卡暂停 / 恢复
- 依赖失效传播

这些逻辑塞进 ComfyUI 工作流会变成一团乱麻。**ComfyUI 只负责「图 → 图」「文 → 音」「图+音 → 视频」这三种原子推理，其余全部在 FastAPI 层。**

#### 为什么不用数据库？

单机、单用户、一个工程一个文件夹。JSON 文件天然支持「工程可复制、可打包、可版本管理」，而且调试时可以直接看文件。工程量大时（>100 期）再加 SQLite 索引即可。

#### 为什么前端不直接调 ComfyUI？

- 需要鉴权与路径隔离（前端不应知道 ComfyUI 的文件系统）
- 需要统一的任务抽象（`jobId` + WebSocket）
- 需要把多个原子推理串成一个业务流程

### 2.3 模型选型

| 用途 | 首选 | 备选 | 说明 |
|---|---|---|---|
| **TTS / 音色克隆** | **IndexTTS-2.5** | Fun-CosyVoice3-0.5B | 2.5 支持中英日西阿、8 维情绪向量、`duration_factor` 语速控制、`use_random` 可关（保音色） |
| **形象图 / 场景图 / 底图融合** | **Qwen-Image-Edit 系列**（图生图 + 多参考图） | FLUX Kontext 系列 | 关键是**多参考图 + 身份保持**；Qwen 系在中文提示词与多图参考上表现好 |
| **口型视频** | **InfiniteTalk-MultiTalk** | LongCat-Video-Avatar 1.5 | InfiniteTalk 有官方 ComfyUI 支持、无限长、多音频 + mask；LongCat 更强但目前无 ComfyUI 节点 |
| **补帧** | **RIFE** | — | ComfyUI 节点或独立 CLI |

> **重要事实（已核对源码/官方文档）**：
> - InfiniteTalk 的多人物分支：`audio_type` 支持 `para`（并行相加）和 `add`（串行拼接 + 零填充）；`ref_target_masks` 是「每说话人一张语义 mask + 可选背景 mask」，用于**跨注意力分配**（哪条音轨驱动哪块像素）。
> - 未提供 bbox 时，默认 mask 是**画面左右各半**。因此底图必须让两人左右分居、居中留空。
> - 官方明确：I2V 模式超过 1 分钟会出现明显颜色漂移，需分段 + 固定首帧。
> - IndexTTS-2.5 的 8 维情绪顺序固定为 `[happy, angry, sad, afraid, disgusted, melancholic, surprised, calm]`；`use_random=True` 会降低克隆保真度。

---

## 3. 系统架构

### 3.1 分层

```
┌───────────────────────────────────────────────────────────────────┐
│  UI 层（React SPA，浏览器）                                        │
│  · 9 个步骤视图 · 右检查器 · 底部时间轨 · 关卡面板 · 一键生成        │
│  · 状态：Zustand 单一 state · 请求：TanStack Query                 │
└──────────────────────────┬────────────────────────────────────────┘
                           │ HTTP / WebSocket
                           ▼
┌───────────────────────────────────────────────────────────────────┐
│  编排层（FastAPI @ 127.0.0.1:8000）                                │
│  ┌─────────────┬─────────────┬─────────────┬───────────────────┐ │
│  │ 工程服务     │ 任务服务     │ 时间轨服务   │ 编排服务          │ │
│  │ project.json │ job queue   │ 对齐/补静音  │ 分段/队列/关卡    │ │
│  └─────────────┴─────────────┴─────────────┴───────────────────┘ │
│  · WebSocket 推送 · 日志 · 文件管理 · ffmpeg 调用                  │
└──────────────────────────┬────────────────────────────────────────┘
                           │ HTTP + WebSocket
                           ▼
┌───────────────────────────────────────────────────────────────────┐
│  推理层（ComfyUI @ 127.0.0.1:8188）                                │
│  · 图生图工作流（形象/场景/底图融合）                               │
│  · IndexTTS 工作流（整段合成 / 试听句）                             │
│  · InfiniteTalk 工作流（图 + 双音轨 + mask → 口型视频）             │
│  · RIFE 工作流（补帧）                                              │
└───────────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌───────────────────────────────────────────────────────────────────┐
│  后处理层（ffmpeg / 本地 CLI）                                     │
│  · 分段拼接 + 交叉融合 · 数字运镜滤镜链 · 音画合并 · 导出           │
└───────────────────────────────────────────────────────────────────┘
```

### 3.2 任务模型

所有耗时操作统一抽象为 **Job**：

```typescript
interface Job {
  id: string;
  projectId: string;
  type: JobType;                 // 'image' | 'tts' | 'align' | 'video' | 'post' | 'auto'
  step: StepId;                  // 归属步骤
  status: 'queued' | 'running' | 'paused' | 'done' | 'failed' | 'cancelled';
  progress: number;              // 0–100
  message: string;               // 人类可读进度
  params: Record<string, unknown>;// 提交时的完整参数（用于复现）
  result?: unknown;
  error?: string;
  createdAt: string;
  startedAt?: string;
  finishedAt?: string;
  logPath: string;               // logs/{jobId}.jsonl
}
```

**任务队列策略**：

| 策略 | 说明 |
|---|---|
| **串行执行** | 单 GPU，推理任务严格串行，避免显存冲突 |
| **优先级** | 交互式任务（试听句、单张图）> 批量任务（分段渲染） |
| **可取消** | 取消时终止 ComfyUI 当前 prompt 并清理临时文件 |
| **可续跑** | 队列状态持久化在 `render/queue.json`，重启后可恢复 |
| **进度来源** | 订阅 ComfyUI 的 `/ws`，按节点执行进度映射为 0–100 |

### 3.3 ComfyUI 工作流模板

三套模板，前端只替换参数，不改节点结构：

| 模板 | 文件 | 关键可替换参数 |
|---|---|---|
| **图生图** | `workflows/image_edit.json` | 参考图路径、提示词、负面提示词、分辨率、步数、CFG、种子、批量 |
| **TTS** | `workflows/indextts.json` | 参考音路径、文本、情绪向量、`emo_alpha`、`duration_factor`、`use_random`、`lang` |
| **口型视频** | `workflows/infinitetalk.json` | 底图路径、音轨1路径、音轨2路径、mask 路径、帧数、`audio_type`、`audio_cfg_scale`、`motion_frame`、步数、种子、分辨率 |
| **补帧** | `workflows/rife.json` | 输入视频、倍率 |

> **模板管理约定**：模板以 JSON 存放于 `backend/workflows/`，由后端在提交前做参数注入。**不要把参数硬编码进节点**，全部通过占位符（如 `{{prompt}}`）替换。

---

## 4. 本地环境与依赖

### 4.1 目标机器（当前实测）

| 项 | 值 |
|---|---|
| GPU | NVIDIA RTX 5080，16 GB 显存，驱动 591.86 |
| CPU | Intel i7-14700KF |
| 内存 | 32 GB |
| 系统 | Windows |

> **注意**：RTX 5080 是 Blackwell 架构（sm_120）。部分 CUDA 扩展（尤其 flash-attn 的预编译轮子）可能不兼容，需要从源码编译或改用 xformers / SDPA。**这是本项目第一个要验证的环境风险。**

### 4.2 软件依赖

| 组件 | 版本 | 说明 |
|---|---|---|
| Python | 3.11 | IndexTTS 与 ComfyUI 的共同可用版本 |
| PyTorch | 2.6+ / cu124+ | 需匹配 sm_120 |
| CUDA | 12.4+ | 与驱动匹配 |
| ffmpeg | 6.0+ | 需在 PATH 中 |
| Node.js | 20 LTS | 前端构建 |
| ComfyUI | 最新 | 建议独立目录 + 独立虚拟环境 |
| IndexTTS | 2.5 | 已在本地 |
| faster-whisper | 最新 | ASR 对齐 |

### 4.3 模型权重清单

| 模型 | 用途 | 预估体积 |
|---|---|---|
| IndexTTS-2.5 | TTS | ~5 GB |
| 图生图模型（Qwen-Image-Edit 或 FLUX Kontext） | 形象/场景/底图 | ~20–30 GB |
| Wan2.1-I2V-14B-480P（InfiniteTalk 底座） | 口型视频 | ~30 GB |
| InfiniteTalk multi 权重 | 口型视频 | ~2 GB |
| chinese-wav2vec2-base | 音频编码 | ~1 GB |
| RIFE | 补帧 | ~0.1 GB |
| faster-whisper large-v3 | ASR | ~3 GB |

> 磁盘预留 **≥150 GB**（权重 + 工程产物 + 缓存）。

### 4.4 显存预算（单卡 16 GB）

| 阶段 | 估算占用 | 配置建议 |
|---|---|---|
| 图生图（1024×1024） | 8–12 GB | 可跑 |
| 图生图（1280×720） | 10–14 GB | 可跑，注意批量数 |
| IndexTTS-2.5 | 4–6 GB | 可跑 |
| InfiniteTalk 480p + int8/fp8 | 12–16 GB | **临界**，需 `num_persistent_param_in_dit=0` |
| InfiniteTalk 720p | >16 GB | **单卡跑不了**，需量化或分块 |
| RIFE 补帧 | 2–4 GB | 可跑 |

> **关键约束**：InfiniteTalk 480p 在 16 GB 上属于临界状态。**必须实测**，并准备好「降分辨率 / 降帧数 / 用 fp8 量化 / 关掉 TeaCache 之外的加速」等降配手段。

---

## 5. 关键算法实现

### 5.1 音频：按说话人整段合成 + 句级回填

```
输入：lines[] = [{id, who, text, tone, rate, prePause, postPause}]

Step 1  分组：同一说话人的「连续段落」各成一段
        runs = []
        current = null
        for line in lines:
            if current == null or line.who != current.who:
                current = {who: line.who, lines: []}
                runs.append(current)
            current.lines.append(line)

Step 2  逐段合成（IndexTTS-2.5）
        for run in runs:
            text = join(run.lines.map(l => l.text), "，")
            emoVector = blend(角色情绪向量, run 内语气映射)   # 8 维
            df = 角色.durationFactor * mean(run.lines.rate)
            audio = IndexTTS.infer(
                spk_audio_prompt = 角色.reference,
                text             = text,
                emo_vector       = emoVector,
                emo_alpha        = 1.0,
                duration_factor  = df,
                use_random       = false,       # 必须 false
                lang             = "ZH"
            )
            缓存 audio

Step 3  按全局顺序拼接
        cursor = 0
        for run in runs:
            if run 前有停顿: cursor += prePause
            place run.audio at cursor
            cursor += run.audio.duration
            cursor += postPause
        → A_full.wav / B_full.wav（各自只含自己的说话段 + 静音）

Step 4  句级时间码回填（ASR 强制对齐）
        用 faster-whisper 对每个 run 的音频做强制对齐，
        得到 run 内每句的 [start, end]，映射回全局时间轴。
        confidence < minConfidence → 标记人工确认。

Step 5  构造两条严格等长的轨
        T = 全局总时长
        A_track = zeros(T); 对 A 的每句写入其 [start,end]
        B_track = zeros(T); 对 B 的每句写入其 [start,end]
        assert len(A_track) == len(B_track) == T   # add 模式前置条件

Step 6  混音
        mix = A_track + B_track      # 脚本保证不抢话，故不会削顶
```

**为什么整段合成**：单次零样本克隆推理，音色漂移机会只有一次。
**为什么还要回填**：视频侧需要句级切点与重录能力，回填让两者兼得。

### 5.2 镜头表：从静音间隙到镜头段

```
输入：timeline（句级 [start,end]）、shotConfig

Step 1  找静音间隙
        gaps = []
        cursor = 0
        for line in sorted(timeline, by start):
            if line.start - cursor >= minSilence:
                gaps.append({start: cursor, end: line.start})
            cursor = max(cursor, line.end)

Step 2  候选切点 = 每个 gap 的中点

Step 3  按最短段长切分
        cuts = []
        segStart = 0
        for p in candidateCutPoints:
            if p - segStart >= minShotLength:
                cuts.append(p); segStart = p
        末尾不足 minShotLength 的段并入前一段

Step 4  判定镜头类型（启发式）
        对每段统计 A/B 发言时长占比：
          A 占比 >= 0.7 → soloA
          B 占比 >= 0.7 → soloB
          否则          → wide

Step 5  分配运镜（保守）
        · 连续同类型段 > 2 → 第 3 段起加轻微推近/拉远
        · 段长 > 20s → 允许一次推近
        · 默认 static

Step 6  计算裁切框
        soloA: crop = expand(stations.A, margin=0.15)，再按 cropScale 放大到输出尺寸
        soloB: 同理
        wide:  crop = 全画面
```

### 5.3 数字运镜：ffmpeg 滤镜链

对每个镜头段生成一段 `zoompan` / `crop` 滤镜，然后按时间轴串联。

**推近（zoom in）示例**：

```bash
ffmpeg -i seg.mp4 -filter_complex \
  "zoompan=z='min(zoom+0.0005,1.15)':d=1:x='iw/2-(iw/zoom/2)':y='ih/2-(ih/zoom/2)':s=854x480:fps=25" \
  -c:v libx264 -crf 18 -pix_fmt yuv420p out.mp4
```

**单拍裁切 + 推近**：

```bash
ffmpeg -i master.mp4 -filter_complex \
  "[0:v]crop=w=540:h=360:x=120:y=90,scale=854:480,zoompan=z='1+0.10*on/(25*12)':d=1:x='iw/2-(iw/zoom/2)':y='ih/2-(ih/zoom/2)':s=854x480[out]" \
  -map "[out]" out.mp4
```

**通用化：按镜头段生成表达式**

```python
def build_filter(shot, out_w, out_h, fps):
    x, y, w, h = shot.crop
    parts = []
    if shot.type != 'wide':
        parts.append(f"crop=w={w}:h={h}:x={x}:y={y}")
        parts.append(f"scale={out_w}:{out_h}")
    if shot.camera.move != 'static':
        strength = shot.camera.strength
        frames = int((shot.end - shot.start) * fps)
        easing = EASING[shot.camera.easing]
        if shot.camera.move in ('pushIn', 'pushPan'):
            z = f"1+{strength}*{easing}(on/{frames})"
        elif shot.camera.move == 'pullOut':
            z = f"{1+strength}-{strength}*{easing}(on/{frames})"
        else:
            z = "1"
        parts.append(
            f"zoompan=z='{z}':d=1:"
            f"x='iw/2-(iw/zoom/2)':y='ih/2-(ih/zoom/2)':"
            f"s={out_w}x{out_h}:fps={fps}"
        )
    return ",".join(parts) if parts else "null"
```

> **注意**：`zoompan` 会引入抖动。建议先放大到 2× 再 zoompan，最后缩小，或用 `scale` + `crop` 的组合替代。**这是需要实测调优的点。**

### 5.4 分段渲染与拼接

```
Step 1  分段
        优先在镜头段边界切分；若镜头段长于 segLength，则再按 segLength 均分。
        每段记录：起始时间、结束时间、帧数、需要哪些音轨切片、对应 mask。

Step 2  逐段渲染（串行）
        for seg in segments:
            1. 从 A_track / B_track 裁出 [seg.start, seg.end] 的音频片段
            2. 提交 InfiniteTalk 工作流：
               image      = master.png
               audio_1    = A_seg.wav
               audio_2    = B_seg.wav
               mask_1/2/bg= masks/*.png
               audio_type = 'add'
               frames     = seg.frames
               seed       = 固定种子 + seg.index（保证可复现）
               audio_cfg  = 4.0
               motion_frame = 9
            3. 订阅进度，写入 queue.json
            4. 保存 seg_XXX.mp4

Step 3  拼接（含交叉融合）
        相邻段重叠 overlapFrames 帧。
        用 ffmpeg xfade 或逐帧加权融合：
        ffmpeg -i a.mp4 -i b.mp4 -filter_complex \
          "[0][1]xfade=transition=fade:duration=0.2:offset=<a_dur-0.2>" out.mp4
        段数多时用 concat demuxer 分组合并，避免一次加载全部。

Step 4  运镜
        按 shots.json 逐段套滤镜后重新拼接。

Step 5  补帧
        RIFE: 25fps → 50fps

Step 6  音画合并
        ffmpeg -i video.mp4 -i mix.wav -c:v copy -c:a aac -b:a 192k -shortest final.mp4
```

> **一致性关键**：所有分段必须共用**同一张底图、同一对 mask、同一套采样参数**；种子用「基础种子 + 段号」的确定性组合，不要每段随机。

### 5.5 依赖失效传播

```python
DEPENDENCIES = {
  'P1': ['P4', 'P5', 'P6', 'P7', 'P8'],   # 角色变更影响音色与形象
  'P2': ['P3', 'P4', 'P5', 'P6', 'P7', 'P8'],
  'P3': ['P4', 'P5', 'P6', 'P7', 'P8'],
  'P4': ['P6', 'P7', 'P8'],
  'P5': ['P6', 'P7', 'P8'],
  'P6': ['P7', 'P8'],
  'P7': ['P8'],
}

def mark_stale(state, changed_step):
    for s in DEPENDENCIES.get(changed_step, []):
        state.stale[s] = True
```

更细的粒度：记录每个产物依赖的**参数哈希**，只有哈希变化才标过期（避免「改了无关参数也全标过期」）。

---

## 6. 数据与工程目录规范

### 6.1 工程目录

```
DuoCast/projects/EP001/
├── project.json
├── assets/characters/     # 角色形象图、参考音
├── assets/scene/          # 场景图
├── assets/imports/        # 用户原始素材
├── script/article.md
├── script/podcast.md
├── script/lines.json
├── audio/A_full.wav
├── audio/B_full.wav
├── audio/A_track.wav
├── audio/B_track.wav
├── audio/mix.wav
├── audio/lines/L001.wav …
├── images/charA.png
├── images/charB.png
├── images/scene.png
├── images/master.png
├── masks/person1.png
├── masks/person2.png
├── masks/background.png
├── shots/shots.json
├── shots/filters.json
├── render/queue.json
├── render/seg_001.mp4 …
├── output/final.mp4
└── logs/run-*.jsonl
```

### 6.2 文件命名约定

| 类型 | 约定 | 示例 |
|---|---|---|
| 工程 | `EP{三位序号}` | `EP001` |
| 句 | `L{三位序号}` | `L003` |
| 镜头段 | `S{两位序号}` | `S03` |
| 渲染分段 | `seg_{三位序号}` | `seg_007` |
| 日志 | `run-{YYYYMMDD-HHMMSS}.jsonl` | `run-20260908-143022.jsonl` |
| 角色资产 | `char{角色}{版本}.png` | `charA_v2.png` |

### 6.3 资产引用

- `project.json` 中一律存**相对工程目录的路径**，不存绝对路径、不存 base64
- 素材上传时复制进 `assets/imports/`，再按用途引用
- 所有图片/音频记录尺寸、时长、大小，便于预算估算

### 6.4 状态持久化时机

| 时机 | 说明 |
|---|---|
| 自动保存 | 改动后 2s 防抖写入 `project.json` |
| 步骤完成 | 立即写入 |
| 任务完成 | 立即写入（含产物路径） |
| 应用退出 | 强制写入 |
| 崩溃恢复 | 启动时检查 `render/queue.json` 与 `logs/`，提示「上次渲染中断，是否从断点继续」 |

---

## 7. 接口契约

> 完整接口列表见《页面详细设计文档》第 13 章。此处只强调实现约定。

### 7.1 通用约定

| 约定 | 说明 |
|---|---|
| Base URL | `http://127.0.0.1:8000/api` |
| 鉴权 | 无（仅监听 127.0.0.1） |
| 错误格式 | `{code, message, detail, step}` |
| 长任务 | 一律返回 `{jobId}`，进度走 WebSocket |
| 幂等 | 提交类接口带 `clientToken`，重复提交返回同一 jobId |
| 取消 | `POST /api/jobs/{jobId}/cancel` |

### 7.2 错误码

| code | 含义 | 前端表现 |
|---|---|---|
| `ENGINE_DOWN` | ComfyUI 未连接 | 禁用推理按钮 + 提示启动命令 |
| `MODEL_MISSING` | 模型文件缺失 | 提示缺失文件与路径 |
| `VRAM_INSUFFICIENT` | 显存不足 | 提示降配方案 |
| `INVALID_PARAM` | 参数越界 | 定位到具体字段 |
| `ASSET_MISSING` | 依赖素材缺失 | 高亮对应上游步骤 |
| `JOB_CANCELLED` | 任务被取消 | 静默 |
| `RENDER_FAILED` | 渲染失败 | 显示错误 + 重试 |
| `ALIGN_LOW_CONFIDENCE` | 对齐置信度低 | 列入人工确认列表 |

---

## 8. 性能预算与容量规划

### 8.1 耗时估算模型

```
总耗时 ≈ TTS耗时 + 图片耗时 + 视频渲染耗时 + 后处理耗时

TTS：  音频时长 × RTF(≈0.2–0.35)          → 3 分钟音频 ≈ 40–60 秒
图片：  每张 10–60 秒 × 张数
视频：  成片时长 × 渲染倍率 R
       R 取决于分辨率/步数/量化，必须实测
       参考：480p + 8 步 + 量化，预期 R ≈ 3–10
后处理：拼接 + 运镜 + 补帧，约成片时长 × 1–2
```

### 8.2 典型场景预算（RTX 5080 单卡，480p）

| 成片时长 | 帧数(25fps) | 分段数(20s) | 视频渲染 | 后处理 | 合计 |
|---|---|---|---|---|---|
| 30 秒 | 750 | 2 | 2–5 分钟 | 1 分钟 | **3–6 分钟** |
| 1 分钟 | 1,500 | 3 | 5–10 分钟 | 2 分钟 | **7–12 分钟** |
| 3 分钟 | 4,500 | 9 | 15–30 分钟 | 5 分钟 | **20–35 分钟** |
| 10 分钟 | 15,000 | 30 | 50–100 分钟 | 15 分钟 | **65–115 分钟** |

> **这些数字必须用 pilot 实测校准。** 上表是量级参考，不是承诺。

### 8.3 容量

| 项 | 估算 |
|---|---|
| 单个 3 分钟工程磁盘占用 | 音频 ~50 MB + 图片 ~100 MB + 分段视频 ~1.5 GB + 成片 ~200 MB ≈ **2 GB** |
| 100 期工程 | ~200 GB |
| 模型权重 | ~60 GB |

---

## 9. 风险清单与应对

### 9.1 高优先级风险（会导致方案不成立）

| # | 风险 | 影响 | 应对 | 验证时机 |
|---|---|---|---|---|
| **R1** | InfiniteTalk 480p 在 16 GB 上跑不起来或极慢 | 整个视频侧不成立 | pilot 实测；准备 fp8/int8 量化、降帧数、降分辨率；必要时换 LongCat（需自建 API） | **第 1 周** |
| **R2** | RTX 5080（sm_120）与 flash-attn / CUDA 扩展不兼容 | 环境搭不起来 | 优先用 xformers 或 SDPA；从源码编译；必要时用 WSL2 | **第 1 周** |
| **R3** | 镜头切换导致身份/背景漂移 | 成片不可用 | 主用同框 + 数字运镜；真切镜限量；固定底图与种子；段间重叠 | 第 2 周 |
| **R4** | 长片颜色漂移（官方已知） | 后半段偏色 | 分段渲染，每段重置首帧；限制单段 ≤30 秒；必要时加色彩校正 | 第 3 周 |
| **R5** | 图生图融合出的底图两人站位不对 | mask 错位、口型串位 | 底图生成 prompt 强制「左右分居、居中留空」；提供 bbox 校正；加自动检测 | 第 2 周 |

### 9.2 中优先级风险

| # | 风险 | 应对 |
|---|---|---|
| R6 | 整段合成音色稳但句级时间码不准 | ASR 强制对齐 + 低置信人工确认 + 手动微调时间码 |
| R7 | 数字运镜 `zoompan` 抖动 | 用 scale+crop 替代；或先 2× 放大再缩小 |
| R8 | 拼接处跳变 | 段间 25 帧重叠 + 交叉融合；重叠区做直方图匹配 |
| R9 | 两人音色区分度不足 | P1 加自动检测；提示换参考音 |
| R10 | 一键生成中途失败难定位 | 每步日志 + 保留中间产物 + 从失败步继续 |
| R11 | 显存被其他程序占用 | 渲染前检查显存，低于阈值时提示关闭其他程序 |
| R12 | 工程文件损坏 | 每次保存前写临时文件再原子替换；保留最近 5 个备份 |

### 9.3 产品风险

| # | 风险 | 应对 |
|---|---|---|
| R13 | 用户期望「点一下就出片」，实际需要调参 | 明确区分「手动打磨」与「一键生成」；一键生成前强制检查参数完整性 |
| R14 | 步骤太多导致上手难 | 提供「快速开始」引导：默认参数 + 只填必要项 |
| R15 | 单步重跑成本高 | 局部重跑（单句、单段）；缓存已完成的产物 |

---

## 10. 开发路线图

### 阶段 0：技术验证（1 周）— **最关键，先做这个**

| 任务 | 产出 | 通过标准 |
|---|---|---|
| 0.1 环境搭建 | ComfyUI + IndexTTS-2.5 + InfiniteTalk 可运行 | 三个工作流各跑通一次 |
| 0.2 **Pilot 渲染** | 一张同框底图 + 两条交替音轨 + `add` 模式，30–60 秒 | 四项验收：口型跟得上、非说话人不抢戏、画面稳定、音色区分 |
| 0.3 **吞吐实测** | 480p/8步/量化的渲染倍率 R | 得出真实 R 值，校准预算表 |
| 0.4 图生图融合验证 | 角色设定图 + 场景图 → 底图 | 两人站位正确、光照一致、无接缝 |

> **决策点**：0.2 或 0.3 不通过 → 重新评估视频侧模型（LongCat / 其他）或降低目标（更短、更低分辨率）。

### 阶段 1：骨架与角色库（1.5 周）

| 任务 | 产出 |
|---|---|
| 1.1 前端骨架 | React + Vite + Tailwind + 四区布局 + 步骤轨 |
| 1.2 后端骨架 | FastAPI + 工程服务 + 任务服务 + WebSocket |
| 1.3 P1 角色库 | 形象生成/上传、参考音、情绪向量、试听 |
| 1.4 工程持久化 | project.json 读写 + 自动保存 |

### 阶段 2：内容与音频（1.5 周）

| 任务 | 产出 |
|---|---|
| 2.1 P2 内容输入 | LLM 生成播客稿（流式） |
| 2.2 P3 分角脚本 | 自动分角 + 逐句编辑 |
| 2.3 P4 配音锁轨 | 整段合成 + ASR 回填 + 时间轨组件 |
| 2.4 时间轨 | 波形、播放头、双轨、缩放 |

### 阶段 3：视觉与镜头（1.5 周）

| 任务 | 产出 |
|---|---|
| 3.1 P5 视觉设定 | 场景生成、底图融合、站位标注、mask 生成 |
| 3.2 P6 镜头表 | 自动切分、镜头类型、数字运镜、滤镜链导出 |
| 3.3 时间轨集成 | 镜头段轨 + 切点吸附 |

### 阶段 4：渲染与成片（2 周）

| 任务 | 产出 |
|---|---|
| 4.1 P7 试镜 | 样片渲染 + 验收清单 |
| 4.2 P8 渲染队列 | 分段、串行队列、进度、续跑 |
| 4.3 后处理 | 拼接、运镜、补帧、音画合并 |
| 4.4 导出 | 成片输出 + 日志 |

### 阶段 5：一键生成与打磨（1.5 周）

| 任务 | 产出 |
|---|---|
| 5.1 确认关卡 | CheckpointPanel + 三种模式配置 |
| 5.2 P9 一键生成 | 全流程编排 + 失败恢复 |
| 5.3 依赖失效 | 过期标记 + 重算下游 |
| 5.4 打磨 | 参数默认值优化、提示语、空态、错误处理 |

**总工期估算：约 9 周**（单人全职，不含等待模型下载）。

---

## 11. 验收标准

### 11.1 功能验收

| 编号 | 验收项 | 标准 |
|---|---|---|
| F1 | 角色库 | A 锁定后跨期复用；B 可切换 |
| F2 | 内容生成 | 输入话题 → 生成结构完整、时长符合目标的播客稿 |
| F3 | 分角 | 自动分角结果可用，逐句可改说话人/语气/语速 |
| F4 | 配音 | 整段合成音色稳定；句级时间码误差 ≤0.3s |
| F5 | 底图 | 图生图融合出两人站位正确、光照一致的底图 |
| F6 | 镜头表 | 切点落在静音间隙；可逐段配置类型与运镜 |
| F7 | 试镜 | 5 秒样片四项验收可完成 |
| F8 | 渲染 | 分段串行渲染，可暂停/续跑；拼接无跳变 |
| F9 | 导出 | 成片音画同步，分辨率与帧率符合设置 |
| F10 | 一键生成 | 从话题到成片，仅在最必要的关卡停顿 |

### 11.2 质量验收

| 编号 | 验收项 | 标准 |
|---|---|---|
| Q1 | 口型同步 | 主观评分 ≥4/5（样片） |
| Q2 | 非说话人静止 | 不说话方无明显动作抢戏 |
| Q3 | 画面稳定性 | 无面部崩坏、背景无扭曲 |
| Q4 | 身份一致性 | 跨镜头同一人可辨认 |
| Q5 | 音色区分度 | 盲听可区分 A/B |
| Q6 | 音频质量 | 无爆音、无断裂、无异常停顿 |

### 11.3 工程验收

| 编号 | 验收项 | 标准 |
|---|---|---|
| E1 | 可调性 | 任意步骤可单独重跑；单句/单段可局部重跑 |
| E2 | 可恢复 | 崩溃后可从断点续跑 |
| E3 | 可复现 | 相同参数 + 相同种子 → 相同产物 |
| E4 | 可观测 | 每个任务有日志、进度、错误详情 |
| E5 | 隐私 | 全程仅访问 127.0.0.1，无外部请求 |

---

## 12. 运维与故障排查

### 12.1 启动顺序

```powershell
# 1. 启动 ComfyUI
cd <ComfyUI目录>
python main.py --listen 127.0.0.1 --port 8188

# 2. 启动编排服务
cd <backend目录>
uvicorn app.main:app --host 127.0.0.1 --port 8000

# 3. 启动前端（开发）
cd <frontend目录>
npm run dev
```

### 12.2 常见故障

| 现象 | 原因 | 排查 |
|---|---|---|
| 顶栏显示「引擎未连接」 | ComfyUI 未启动或端口占用 | `curl http://127.0.0.1:8188/system_stats` |
| 模型芯片变灰 | 权重缺失或路径不对 | 检查 ComfyUI 的 `models/` 目录 |
| 渲染中断，显存 OOM | 显存不足 | 降分辨率/降帧数/开量化/关其他程序 |
| 口型对不上 | mask 错位或音频未对齐 | 检查站位标注、`audio_type`、音频长度是否等长 |
| 不说话的人在动 | `audio_type` 用了 `para` | 改为 `add`；检查静音填充是否正确 |
| 背景漂移/变形 | 长片漂移 | 缩短单段时长、固定首帧、增加段间重叠 |
| 音色忽变 | 逐句合成或 `use_random=True` | 改整段合成、关 `use_random` |
| 拼接处跳变 | 重叠不足 | 增加 `overlapFrames`、启用交叉融合 |
| 运镜抖动 | `zoompan` 精度 | 改用 scale+crop；或先放大再缩小 |
| 时间码不准 | ASR 对齐失败 | 提高 ASR 模型规格；人工微调 |

### 12.3 日志规范

每行一条 JSON：

```json
{"ts":"2026-09-08T14:30:22.123Z","jobId":"j_001","step":"P4","event":"tts.start","role":"A","segments":3,"chars":1024}
{"ts":"...","jobId":"j_001","step":"P4","event":"tts.done","duration":108.32,"rtf":0.28}
{"ts":"...","jobId":"j_002","step":"P8","event":"segment.start","segId":"#03","frames":1313}
{"ts":"...","jobId":"j_002","step":"P8","event":"segment.failed","segId":"#03","error":"CUDA out of memory"}
```

---

## 附录 A：一键部署清单

- [ ] 安装 Python 3.11 + Node 20 + ffmpeg
- [ ] 安装 CUDA 12.4+ 驱动（已满足：591.86）
- [ ] 创建 ComfyUI 虚拟环境，验证 sm_120 兼容性
- [ ] 下载 IndexTTS-2.5 权重并跑通单句合成
- [ ] 下载图生图模型并跑通单张生成
- [ ] 下载 InfiniteTalk 权重（multi）+ Wan2.1-I2V-14B-480P + wav2vec2
- [ ] 下载 RIFE + faster-whisper large-v3
- [ ] 导入三套 ComfyUI 工作流模板
- [ ] 跑通 pilot：30 秒双人交替
- [ ] 实测渲染倍率 R，校准预算表
- [ ] 部署编排服务与前端

## 附录 B：参数速查表

| 参数 | 默认 | 范围 | 调大的影响 |
|---|---|---|---|
| IndexTTS `duration_factor` | 1.00 | 0.5–2.0 | 语速变慢、时长变长 |
| IndexTTS `emo_alpha` | 1.0 | 0–1 | 情绪更强，音色可能偏移 |
| IndexTTS `temperature` | 0.8 | 0–1.5 | 更有变化，音色稳定性下降 |
| IndexTTS `use_random` | **false** | — | **保持 false**，否则降低克隆保真度 |
| 底图 `fusion_strength` | 0.75 | 0–1 | 更贴近参考图身份，自由度下降 |
| 底图 `seed` | 固定 | — | **必须固定**，用于跨段一致 |
| InfiniteTalk `audio_cfg_scale` | 4.0 | 3–5 | 口型更准，可能不自然 |
| InfiniteTalk `motion_frame` | 9 | — | 运动幅度更大 |
| InfiniteTalk `audio_type` | `add` | `add`/`para` | `add`=交替，`para`=同时说话 |
| 镜头 `min_shot_length` | 3.0s | 1–10 | 切镜更少、段更长 |
| 镜头 `min_silence` | 0.25s | 0.1–1.0 | 更少切点 |
| 镜头 `camera_strength` | 0.12 | 0.05–0.25 | 推拉更猛 |
| 渲染 `overlap_frames` | 25 | 0–50 | 拼接更顺滑，成本增加 |
| 渲染 `seg_length` | 20s | 15–30 | 单段更长，漂移风险上升 |
| 渲染 `steps` | 8 | 4–40 | 质量提升，耗时线性上升 |
