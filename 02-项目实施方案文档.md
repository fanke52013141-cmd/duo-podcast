# 双声播客工坊 DuoCast · 项目实施方案

> 版本 v3.0 ｜ 依据：《详细优化方案》（04-详细优化方案.md）｜ 目标：Windows 本地单用户的双人播客视频制作工作台
>
> **本文档的写作目标**：说清「用什么技术、为什么这么选、怎么落地、哪里会踩坑」，让一个开发者能据此开工。页面与数据契约见 01；本文件的算法章节（§5）是相关规则的权威定义。

---

## 目录

- [1. 项目目标与范围](#1-项目目标与范围)
- [2. 技术选型](#2-技术选型)
- [3. 系统架构与职责](#3-系统架构与职责)
- [4. 提供方适配契约](#4-提供方适配契约)
- [5. 关键算法实现](#5-关键算法实现)
- [6. 数据与工程目录规范](#6-数据与工程目录规范)
- [7. 任务调度、恢复与 Windows 运行](#7-任务调度恢复与-windows-运行)
- [8. 接口契约](#8-接口契约)
- [9. 性能预算与容量规划](#9-性能预算与容量规划)
- [10. 风险清单与应对](#10-风险清单与应对)
- [11. 开发路线图](#11-开发路线图)
- [12. 验收标准](#12-验收标准)
- [13. 运维与故障排查](#13-运维与故障排查)
- [附录 A：环境与依赖清单](#附录-a环境与依赖清单)
- [附录 B：参数与能力速查](#附录-b参数与能力速查)

---

## 1. 项目目标与范围

### 1.1 目标

让一位创作者在**本地 Windows 机器**上，通过一个 Web 工作台，按五阶段（创建本期 → 编辑对话 → 试听配音 → 预览画面 → 生成导出）产出一段**完整成片不超过 300 秒**的双人对谈视频。

**数据边界（与 v2.1 的关键变化）**：工程与成片保存在本机；文本与图片请求发送到所选 API；MiniMax 启用时发送相应语音输入；本地语音合成与本地口型视频生成不离开本机。**不再承诺「全程数据不出本机」。**

### 1.2 核心能力

| 能力 | 说明 |
|---|---|
| 两次生成直出对话 | 先内容结构、再结构化话轮；已有脚本可导入解析 |
| 三层音频对象 | 句子（编辑/字幕/读音）、发言（自然话轮）、合成单元（引擎请求） |
| 多引擎语音 | 统一 TTS 接口：本地引擎默认 + MiniMax（系统音色 / 已有 voice_id） |
| 样本数时间权威 | 48kHz PCM 母轨，整数样本偏移，节目时长以样本数为准 |
| 图片 API 或上传 | 角色图、场景、同框底图可 API 生成或上传替代 |
| 镜头与分段解耦 | 基础视频分段独立于后期镜头；改镜头不重跑推理 |
| 分段可恢复渲染 | 三区间（output/render/context）、暂停、取消、断点恢复 |
| 版本驱动 | 草稿/候选/已用三版本身份；产物不可变版本路径 |
| 局部重跑 | 按重跑矩阵精确传播，最小化重复推理与重复计费 |
| 横竖屏 | 横屏主路径；竖屏版式导出；原生竖屏动态待验证 |

### 1.3 明确不做（首版边界）

- ❌ 多人（>2）对话、同时说话 / 抢话
- ❌ 本地文本模型、本地图像生成 / 编辑模型（不部署 Ollama 与本地图像模型）
- ❌ 多用户、账号体系、云端渲染、跨机器分布式渲染
- ❌ 实时预览（渲染均为离线）
- ❌ 未经验证的原生竖屏高规格动态
- ❌ 默认补帧（RIFE 可选、默认关闭）、花字、复杂片头包装
- ❌ 多语言翻译配音、逐音素编辑、全功能时间线编辑器

---

## 2. 技术选型

### 2.1 选型总览

| 层 | 选型 | 说明 |
|---|---|---|
| 前端框架 | React 18 + TypeScript + Vite | **沿用 v2.1 选型，不因产品调整重选** |
| 样式 / 组件 | Tailwind CSS + Radix/shadcn | 同上 |
| 状态 / 请求 | Zustand + TanStack Query | 同上；版本与任务状态由服务端推导，前端不自算 |
| 可视化 | WaveSurfer.js / Konva / dnd-kit | 时间轨 / 人物区域矩形标注 / 排序 |
| 后端 | Python 3.11 + FastAPI | WebSocket / SSE 成熟 |
| 文本 | **通用文本 API 适配器** | 一套实际可用实现；请求格式、流式解析、错误码、结构校验在适配层 |
| 图片 | **图片 API 适配器** | 能力分开登记：文生图 / 单图编辑 / 多参考图编辑 / 比例 / 异步 / 取消 / 下载 |
| 语音 | **统一 TTS 接口 + 两适配器** | LocalTTS（ComfyUI 或隔离进程，默认）+ MiniMaxTTS |
| 视频 | ComfyUI 对口型工作流 | 本地；双轨时序适配层待验证（见 5.5） |
| 后处理 | ffmpeg | 拼接 / 镜头 / 版式 / 字幕烧录 / 音画合并；RIFE 补帧可选 |
| 对齐 | faster-whisper（或 WhisperX 对齐） | 优先使用提供方时间信息，本地对齐为回退 |
| 数据库 | 无（工程文件夹 + JSON） | project.json 存工程索引，jobs 存任务快照与事件，artifacts 存产物清单 |

### 2.2 关键选型说明

#### 为什么文本 / 图片不塞进 ComfyUI？

ComfyUI 生态强在本地视觉 / 音频模型。文本与图片走 HTTP API 时，编排服务直接调用适配器即可获得重试、限流、结构校验与费用控制；包进 ComfyUI 节点只会多一层无意义的转发。**ComfyUI 只承载本地推理任务（口型视频，以及可用时的本地 TTS）。**

#### 为什么本地 TTS 可能用隔离进程？

本地 TTS 优先沿用可用 ComfyUI 工作流；如果语音依赖与视频依赖冲突（版本、显存），可用隔离进程。**UI 与业务接口保持一致**（都是 `providerProfileId` 指向的 LocalTTS 适配器），是否分进程由依赖验证决定，不要求第一天同时维护两套本地语音入口。

#### 为什么删除 Ollama 与本地图像模型？

04 方案的已确认约束：通用文本 API + 图片 API，不部署本地文本或图像生成模型。本地 LLM（Qwen3-8B）与本地图像模型（Qwen-Image-Edit）从依赖清单中移除；隐私承诺相应改为「工程与成片本地，文本/图片经所选 API」。

### 2.3 提供方与模型候选

> **候选状态声明**：以下为原方案候选，**未实测标定**。版本名、默认步数、兼容性不写成已验证事实；生产工作流固定版本并通过验证集后，才成为「已验证组合」。

| 用途 | 候选 | 状态 |
|---|---|---|
| 本地 TTS | IndexTTS-2.5 | 候选；以本机可运行的 ComfyUI 工作流验证为准 |
| 口型视频 | InfiniteTalk-MultiTalk | 候选；双轨适配是第一验证项（阶段 A） |
| 补帧 | RIFE | 可选，默认关闭 |
| 文本 API | 由服务设置配置 | 联调前提供服务名称、地址和模型 |
| 图片 API | 由服务设置配置 | 联调前明确，特别核实多参考图编辑能力 |
| 云语音 | MiniMax（`/v1/t2a_v2`） | 首版支持系统音色与已有 voice_id |

---

## 3. 系统架构与职责

### 3.1 分层

```
本地浏览器工作台（React SPA）
    │  HTTP + SSE/WS（事件带序号，重连可补发）
    ▼
本地编排服务 FastAPI（127.0.0.1:8000）
    ├─ 文本适配器 ───────── 文本 API（云端）
    ├─ 图片适配器 ───────── 图片 API（云端）
    ├─ 语音适配器
    │    ├─ LocalTTS ───── ComfyUI 或隔离的本地语音进程
    │    └─ MiniMaxTTS ─── MiniMax API
    ├─ 对齐、音轨、镜头与资产服务
    ├─ 本地视频适配器 ───── ComfyUI 对口型工作流
    ├─ 后处理 ─────────── FFmpeg；补帧可选
    └─ 工程文件、任务日志、版本和缓存
```

职责划分原则：

- **编排层**负责：业务规则（单元划分、三区间、重跑矩阵、版本采用）、任务快照与恢复、事件推送、文件唯一写入入口
- **适配层**负责：请求格式、流式解析、错误码映射、结构校验、能力声明、时间信息归一化
- **ComfyUI** 负责本地推理原子任务；参数注入用**节点 ID 与字段的结构化映射**，不做 JSON 文本字符串替换

### 3.2 三类任务队列

| 队列 | 资源 | 规则 |
|---|---|---|
| GPU 队列 | 单卡排他 | 本地 TTS、视频、使用 GPU 的对齐、补帧共享；严格串行 |
| API 队列 | 按提供方限流 | 文本、图片、MiniMax；并发按提供方配置；**不占 GPU 锁** |
| CPU / 磁盘队列 | 限并发 | 解码、对齐、后处理；避免 32 GB 内存与磁盘被同时挤满 |

- **优先级在任务或片段边界生效**；试听任务不能无损抢占正在执行的 GPU 采样
- 从 TTS 切视频时检查模型卸载与资源释放——「串行请求」不自动代表上一模型已释放显存

### 3.3 工作流版本与验证包

每套通过验证的组合保存为**验证包**：

```
ComfyUI commit、节点仓库及 commit、模型文件校验值、量化方式、
采样器、步数、尺寸、帧率、帧数约束、音频编码器、输入映射、
依赖环境、测试样片结果
```

- 参数注入：节点 ID + 字段的结构化映射；提交前检查节点输入及类型；能力探测失败时提示修复工作流，**不把参数缺失错误拖到长片中途**
- 升级工作流版本后必须重跑验证集，才能用于正式生成

---

## 4. 提供方适配契约

### 4.1 文本与图片 API

**通用文本 API 不等于所有协议和结构化输出能力相同**。适配器负责：请求格式、流式解析、错误码、结构校验；业务层只接收统一结果。

**图片 API 能力至少分开登记**：

| 能力项 | 说明 |
|---|---|
| 文生图 / 单图编辑 / 多参考图编辑 | 三种能力独立开关；连接成功不代表具备人物一致性或多参考图能力 |
| 比例限制 | 登记支持的输出比例 |
| 异步任务 / 取消支持 | 决定任务模型映射 |
| 结果下载方式 | **下载成功且可解码后才标记完成**；远程 URL 只作来源记录，本地文件才是工程资产 |
| 确定性复现 | 可用种子和模型版本随结果保存；提供方不支持时不承诺同参必得同图 |

**同框底图路径**：首选支持多参考图的编辑接口；不支持时提供「上传已完成同框图」或「文字生成 + 人工检查」——**不默默把两个人物参考图丢掉**。

### 4.2 统一语音接口

```typescript
type TtsRequest = {
  projectRevision: string;
  unitId: string;
  speakerId: 'A' | 'B';
  providerProfileId: string;
  modelId: string;
  voiceBindingId: string;
  displayText: string;
  spokenText: string;
  emotion: { label: string; intensity?: number };
  speedRatio: number;               // >1 更快；适配器按具体模型参数方向换算
  pronunciationRevision: string;
  providerOptions: Record<string, unknown>;
};

type TtsResult = {
  originalAudioAssetId: string;     // 提供方原始音频
  canonicalAudioAssetId: string;    // 规范化 PCM
  sampleRate: number;
  sampleCount: number;              // 权威时长来源
  timingAssetId?: string;
  timingSource: 'provider' | 'aligner' | 'manual' | 'none';
  providerRequestId?: string;
  usage?: Record<string, number>;
  warnings: string[];
};
```

**能力声明**：每个提供方声明是否支持音色克隆、已有音色、情绪、语速、停顿、读音词典、时间戳、流式和取消。**UI 按能力渲染，不能假装某项设置生效。**

- 情绪标签与本地情绪向量的映射单独维护；不把向量直接传给 MiniMax
- 未支持的强度选项隐藏；不支持的语气在提交前解释降级方式

### 4.3 MiniMax 首版接入范围

首版支持：系统音色、已有 voice_id、短文本试听、按发言单元正式合成、语速与受支持的情绪、读音校正、音频落盘、时间信息归一化。**新建克隆音色是角色库中的可选操作，不是使用 MiniMax 的前置条件。**

实施要点（官方 HTTP 入口 `/v1/t2a_v2`，支持 `voice_setting`、读音词典、hex/URL 音频结果）：

1. 从服务配置读取区域、地址、模型和凭据引用；**不假定不同站点的音色 ID 互通**
2. 两位角色分别绑定声音，用同一段测试文本试听
3. 首版按短合成单元用**非流式请求，应用侧以后台 Job 执行**——HTTP 同步接口不代表页面阻塞等待
4. 音频解码、校验后转内部 PCM；保留原文件、实际请求参数、request/trace ID 和用量字段
5. 时间信息存在时与 spokenText 匹配（检查范围与顺序）；缺失或不可靠转本地对齐；**时间戳视为候选标注而非无条件准确值**
6. 不把某模型写成永远默认；提供「快速试听」「正式配音」预设，换模型后重新试听

异步接口（任务创建 / 查询）首版不引入——短单元合成不需要；未来扩展时持久化远程任务 ID 后查询续跑。

### 4.4 音色绑定与切换矩阵

角色身份、角色形象、声音绑定是三个对象。一个角色可同时有本地参考音绑定与 MiniMax voice_id 绑定，**不保证两种绑定声音完全相同**。

| 场景 | 行为 |
|---|---|
| 本地切 MiniMax | 保留脚本、形象、场景；要求选择可用音色并试听；重新生成受影响角色的音频 |
| MiniMax 切本地 | 检查本地参考音或声音资产；**不把 voice_id 当作本地可用音色** |
| 只有 A 切引擎 | B 的合成结果可复用；全局轨重新排布，视频按实际输入变化失效 |
| 修改绑定但尚未采用 | 当前配音保持已用版本；新声音作为候选 |
| 请求失败 | 保留旧结果；**不自动改用另一提供方生成同一角色剩余台词** |

新建 MiniMax 克隆音色流程：上传参考音 → 创建绑定 → 试听 → 正式使用。界面按实际响应显示状态（平台认证、未使用音色保留期限），**不把一次创建成功等同于永久可用**。

### 4.5 API 重试与费用控制

| 规则 | 说明 |
|---|---|
| 输入哈希复用 | 已成功内容按输入哈希复用；音频/图片已生成但下载失败时**优先恢复获取结果，避免再次付费生成** |
| 退避重试 | 明确限流可退避重试；余额 / 权限 / 参数错误直接停止并显示处理入口 |
| 不确定窗口 | 提交后超时且不知服务是否处理 → 标 `unknown`（结果待核实）；能查询就查询，不能则告知再次生成可能重复计费，**不盲目无限重试** |
| 预算 | 可选每期上限；缺价格配置时展示字符数、请求数、实际用量，**不伪造金额** |
| 取消语义 | 取消本地等待不代表云端任务已停止或不计费 |

---

## 5. 关键算法实现

> 本章为以下规则的**权威定义**：单元划分、停顿归属、建轨、对齐、双轨视频适配、分段三区间、重跑矩阵。01 / 03 引用本章，不另写版本。

### 5.1 三层对象与合成单元划分

| 层 | 对象 | 用途 |
|---|---|---|
| 句子 | Line | 编辑、字幕、读音修正 |
| 发言 | Turn | 一次自然说话行为，承载角色与对话意图 |
| 合成单元 | Unit | 实际送给一个语音引擎的一次请求，可覆盖一个或多个句子 |

**单元划分规则**：

```
按以下维度拆分单元：
  1. 说话人不同
  2. 语音提供方 / 模型 / 音色不同
  3. 明显情绪变化
  4. 语速变化
  5. 显式精确停顿（句内精确停顿需求 → 拆分后本地插静音）
  6. 超过最大长度上限
相邻同角色且参数一致的短句允许合并。
长度上限由引擎能力与试听效果确定，不固定为某模型的极限字符数。
```

**取舍声明**：「更自然」（长单元）与「更容易单句改动」（短单元）有权衡。默认以自然话轮为单位，允许用户拆分；**不宣称整段合成必定比逐句更保音色**。

### 5.2 停顿模型：单一归属

- **取消** pre + post 双重编辑：每两个单元之间保存**一个** `transitionGapMs`
- 首尾分别保存 `leadInMs` 与 `tailOutMs`
- **保留模型自然生成的呼吸与停顿**；显式间隔是**额外静音**并在界面说明
- 「从最后发声到下一次发声精确 0.5 秒」类需求：启用**有预览的边界裁切**（检测原音频尾部静音并裁掉），不能把模型原有尾静音和额外静音重复计算

### 5.3 建轨算法（样本数权威）

内部母轨统一 **48 kHz PCM**；视频模型所需采样率由适配层派生，**不反向改变母轨时长**；采样率转换不能被当作音质提升。

```text
cursor = leadInSamples
for unit in orderedUnits:
    unit.offsetSamples = cursor
    将 unit 的规范 PCM 放入对应说话人轨
    cursor += unit.sampleCount
    如果后面还有单元：cursor += 当前边界的 gapSamples
N = cursor + tailOutSamples
A、B 补齐为 N 个样本
mix = 经过增益与峰值检查的 A + B
节目权威时长 T = N / sampleRate
句级全局位置 = 单元局部位置 + unit.offsetSamples
```

- **整数样本偏移**，避免累计浮点误差
- 无抢话模式检查发言区间不重叠；**即使不重叠也要检查音频本身是否削顶**——不能依靠「交替说话」保证所有输入音量正常
- 片头片尾发言、首尾停顿全部计入 N；最终字幕与片尾留白与同一时间轴绑定，不能「限制 TTS 总时长后再附加 20 秒片尾」

### 5.4 对齐顺序与质量

```
优先级：提供方有效时间信息 → 本地对齐（合成单元级）→ 人工标注
```

- faster-whisper 的「转写 + 文本匹配」与 WhisperX 的 wav2vec2 **强制对齐是不同路径**；页面显示真实使用的方法
- 时间信息统一映射到规范音频，**记录重采样、裁切、插入静音造成的时间变换**
- 数字展开、英文缩写、朗读替换需要 spokenText 与 displayText 的对应关系
- 质量字段：文本覆盖率、无法匹配的词、时间范围异常、待人工处理片段
- 提供方时间戳与 ASR 概率**不共用未经校准的 0.6 阈值**
- 手工修改时间码只更新标注，不改变实际音频

### 5.5 双人音轨与视频适配（add 假设修正）

**本次核对事实**：InfiniteTalk 官方脚本中 `add` 会**串行拼接**两条输入；ComfyUI 主分支对应节点也存在内部串接行为。因此 v2.1 的「已经对齐的双轨 + 固定 `add`」**不能直接作为开发接口**。

**视频任务输入契约**：

```
同框底图 + A/B 人物区域 + 两条已按同一时间轴对齐的音轨
+ 需生成的区间 + 可选前段上下文 + 版本化工作流
```

必须满足：

- A/B 音轨总长度一致；音频条件最终长度对应**节目区间**，而非两倍长度
- A→B→A、以 B 开场、全静音短片段、只有单方发言的区间都有确定处理
- 零幅度音频经编码不一定等同全零音频特征：**根据已知发言区间建立活动掩码**，特征是否清零由适配器验证——不能凭波形静音假设另一人一定闭嘴
- 人物区域决定驱动绑定，**不保证听者绝对静止**；验收允许眨眼和轻微动作

**实施顺序**：

1. 先挑选支持时间对齐双轨的现有工作流并检查源码
2. 没有合适实现时，评估**新增独立适配节点**，保持两路特征在同一时间轴并正确处理静音
3. **不**直接给没有该输入的节点注入 `audio_type`；**不**为了配合串接接口重排用户台词

产品继续限制不抢话；底层并行对齐双轨表达 ≠ 允许同时发言。

### 5.6 分段：三个区间与连续性

**分段不等于镜头**：渲染段服务于模型时长、内存、失败重试和连续性；镜头服务于叙事和构图。默认以**发言边界附近的自然停顿与可用模型窗口**切段。

每段记录：

| 区间 | 含义 |
|---|---|
| `outputRange` | 该段在最终节目中负责的、不与其他保留区间重复的时间范围 |
| `renderRange` | 实际生成范围，可含边界缓冲 |
| `contextRange` | 续接引用的前段画面范围及来源资产版本 |

**示例**（40 秒节目，1 秒融合）：

```
第一段 output [0,20)   render [0,20.5)
第二段 output [20,40)  render [19.5,40)   共同覆盖 [19.5,20.5)
两份音频切片使用同样全局区间，融合后总长仍为 40 秒。
区间在执行前转换为整数帧（示例半秒边界按实际帧率量化）。
```

**连续性策略（三选一，主工作流只选一套）**：

| 策略 | 优点 | 代价与验证 |
|---|---|---|
| 参考前段末尾帧续接 | 有机会减轻姿态跳变 | 后段依赖前段；改前段可能连带重跑；需检查累积漂移 |
| 固定底图独立生成 | 易于独立重试 | 边界可能姿态复位，需设计切点或镜头过渡 |
| 公共时间区间融合 | 可缓和部分边界变化 | 人脸嘴部可能重影；**不是默认保证连续的办法** |

- 必要时周期性设置重新使用底图的**锚点**，限制续接依赖范围；锚点附近切换必须过样片检查
- 固定种子保存以便追溯，**不能代替连续性验证**；基础种子用**稳定片段 ID** 派生——插入一段后不能让所有后续段因编号变化而换种子；跨硬件不承诺位级复现
- 长业务段可由多个模型窗口内部生成；不把「20 秒片段」当作一次 500 帧推理请求
- 候选窗口（5 / 10 / 15 秒等）由本机试验确定

### 5.7 帧数与最终时长

- 使用**整数帧**作为视频坐标；采样点与帧之间通过同一个转换函数映射
- 模型要求特定帧数形式时先补足合法输入帧数，输出后去除额外上下文与补齐帧
- **验收**：最终帧覆盖完整音轨且误差 < 一个输出帧，不遗漏首尾语音；连续片段从全局边界统一推导，**不能每段各自四舍五入累积误差**

### 5.8 后处理顺序（权威）

```
基础视频完成
  → 按保留区间与连续性策略合成
  → 应用镜头裁切和版式
  → 可选补帧（在字幕烧录之前，以镜头边界分开处理，避免跨硬切生成过渡帧）
  → 可选烧录字幕
  → 合并权威音轨并编码（最终音频只合并一次，不把分段音轨再次混入）
  → 校验导出
```

- SRT 独立从校正后的时间信息生成；显示文本不包含引擎专用标签

### 5.9 缓存、版本与重跑（权威）

**缓存键**：

| 产物 | 缓存键包含 |
|---|---|
| 音频 | 实际朗读文本、声音绑定、模型、合成参数、读音词典、适配器版本 |
| 基础视频 | 底图、人物区域、该段实际音频内容、活动区间、输入规格、工作流、上下文资产 |
| 后期 | 已选视频资产、镜头、版式、字幕、输出规格 |

- 字幕显示文字的标点修改不影响朗读输入 → **不应触发 TTS**
- 全局起始时间变化但局部内容与上下文一致 → 可复用并更新时间位置；**不能仅凭段 ID 相同就复用**
- 哈希之外还必须**检查结果文件存在且可解码**

**重跑矩阵（权威定义）**：

| 改动 | 音频合成 | 时间轨/标注 | 基础视频 | 后处理 |
|---|---|---|---|---|
| 改台词的实际读法 | 对应单元 | 对应标注及后续偏移 | 变化区间及其上下文依赖 | 相关输出 |
| 只改字幕标点 | 否 | 字幕文本版本 | 否 | SRT 或烧录字幕 |
| 改显式发言间隔 | 否 | 重排 | 实际输入变化的段及依赖 | 是 |
| 改 A 的声音 | A 的单元 | 重新建立相关偏移 | 音频变化区间及依赖 | 是 |
| 只改时间标注 | 否 | 是 | 否，除非重新采用的分段计划改变输入 | 镜头与字幕相关部分 |
| 改底图/人物区域 | 否 | 否 | 对应视觉版本的段 | 是 |
| 改数字推拉/裁切 | 否 | 否 | 否 | 镜头处理及导出 |
| 开关烧录字幕 | 否 | 否 | 否 | 字幕与编码 |
| 改编码规格 | 否 | 否 | 通常否；改变推理规格除外 | 编码或版式 |
| 换模型或工作流 | 对应引擎产物 | 按实际影响 | 对应产物 | 对应下游 |

**续接模式的段必须把前段上下文作为依赖**——不能一面承诺跨段连续，一面承诺任何前段修改都绝不影响后段。

---

## 6. 数据与工程目录规范

### 6.1 工程目录

```
project/
  project.json               # 工程索引（单一写入入口维护）
  revisions/                 # 脚本、声音绑定与配置快照
  assets/
    characters/              # 角色形象（复制引用的版本）
    images/original/         # 原始分辨率图片
    images/model-input/      # 模型实际输入（含确定变换）
    masks/                   # 人物区域 mask
  audio/
    original/                # 提供方原始音频
    units/                   # 规范 PCM 单元
    timelines/               # 分轨、混音与标注版本
  shots/                     # 镜头计划
  rehearsals/                # 样片
  render/
    plans/                   # 渲染计划（可复用/需重算/估算）
    segments/                # 分段视频
    post/                    # 后处理产物
  artifacts/                 # 产物清单（不可变版本路径）
  jobs/                      # 任务快照与事件
  logs/
  output/
  tmp/                       # 临时输出，校验后才纳入产物清单
```

### 6.2 不可变版本路径与引用

- **正式产物使用不可变版本路径**：不覆盖同名 `final.mp4` 后让历史仍指向它
- 工程存**相对路径**；配置存机器级凭据引用（**打包工程不包含 API 密钥**，包含所选产物与依赖）
- 图片产物：下载成功且可解码后才是工程资产；远程 URL 只作来源记录
- 角色：工程复制其**已选版本**；资产库更新不追溯改变历史工程

### 6.3 持久化与原子写

| 规则 | 说明 |
|---|---|
| 单一写入入口 | 所有元数据由后端单一写入入口维护 |
| 原子替换 | 临时文件 + 原子替换 + 可恢复事件记录 |
| 自动保存 | 改动后 2s 防抖 |
| 并发保护 | PATCH 携带 `expectedRevision`，旧页面不能覆盖新状态 |
| 清理 | 按引用关系回收；不删除进行中任务、当前成片、保留版本引用的文件；**清理前展示可释放空间** |

---

## 7. 任务调度、恢复与 Windows 运行

### 7.1 Job 状态机（权威）

```
queued → running → succeeded
          │  ├→ pause_requested → paused
          │  └→ cancel_requested → cancelled
          ├→ waiting_confirmation（确认点暂停）
          ├→ recovering（启动恢复中）
          └→ failed
unknown：提交后超时且不知提供方是否已处理（结果待核实）
```

- **正在取消和已取消分开**；前端断线不修改后端执行状态
- 任务提交前记录**尝试意图**，提交后立即记录提供方 ID；两者之间崩溃留下不确定窗口——**本地哈希不能保证外部 API 恰好执行一次**，必须有 `unknown` 处理

### 7.2 启动恢复顺序

1. 扫描未结束任务与执行尝试记录
2. 检查已落盘输出是否完整及对应输入快照
3. 有 ComfyUI `prompt_id` 的任务查询队列 / 历史；还在运行则继续跟踪，已完成则恢复取结果
4. 有远程任务 ID 的 API 任务使用提供方查询能力；**不支持查询且结果不明的标记 `unknown`**
5. 确认未提交或确定失败后才重新派发；**不把旧 running 一律变成 queued**

### 7.3 ComfyUI 任务归属

- 首选**供工作台管理的专用实例**；接用户已有实例时，检查当前 prompt 是否属于本应用——不能因取消一期节目中断其他工作流
- 没有可验证的定向取消能力时：停止后续派发并等待当前任务结束，显示「**取消待生效**」
- 进度分五阶段：准备 / 推理 / 下载落盘 / 校验 / 后处理；节点进度映射为阶段进度，**不当作精确剩余时间**；实际 ETA 用同规格历史测量

### 7.4 本地启动与存储

- 交付**一个启动入口**：检查配置和服务 → 启动必要本地进程 → 打开工作台；前端构建产物由本地服务提供，**日常使用不要求手动运行 Vite**
- Python 环境**编排与推理隔离**，固定验证过的依赖组合
- 启动自检实际驱动、GPU、可用显存 / 内存、模型和磁盘；**不把旧文档驱动版本当作当前机器事实**
- 路径用结构化参数传给子进程，**支持中文和空格**
- 临时输出完成校验后再纳入产物清单

---

## 8. 接口契约

> 业务接口清单见 01 §12（唯一清单）。此处为实现约定。

### 8.1 通用约定

| 约定 | 说明 |
|---|---|
| Base URL | `http://127.0.0.1:8000/api`（仅监听 127.0.0.1，无鉴权） |
| 并发保护 | PATCH 携带 `expectedRevision`；版本冲突返回 409 |
| 幂等 | 提交类接口带 `clientToken`；相同请求连续点击只创建一项任务 |
| 长任务 | 一律返回 Job；进度走 `/api/events`（SSE/WS，**事件带序号**） |
| 重连 | 从服务端读取完整状态（`GET /api/jobs/{id}`）；事件可补发或读快照 |
| 渲染计划 | `renders/plan` 后输入变化 → 提交旧计划必须显式选择「用旧版本」或「重新规划」 |

### 8.2 错误码

| code | 含义 | 前端表现 |
|---|---|---|
| `PROVIDER_DOWN` | API 服务未连接 / 未配置 | 只禁用依赖操作 + 去服务设置 |
| `PROVIDER_AUTH` | 余额 / 权限错误 | 直接停止，显示处理入口，**不自动重试** |
| `PROVIDER_UNKNOWN` | 提交后结果不明 | 标「结果待核实」，提供查询入口 |
| `ENGINE_DOWN` | 本地引擎未连接 | 禁用本地推理按钮 |
| `CAPABILITY_MISSING` | 提供方不具备该能力（如多参考图） | 提供替代路径（上传） |
| `MODEL_MISSING` | 模型文件缺失 | 提示缺失文件与期望路径 |
| `VRAM_INSUFFICIENT` | 显存不足 | 提示降配方案 |
| `INVALID_PARAM` | 参数越界 | 定位到具体字段 |
| `ASSET_MISSING` | 依赖素材缺失 | 高亮对应上游 |
| `ALIGN_LOW_QUALITY` | 对齐质量低 | 列入人工处理列表 |
| `JOB_CANCELLED` | 任务被取消 | 静默 |
| `RENDER_FAILED` | 渲染失败 | 显示错误 + 原位重试 |

---

## 9. 性能预算与容量规划

> **原则**：不把未实测的分钟数写成承诺。工作台显示依据**本机同规格样本**计算的区间；没有测速数据时显示「尚未测定」。

### 9.1 耗时模型

```
总耗时 ≈ 文本API耗时 + TTS耗时 + 图片API耗时 + 视频推理耗时 + 后处理耗时

视频倍率 R = 实际视频推理墙钟时间 / 实际生成的视频秒数
  R=10 → 300 秒纯推理量 ≈ 50 分钟
  R=20 → 300 秒纯推理量 ≈ 100 分钟
  （均未含额外重叠、模型加载与其他步骤；这是计算示例，不是 RTX 5080 测速结果）
```

### 9.2 性能记录要求（同机分别记录）

冷启动与热启动、TTS 耗时、视频生成秒数与实际墙钟、模型加载、重叠额外生成、峰值显存 / 内存、后处理、失败次数、磁盘占用。

### 9.3 容量

| 项 | 估算 |
|---|---|
| 单个 3 分钟工程磁盘 | 音频 ~50 MB + 图片 ~100 MB + 分段视频 ~1.5 GB + 成片 ~200 MB ≈ **2 GB** |
| 权重（本地 TTS + 口型 + 对齐） | ~40–60 GB（按验证包实际清单） |
| 磁盘预留 | ≥150 GB；清理前展示可释放空间 |

---

## 10. 风险清单与应对

### 10.1 高优先级风险（阶段 A 必须验证）

| # | 风险 | 影响 | 应对 | 验证时机 |
|---|---|---|---|---|
| **R1** | 现有 ComfyUI 双人节点不满足「已对齐双轨」输入（add 实为串接） | 视频侧接口不成立 | 无 GPU 契约检查 → 选工作流或新增适配节点；不重排用户台词迁就串接 | **阶段 A** |
| **R2** | 16 GB 显存跑 480p 双人口型过慢或跑不动 | 耗时不可接受 | 实测 R；准备量化 / 降帧 / 降分辨率；评估替代形式 | **阶段 A** |
| R3 | 分段边界姿态跳变 / 双脸 | 成片不可用 | 三区间 + 连续性策略对比；锚点限制续接依赖 | 阶段 A |
| R4 | 长片颜色漂移（官方已知） | 后半段偏色 | 分段 + 锚点重置底图；接近 5 分钟完整片验证 | 阶段 B |
| R5 | 图片 API 多参考图能力不达预期 | 同框底图生成不可用 | 走「上传已完成同框图」替代；能力分开登记 | 阶段 C |

### 10.2 中优先级风险

| # | 风险 | 应对 |
|---|---|---|
| R6 | 提供方时间戳不可靠 | 时间戳视为候选标注；与 spokenText 匹配校验；回退本地对齐 |
| R7 | 替代形式不一定更快 | 同机测量后才对比；不自动降级 |
| R8 | API 任务结果不确定窗口 | unknown 状态 + 查询能力 + 不盲目重试 |
| R9 | TTS 与视频依赖冲突 | 本地 TTS 隔离进程方案（接口不变） |
| R10 | 工程文件损坏 | 单一写入入口 + 原子替换 + 事件记录 |
| R11 | 显存被其他程序占用 | 渲染前检查；从 TTS 切视频检查模型卸载 |

### 10.3 产品风险

| # | 风险 | 应对 |
|---|---|---|
| R12 | 用户期望「点一下就出片」 | 三确认点默认明确；模板 + 自动模式；失败可局部重跑 |
| R13 | 跨引擎声音不一致被当作 bug | 明示「同一角色的不同引擎绑定不保证声音相同」 |
| R14 | 耗时焦虑 | 开始前摘要给预估区间与复用段数；实测 ETA |

---

## 11. 开发路线图（交付关口制）

> 旧九周计划不作为当前承诺；排期在阶段 A 后按实测与可用节点重新估算。**阶段 A 未通过前不先做完全部页面。**

| 阶段 | 交付物 | 进入下一阶段的条件 |
|---|---|---|
| **A · 工作流验证** | 双轨适配检查（无 GPU 契约测试）、短样片、30–60 秒跨段样片、硬件实测记录 | 正确驱动双方；分段不丢时间；质量和耗时可评估 |
| **B · 最小生产闭环** | 已有脚本 → 本地 / MiniMax 音频 → 上传底图 → 视频 → 导出 | 至少一条接近 5 分钟的完整片通过验收；**失败恢复有效** |
| **C · 内容与图片 API** | 提纲与对话、局部改写、角色与场景 API、资产复用 | 两类 API 失败不丢工作；同框图可生成或可替换上传 |
| **D · 五阶段编辑工作台** | 全流程页面、时间轨、镜头、版本与影响提示 | **局部修改不会无意义重跑整个工程** |
| **E · 自动执行与交付** | 模板、确认点、后台恢复、启动器、字幕和竖屏版式 | 用同一模板连续制作多期，操作与产物一致 |

- 任务快照、产物哈希与持久化**从阶段 B 开始实现**，不拖到自动化阶段
- 五分钟测试不能只把短片复制多次：覆盖真实多话轮与**至少一次长片中途恢复**
- 首次工作流验证的覆盖面：数字、英文缩写、短追问、长发言、A→B→A、B 开场、停顿、片尾；视频侧统一先使用一套质量合格的音频

---

## 12. 验收标准

### 12.1 功能验收用例

| 验收项 | 最低通过标准 |
|---|---|
| 双轨时序 | A→B→A 与 B 开场均正确；视频条件不二次拼接或翻倍 |
| 单方发言与静音 | 静音区间不报错；另一角色无明显持续错误开口 |
| 发言参数 | 单独修改语速、情绪、停顿有可听见且范围准确的结果 |
| 声音来源切换 | 本地和 MiniMax 结果进入同一时间轨和视频管线，已有资产保留 |
| 对齐 | 所有句子覆盖有效音频区间；低质量部分可定位、试听、人工修正 |
| 五分钟限制 | 实际总音轨及附加时长超上限时可精简，无截断尾句 |
| 边界 | 无重复或丢失台词；拼接处无明显双脸、跳姿、音画累积偏移 |
| 音画时间 | 整片视频与母轨长度误差 < 一个输出帧；局部口型误差人工检查 |
| 局部重跑 | 改裁切不触发 TTS / 基础视频；改一句仅重合成相关单元，并正确传播续接依赖 |
| 中断恢复 | 关闭页面、重启编排、单段失败、输出下载失败都可恢复且不丢有效结果 |
| 版本隔离 | 旧任务完成不覆盖新草稿；旧成片可独立播放和导出 |
| 路径与磁盘 | 中文 / 空格路径可运行；磁盘不足在开始前或落盘时明确报告 |
| 横竖屏 | 横屏成片无拉伸；竖屏版式不误切另一位角色、不遮挡关键字幕 |
| 字幕 | 不显示情绪 / 读音控制标签；时间戳合法；开关字幕只影响对应后处理 |

### 12.2 质量验收

- 口型与身份质量**先使用人工检查**，不凭单一自动评分宣布通过
- 接近五分钟的测试需**完整观看**，不能只看开头和结尾
- 样片验收项：错误开口、口型同步、角色一致、边界连续、声音与清晰度

### 12.3 工程验收

| 编号 | 验收项 | 标准 |
|---|---|---|
| E1 | 数据边界 | 工程与成片本地；文本 / 图片 / MiniMax 请求仅发往服务设置中配置的地址 |
| E2 | 可观测 | 每个任务有阶段、版本、耗时、失败信息与日志 |
| E3 | 可恢复 | 按 7.2 恢复顺序验证 |
| E4 | 费用可控 | 输入哈希复用；下载失败优先恢复获取；unknown 不盲目重试 |
| E5 | 可追溯 | 产物带文件哈希、依赖哈希、参数与工作流版本 |

---

## 13. 运维与故障排查

### 13.1 启动

```text
单一启动入口（launcher）：
  1. 检查配置与服务（文本 / 图片 API、本地引擎、MiniMax）
  2. 启动必要本地进程（编排服务，按需 ComfyUI / 隔离 TTS）
  3. 打开工作台（前端构建产物由本地服务提供）
退出选项：「当前段完成后退出」「立即取消并退出」（说明已完成内容已保存）
```

### 13.2 常见故障

| 现象 | 原因 | 排查 |
|---|---|---|
| 服务状态组某项红色 | 对应 API 未配置 / 断连 / 凭据失效 | 服务设置 → 测试连接；只影响依赖操作 |
| 双人视频口型串位 | 双轨适配层未验证 / 人物区域错位 | 检查活动掩码与适配节点；核对人物区域坐标三类变换 |
| 不说话的人在动 | 零幅度音频 ≠ 全零特征 | 适配器验证特征清零；活动掩码建立 |
| 拼接处跳变 / 双脸 | 连续性策略选择不当 | 检查三区间配置；锚点；样片检查边界 |
| 时间码不准 | 提供方时间戳不可靠 / 对齐失败 | 查看 timingSource；回退本地对齐；人工标注 |
| 时间码改了没效果 | 手工修改只更新标注 | 预期行为；标注与音频分离 |
| 某段渲染失败后续停住 | 默认暂停策略 | 任务中心原位重试 |
| 音色切换后部分台词没变 | 候选未采用 / B 复用 | 检查绑定采用状态；重跑矩阵 |
| API 结果待核实 | 超时不确定窗口 | 任务中心查询；评估是否重复计费 |
| 磁盘不足 | 工程累积 | 清理前查看可释放空间；不可变版本按引用回收 |

### 13.3 日志规范

每行一条 JSON，任务事件带 `jobId`、阶段、版本：

```json
{"ts":"2026-09-10T14:30:22.123Z","jobId":"j_001","stage":"infer","event":"tts.unit.start","unitId":"U12","provider":"minimax","rev":"R9"}
{"ts":"...","jobId":"j_001","stage":"infer","event":"tts.unit.done","unitId":"U12","sampleCount":595200,"timingSource":"provider"}
{"ts":"...","jobId":"j_002","stage":"infer","event":"segment.start","segId":"#04","outputRange":[48.0,68.0],"frames":500}
{"ts":"...","jobId":"j_002","stage":"infer","event":"segment.failed","segId":"#04","error":"CUDA out of memory","retryable":true}
```

---

## 附录 A：环境与依赖清单

- [ ] Python 3.11（编排环境与推理环境隔离）
- [ ] PyTorch 2.7+ / cu128+（若目标机为 Blackwell sm_120；以启动自检实测为准）
- [ ] Node 20 LTS（构建期）；ffmpeg 6.0+（PATH）
- [ ] ComfyUI 专用实例 + 验证过的对口型工作流（含验证包记录）
- [ ] 本地 TTS 可用工作流或隔离进程（候选 IndexTTS-2.5）
- [ ] 文本 API：配置地址 / 模型 / 凭据引用，测试连通与结构化输出
- [ ] 图片 API：配置并**分开测试**文生图 / 单图编辑 / 多参考图编辑能力
- [ ] MiniMax：区域 / 地址 / 模型 / 凭据引用；双方音色绑定与试听
- [ ] 对齐：faster-whisper（或 WhisperX 对齐管线）
- [ ] 阶段 A 验证：双轨契约检查（无 GPU）→ 短样片 → 跨段样片 → 实测记录 R

> ~~Ollama + Qwen3-8B~~、~~本地图像生成模型~~：v3.0 起从依赖清单移除。

## 附录 B：参数与能力速查

| 项 | 权威定义 |
|---|---|
| 合成单元划分 | §5.1 |
| transitionGapMs / leadInMs / tailOutMs | §5.2 |
| 建轨与权威时长 T | §5.3 |
| 对齐顺序与质量字段 | §5.4 |
| 双轨视频输入契约 | §5.5 |
| 三区间与连续性策略 | §5.6 |
| 帧数换算与验收 | §5.7 |
| 后处理顺序 | §5.8 |
| 缓存键与重跑矩阵 | §5.9 |
| Job 状态机与恢复 | §7.1 / §7.2 |
| 业务接口清单 | 01 §12 |
| 页面状态七态与确认点 | 01 §13 / §0.3 |
