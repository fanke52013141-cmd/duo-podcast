# 12 · 第三轮用户审查报告（流程 / 并发队列 / 特写镜头 / ComfyUI 方案）

> 审查基线：`7977042`（2026-09-16 与远程 806f654 合并后）。审查日期：2026-09-18。
> 视角：真实用户走完整流程 + 多流程并发 + 双人特写镜头控制 + 本机（Windows 单机、RTX 5080 16GB）技术路线。
> 本轮修复批次（C/U 编号）已随本报告落地，见 §7 与 [09-优化落地记录.md](09-优化落地记录.md)。

---

## 0. 总体结论

**流程骨架 OK，内容生产链路不 OK。** 五阶段编排、确认点、版本存储、任务状态机在前两轮加固后是健康的；但从「用户做出成片」的终点看，存在三类结构性缺口：

1. **失效传播只有半条链**：脚本改动 → 配音/画面会过期，但**成片（render）永不过期**（已修复 U-3）。
2. **队列有并发无策略**：三队列纯 FIFO、无优先级、UNKNOWN 任务永久滞留、未知队列类别可让启动崩溃（已修复 C-1/C-2/C-4）。
3. **特写与镜头是"孤儿模型"**：`ShotPlan/CameraMove/RenderSegment` 数据类齐备但全仓零消费方，前端镜头区是本地 state 从不提交，样片确认点可以在**从未看到样片**的情况下被勾选通过（最高优先缺口，见 §3）。

---

## 1. 流程与用户体验

### 1.1 端到端走查（用户视角）

| 步骤 | 现在能做 | 卡点（证据） |
|---|---|---|
| 首页 | 新建/重命名/继续制作 | 「导入工程/打开目录/保存模板」为 alert 占位（HomePage.tsx:147,182,198）；删除未接后端（:282） |
| ① 创建 | 生成脚本、改比例 | 生成失败**完全静默**（genJob 收了 failed 但不展示，CreateEpisodePage:53-60）；「取消」假按钮而后端 `/jobs/{id}/cancel` 早已存在（:144）——**本轮已修复** |
| ② 编辑 | 话轮增删/改写/确认 | 读音词典纯组件 state，关页即丢、不进合成（EditScriptPage.tsx:614-648）→「读音校正」为假功能 |
| ③ 配音 | 整期合成/确认 | 试听、连续试听、独听A/B、单元级重生成、插入停顿全部 disabled 占位（VoicePage.tsx:216-218,253,374,435-437）——根因是**后端没有单元级重合成与预览端点** |
| ④ 画面 | 生成底图、勾检查项 | 「生成样片」无 onClick、后端无 `POST /rehearsals`（VisualPage.tsx:343 / 04:598）；`canAccept` 只看手动勾满 5 项 → **从未见过样片也能确认样片**，第三确认点形同虚设 |
| ⑤ 导出 | 提交整期渲染 | 编码/补帧/烧字幕选项不发后端（RenderPage.tsx:112-122）；`reusedCount` 恒 0；**后端无任何媒体路由**，成片无法播放/下载 |

**结论**：mock 阶段流程可走通（冒烟 ALL PASS），但一旦接入真实提供方，③④⑤的"确认→导出"链路会立即暴露上述断层。当前 UX 最大问题不是样式，而是**假通过**（样片确认）与**静默失败**（生成失败无提示）。

### 1.2 节点修改对下游的影响（失效传播）

现状矩阵（代码实测）：

| 上游改动 | 脚本 | 配音 | 画面 | 成片 |
|---|---|---|---|---|
| 改话轮文本（新草稿） | 编辑 | ✅ STALE（timeline.revision≠draft） | ✅ STALE（间接经 timeline） | ❌ **恒「已就绪」**（stage_svc.py:38）→ **已修复 U-3：解析 `OUT-{rev}-vN` 内嵌版本比对草稿** |
| 改停顿/gap | — | 必须整期重合成（voice_svc.py:64-80 每次重建全部单元，无文本哈希缓存）→ 违反 04:526 重跑矩阵「改显式间隔不全重做」 | — | — |
| 改单一话轮 | 无单元级端点，粒度=整期 | — | — | — |
| 换分辨率/工作流 | — | — | — | Approval 不绑规格（`spec:{}`，api.ts:226），换规格不失效 |
| 撤销确认 | — | `latest` 过滤把撤销确认变 pending_confirm，Alert 说「过期」左轨说「待确认」，两套口径 | — | — |

**建议（按投入产出排序）**：
1. （已做）render 过期传播。
2. **单元级缓存键**：`hash(text, voiceId, emotion, speed)` 命中则复用已合成样本——这是兑现 04 §12.3 重跑矩阵的前提，也是接真实 TTS 前的最后一块地基。
3. **下游待重做清单**：确认点被失效时，在阶段头集中列出「配音需重合成 / 画面边界需重截 / 成片需重渲」，而不是各页各报各的。
4. Approval.spec 绑定渲染规格，规格变更自动过期。

## 2. 并发与排队机制

### 2.1 现状（修复前）

- 三队列 GPU(1)/API(16)/CPU(4)，cap 实为 worker 协程数；CPU 队列零生产者；上限无法环境变量修改。
- 纯 FIFO、无优先级；跨工程任务一视同仁排队。
- 同工程多任务并行无互斥，靠 revision 乐观锁兜底——冲突表现为**任务失败**而非等待重试。
- 未知 queue_class 磁盘数据 → `restore` KeyError → **启动崩溃**（前向兼容缺陷）。
- 崩溃恢复标 UNKNOWN 后**永久滞留**，无任何出路；无重试机制；running 不可中断（协作式，runner 返回后才记账）。

### 2.2 本轮已落地的排队方案（C-1/C-2/C-3/C-4/C-5/C-6）

1. **队列结构**：每类一个 `(priority, created_at, 入队序)` 最小堆。语义：优先级小者先；同优先级保持 FIFO（多并发同队列内仍并行至 worker 上限）。
2. **默认优先级**：`renders=10`，其余（脚本/改写/配音/画面）`=0`。效果：**用户点下"渲染导出"后，任何交互式轻任务（重合成一段配音、生成一张底图）都会插到整期渲染前面**，GPU 队列只有一个 worker 时这一点尤其关键。后续接样片任务（`rehearsal`）给 0 或更高优先级即可。
3. **排队可见性**：`GET /api/jobs` 每项返回 `queuePosition`（队内第几位），任务中心排队卡显示「第 N 位（按优先级与提交顺序）」。
4. **兼容兜底**：`_resolve_class()` 未知类别回落 cpu 并记日志——旧数据、未来新增类别都不会炸启动。
5. **UNKNOWN 出路**：新增 `POST /api/jobs/{id}/abandon`（UNKNOWN→FAILED，幂等），任务中心「无法恢复 · 放弃记账」按钮。
6. **配置兼容**：`DUOCAST_QUEUE_GPU/API/CPU`、`DUOCAST_PROJECT_DEBOUNCE_MS` 环境变量可调，5080 实机调参不必改代码。
7. **事件兼容**：`EventBus.resume_from_log()` 启动时续接序号，修复「重启后序号归零 → 重连补发被误判连续 → 静默事件缺口」。

### 2.3 仍建议下一步做的（未在本轮）

- **同工程写回串行化**：给 runner 的"写回工程"段加 per-project 异步锁，或冲突时自动重读+重放 patch（把"冲突=失败"变成"冲突=等待"）。这是多工程并发真正上量后的第一优先。
- **失败重试**：`attempt` 字段已备好；建议 API 类任务指数退避重试 2 次，GPU 任务只手动重试（成本敏感）。
- **定向取消**：ComfyUI 接入后可用 `/interrupt_prompt` 实现真中断；文本/图片 API 用 httpx 取消。当前协作式取消保留。
- 队列无上限、无背压（submit 恒成功）——单用户本机场景可接受，不建议加拒绝路径。

## 3. 视频特写与镜头控制（双人播客核心问题）

### 3.1 现状一句话

数据模型（`Rect/Transform/CameraMove/VisualVariant.person_regions/ShotPlan/RenderSegment`）与 HF-04 三区 UI 骨架都在，**但零数据流、零执行**：Project 没有 `shot_plans` 字段、`visual_svc` 建的 A/B 区域都是全画幅重叠框、前端镜头选择从不提交、全仓没有任何 ffmpeg/crop/zoom 代码。落地程度约 5%。

### 3.2 建议：两层「底片 + 镜头层」架构（怎么截、怎么动）

**核心原则（04:429 已写明，要在代码里兑现）：分段 ≠ 镜头。** 音频驱动生成的是**稳定的同框底片**（base clip），特写和运镜全部放在**后处理镜头层**用 ffmpeg 对底片做裁切/推拉——改镜头只重跑后处理（分钟级、零显存），不重跑 InfiniteTalk（小时级、占满 16GB）。

**怎么截（截取区间）**：
1. 镜头层的最小单位是**话轮（turn）或话轮组**，不是秒。`ShotPlan.ranges` 用整数样本区间 `[startSample, endSample]`（与时间轨同一坐标系，杜绝浮点漂移），从 `turn.unitOffsets` 自动推导默认值——A 发言时给 A 特写、B 发言给 B 特写、双方互动/开场收尾回同框，这就是已有的 `speakerCrop` 预设的语义。
2. 特写的取景框 = `person_regions[谁]` 向外扩一个安全边距（建议高度方向扩 25%、留头顶空间），裁切框保持目标画幅比例（16:9→裁 1920×1080 等效、竖屏→9:16）。**永远不要在生成模型输出上直接二次插值放大**：480p 底片应先在裁切后的小区域做超分（SeedVR2/RealESRGAN），再上采样到输出分辨率，画质优于全幅放大。
3. 边界连续性：裁切段与相邻同框段之间加 6-10 帧交叉溶解（时间轴上取样本区间边缘），避免"跳框"。这正是 04 §10.4 要求 `context_range` 缓冲的原因——切段渲染时前后多带 2 秒上下文，拼接时裁掉。

**怎么动（运镜）**：
1. 首版只开放三种，全部可用 ffmpeg `zoompan` 表达，**不碰模型**：
   - `push`（缓推）：起框=同框，终框=说话人区域 ×1.15，时长 2-4 秒进入；
   - `pull`（缓拉）：回同框，用于话轮结束/话题转换；
   - `hold`（定格）：特写期间固定框，**微动 0.2%/s 的呼吸感**防呆板（对 5080 输出到 720p 时观感提升明显）。
   `pan/tilt` 建议暂缓：横摇在两个固定机位播客里容易暴露底图合成痕迹。
2. 运镜一律用**关键帧 + 平滑曲线**（ease-in-out），参数落 `CameraMove{kind, strength}`；strength 映射到每帧缩放步长。禁止匀速直线（机械感）与超过 1.3× 的推近（像素崩坏）。
3. 用户可编辑性：HF-04 镜头区 = 预设三选（stable/pushPull/speakerCrop）+ 逐话轮覆盖列表。数据结构就绪后把 `shotOpts` 提交为 `ShotPlan`，加 `Project.shot_plans` 字段与 PATCH 白名单即可——改动成本小，收益是整个特写体系可用。

**渲染管线的落地顺序（衔接 §6 方案）**：
1. 后端 `POST /projects/{id}/shot-plans`（读写 ShotPlan）；
2. 服务端权威分段器（按停顿间隙切段 + 整数帧量化 + 三区间），取代前端自算 `planSegments`（违反 stage_svc.py:1「前端不自算」）；
3. ffmpeg 后处理执行器：`裁切→zoompan→xfade 拼接→字幕→混音`，输出 `RenderSegment.effective_output_version`；
4. 缓存键：`hash(底片段) / hash(镜头层) / hash(封装)` 三层分离，兑现 04 §12.2「改数字推拉只重跑镜头处理」。

### 3.3 样片确认点修复建议（未在本轮，P0 级）

`canAccept` 目前只要求勾选 5 项。底线修法：确认记录必须绑定一个**真实存在的样片产物 id**（哪怕 mock 阶段也要求先生成演示样片），无样片产物时禁用接受按钮并提示原因。「自证勾选」的确认点比没有确认点更危险——用户会以为质量已经被验证过。

## 4. 欠缺项总表（UX + 功能，按优先级）

| 级 | 项 | 状态 |
|---|---|---|
| P0 | 样片可"未见到即确认"（假通过） | 待做（§3.3） |
| P0 | 全链路无媒体播放/下载路由（接真实提供方后立刻挡住验收） | 待做：artifacts 目录挂 StaticFiles + Range 支持 |
| P0 | render 永不过期 | **已修复 U-3** |
| P0 | 未知 queue_class 启动崩溃；UNKNOWN 无出路 | **已修复 C-2/C-4** |
| P1 | 脚本生成失败静默、取消假按钮 | **已修复 U-1** |
| P1 | 排队无优先级、无位次显示 | **已修复 C-1** |
| P1 | 事件序号重启归零 → 补发误判 | **已修复 C-5** |
| P1 | 单元级重合成/停顿编辑端点缺失（③页 6 个灰按钮的根因） | 待做 |
| P1 | ShotPlan 无持久化路径、镜头区不提交、无服务端分段器 | 待做（§3.2 顺序 1-2） |
| P1 | 同工程并发写回冲突=任务失败 | 待做（§2.3） |
| P2 | 读音词典不持久化、不入合成 | 待做 |
| P2 | Approval 不绑规格；撤销确认口径不一（stale vs pending） | 待做 |
| P2 | 渲染规格选项不入库 | 待做 |
| P2 | 七态死码：前端 `failed` 徽章后端永不产出 | 随阶段推导完善一并清 |
| P2 | 失败任务卡无出路（重试/日志/回阶段） | abandon 已给 unknown 出路；failed 重试待 §2.3 |

## 5. 项目限定条件核对（本地 Windows 单用户）

- 架构选型全部成立：FastAPI + 本地文件存储 + 单进程 asyncio + 无鉴权 + 无多租户。本轮修复全部按「无云端、无账号」前提做（如放弃队列拒绝/背压，保留尽力而为的事件日志）。
- 需要注意的本地化细节：路径校验已防 Windows 设备保留名；`events.jsonl` 轮转、防抖落盘参数均已环境变量化，方便本机调参；HTTP 代理拦截 localhost 的坑已记录在 HANDOFF（接真实 API 适配器时注意 `NO_PROXY` 同样会影响回调地址）。

## 6. 本地 ComfyUI 工作流与 5080（16GB）技术方案

### 6.1 方案盘点结论（2026-09 现状，来源见链接）

| 方案 | 定位 | 双人同框 | 16GB 可行性 | ComfyUI 成熟度 | License |
|---|---|---|---|---|---|
| [InfiniteTalk](https://github.com/MeiGen-AI/InfiniteTalk) | 音频驱动整段生成（口型+头+身+表情） | ✅ single/multi 双权重 | 480p 单卡 OK（fp8/GGUF+block-swap） | **主分支原生 `WanInfiniteTalkToVideo` 节点** | Apache-2.0 |
| [MultiTalk](https://github.com/MeiGen-AI/MultiTalk) | 多人对话生成 | ✅ 一张同框图+双人音频 | 480p 单卡；720p 官方要多卡 | WanVideoWrapper multitalk 分支 | Apache-2.0 |
| [LatentSync 1.6](https://github.com/bytedance/LatentSync) | 仅改口型（crop 贴回） | ❌ 单脸两轮 | 1.6 要 18GB（**超**）；1.5 约 8GB/256² | 社区 wrapper | Apache-2.0 |
| [MuseTalk](https://github.com/TMElyralab/MuseTalk) | 下半脸重绘 256² | ❌ 一轨驱动所有脸易串味 | 极低显存、近实时 | 第三方（偏旧） | 代码 MIT |
| [LongCat-Video-Avatar 1.5](https://meigen-ai.github.io/LongCat-Video-Avatar-1.5-Page/) | 统一模型，原生多流音频、8 步蒸馏、INT8 | ✅ | INT8 友好 | 社区节点（较新） | **MIT** |
| HeyGem / SkyReels-V3 / EchoMimic / Hallo2 | 单人口播 / 单人肖像 | ❌ | — | — | 各有商用条款限制 |

### 6.2 推荐管线（TOP1）

**同框底图（Qwen-Image-Edit-2511，走你们已有的图片 API 适配器或本地图）→ ComfyUI 主分支 + `WanInfiniteTalkToVideo` two_speakers（InfiniteTalk multi 权重 fp8/GGUF）→ lightx2v 8 步蒸馏 + TeaCache → `previous_frames` 分段续帧 → ffmpeg 镜头层（§3.2）做特写/运镜 → SeedVR2 超分到 720p → RIFE 补帧 + loudnorm 混音。**

要点与理由：
1. **唯一同时满足**「真同框双人双轨 + 无限长分段 + Apache-2.0 可商用 + 16GB 单卡可跑 + 官方核心节点」的组合。
2. **双轨契约已被源码核实**：`generate_infinitetalk.py:291-309` 中 `audio_type:"para"` 是两路等长各驱动各人，`"add"` 是串行拼接（总长翻倍）——与 02:331 的判断一致；ComfyUI 主分支节点硬编码了 `"add"`，但同函数 `para` 分支已实现，**本地改一行即可对齐**，属可控的最小侵入 patch。这正好可以封装进我们 `adapters/base.py` 的 VideoProvider 契约：提交时固定 para + 两路等长 48k 音轨 + `bbox`（MultiTalk 路径 `wan/multitalk.py:586-616` 证实给 bbox 即按框绑定人物——**这就是 §3 的 person_regions 该去的地方**）。
3. **显存预算（16GB，480p×81 帧/段）**：DiT fp8/GGUF Q4-Q5 权重 9-13GB + block swap/权重卸载；文本编码器/VAE 用完即卸。锚点数据：5080 在 WanVideoWrapper 640²/5s/block-swap 实测约 1300s（[issue#1185](https://github.com/kijai/ComfyUI-WanVideoWrapper/issues/1185)）→ **5 分钟成片 480p 蒸馏档约 1.5-3 小时 GPU**；720p 直出 3-6 小时且易 OOM，所以「480p 生成 + 裁切区域超分」是正确路线（也再次印证底片/镜头两层分离的价值——特写区域小，超分便宜）。
4. **5080（Blackwell, sm_120）环境**：InfiniteTalk 官方 pin 的 torch 2.4.1+cu121/flash_attn **不支持 sm_120**，必须走 ComfyUI（或 [Wan2GP](https://github.com/deepbeepmeep/Wan2GP)）+ Python 3.11 + torch 2.10/cu130（或保守 2.7.1+cu128）；避开 torch 2.8.0（内存泄漏）与 2.9.0（Conv3D VAE 显存暴涨）；SageAttention 需 Blackwell 构建版；给浏览器关硬件加速可回收 1-5GB 显存。
5. **第二路线并行验证**：LongCat-1.5（MIT、原生等长双轨、8 步蒸馏，速度与许可更优，节点较新欠稳定）。Wan2GP 的"滑窗长对白 + 队列 + 量化卸载"工程做法值得直接借鉴到我们 GPU 队列 runner 的实现。
6. **不推荐**：「分别生成两个单人再抠像同框」——缺视线/反应同步、光照不一致，只适用于切镜头/分屏版式，不适合真同框对谈。

### 6.3 与代码的衔接

- `MockVideoProvider` 替换点：`adapters/base.py` VideoProvider Protocol（提交任务/查询/取回三段式），`main.py:136` 装载处；能力快照须同步反映 ComfyUI 在线状态与显存自检（02 §13.4 的 TopBar「显存：尚未测定」应改读 `nvidia-smi --query-gpu=memory.used --format=csv`）。
- GPU 队列 `cap=1` + 本轮优先级堆，正好匹配「整期渲染排后、样片/试听插队」的实际需求；ComfyUI 子进程由后端 managed（端口 8188，崩溃自拉起），`previous_frames` 缓存键放 `artifacts.py` 哈希复用体系里。
- **关口 A 硬件实测清单**（接入前）：① para 双轨 patch 后各段口型归属正确性；② 480p/81 帧段在 16GB 的峰值显存与耗时；③ 跨段 `previous_frames` 续帧的边界连续性（人眼三点检查）；④ zoompan 后处理 5 分钟片段的 CPU 耗时。

## 7. 本轮修复清单（已落地 + 验证）

**后端**
- C-1 排队策略：`Job.priority` + 类内最小堆 + `KIND_PRIORITY(renders=10)` + `queuePosition` API 字段（manager.py / domain/job.py / api/jobs.py）
- C-2 兼容兜底：`_resolve_class()` 未知 queue_class 回落 cpu（submit/restore/resume 三路）
- C-3 配置兼容：`DUOCAST_QUEUE_GPU/API/CPU`、`DUOCAST_PROJECT_DEBOUNCE_MS` 环境变量（core/config.py）
- C-4 UNKNOWN 出路：`manager.abandon()` + `POST /api/jobs/{id}/abandon`（幂等，仅 UNKNOWN 生效）
- C-5 事件兼容：`EventBus.resume_from_log()` 序号跨重启续接，main.py 启动装配接线
- C-6 落盘一致：recovery 原子写统一为唯一临时名 + replace（与 manager._persist 同风格）
- U-3 失效传播：stage_svc 解析 `OUT-{rev}-vN`，草稿变更后 render 阶段 → STALE
- manager 队列实现从 `asyncio.Queue` 换为 `_PriorityQueue`（去掉 task_done 耦合，ghost 副本语义不变）

**前端**
- U-1 创建页：脚本生成失败显示错误 + 「查看任务中心」+「重试生成」；「取消」接真实 `useJobCancel`
- U-2 任务中心：排队卡显示队列位次；unknown 卡新增「无法恢复 · 放弃记账」按钮；types 增加 `priority/queuePosition`

**验证**（2026-09-18）
- 后端 `pytest tests -q` → **424 passed, 7 skipped**（新增 `test_round3_fixes.py` 13 项：优先级弹出序、位次、脏数据兜底、abandon 幂等、序号续接、垃圾尾行容错、recovery 无 tmp 残留、render 过期三分支、env 覆盖）
- 场景验证 `tests/scenario_checks.py` → 全 PASS
- 前端 `pnpm build`（tsc + vite）→ 零错误，dist 305.67 kB / gzip 93.54 kB
- 端到端冒烟 `smoke_e2e.py`（8100 实测）→ 退出码 0；`/api/jobs` 响应确认含 `priority/queuePosition`；`abandon` 对终态任务幂等返回；无脏数据残留。旧落盘任务 priority=0 为兼容缺省值。
- 环境注记：`.venv` 需补装 `setuptools`（`test_project_safety.py` 依赖，本机原缺失）。

## 8. 已知边界（不变）

四个内容提供方仍是 mock：本轮修复不改「不能产出真实成片」的现状，也不冒充真实数据。真实提供方接入按 §6.3 顺序，第一步是文本 API（最低风险），关口 A 硬件实测通过后才实现视频页完整版。
