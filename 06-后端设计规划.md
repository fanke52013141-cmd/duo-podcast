# 双声播客工坊 DuoCast · 后端设计规划

> 版本 v1.0 ｜ 2026-09-10 ｜ 依据：《详细优化方案》（04）、《项目实施方案文档》（02 §3–§8）、《页面详细设计文档》（01 §11–§13）
>
> **本文档的定位**：把 02 的架构分层、关键算法与接口契约组织成**可开工的模块设计**——仓库结构、模块职责、API 面、事件流、任务系统、存储层与适配器接口。**不重新定义任何算法或契约**：算法权威在 02 §5，接口清单权威在 01 §12.1，状态机权威在 02 §7.1；本文所有相关内容均为引用 + 代码落位。

---

## 目录

- [1. 设计目标与边界](#1-设计目标与边界)
- [2. 仓库结构](#2-仓库结构)
- [3. 模块职责与依赖规则](#3-模块职责与依赖规则)
- [4. API 层设计](#4-api-层设计)
- [5. 事件总线设计](#5-事件总线设计)
- [6. 任务系统设计](#6-任务系统设计)
- [7. 存储层设计](#7-存储层设计)
- [8. 适配器设计](#8-适配器设计)
- [9. 领域模型与派生规则落位](#9-领域模型与派生规则落位)
- [10. 前后端协作流程](#10-前后后端协作流程)
- [11. 测试与验证策略](#11-测试与验证策略)
- [12. 实施顺序（对齐 A–E 交付关口）](#12-实施顺序对齐-ae-交付关口)

---

## 1. 设计目标与边界

### 1.1 目标

- 一名后端开发者据此结构可以直接开工：知道每个文件放什么、依赖谁、被谁调用。
- 模块边界与 02 的分层严格对应：**编排层管业务规则，适配层管提供方差异，ComfyUI 只承载本地推理原子任务**。
- 恢复、版本、能力门控这些「正确性关键路径」在结构上有明确归属，不被散落在路由里。

### 1.2 权威引用关系

| 主题 | 权威 | 本文角色 |
|---|---|---|
| 关键算法（单元划分 / 建轨 / 三区间 / 重跑矩阵 / 后处理顺序） | 02 §5 | 只做 `algorithms/` 模块落位 |
| 业务接口清单 | 01 §12.1 | 只做路由 → 模块映射 |
| 错误码与通用约定 | 02 §8 | 引用，不复制语义 |
| Job 状态机与恢复顺序 | 02 §7.1 / §7.2 | 定义实现结构 |
| 数据模型（TS 视角） | 01 §11 | 定义 pydantic 落位与命名对齐 |
| 工程目录规范 | 02 §6.1 | `storage/` 的实现依据 |

### 1.3 明确非目标

- ❌ 不引入数据库、消息队列、Redis、Celery（02 §2 选型：工程文件夹 + JSON + 进程内调度）
- ❌ 不做多用户 / 鉴权（仅监听 127.0.0.1，02 §8.1）
- ❌ 不在本文重写算法伪代码或接口字段表

---

## 2. 仓库结构

```
duocast/
├─ apps/
│  ├─ server/                          # FastAPI 编排服务（本档范围）
│  │  ├─ duocast/
│  │  │  ├─ main.py                    # 入口：装配路由、事件总线、调度器；启动自检（02 §7.4）
│  │  │  ├─ api/                       # 路由层：薄，只做参数校验与编排调用
│  │  │  │  ├─ providers.py            #   /api/providers/*（capabilities / test）
│  │  │  │  ├─ projects.py             #   /api/projects/{id}（读取 / PATCH 保存）
│  │  │  │  ├─ script.py               #   script/generate · script/rewrite
│  │  │  │  ├─ audio.py                #   tts/preview · tts/synthesize · audio/assemble
│  │  │  ├─ visual.py                  #   visual/generate · rehearsals
│  │  │  │  ├─ renders.py              #   renders/plan · renders
│  │  │  │  ├─ approvals.py            #   /api/approvals
│  │  │  │  ├─ jobs.py                 #   /api/jobs/*（含 pause / cancel）
│  │  │  │  └─ events.py               #   /api/events（SSE/WS）
│  │  │  ├─ core/
│  │  │  │  ├─ config.py               #   端口、工程根目录、队列上限、防抖间隔
│  │  │  │  ├─ eventbus.py             #   带序号事件日志（§5）
│  │  │  │  └─ logging.py              #   02 §13.3 日志规范
│  │  │  ├─ domain/                    # pydantic 模型（与 01 §11 TS 定义一一对应）
│  │  │  │  ├─ project.py              #   Project / ScriptRevision / Turn / Line
│  │  │  │  ├─ audio.py                #   VoiceBinding / SynthesisUnit / AudioTimeline
│  │  │  │  ├─ visual.py               #   VisualVariant / ShotPlan
│  │  │  │  ├─ render.py               #   RenderSegment
│  │  │  │  ├─ job.py                  #   Job / JobStatus
│  │  │  │  └─ approval.py             #   Approval
│  │  │  ├─ services/                  # 业务编排（调用 algorithms + adapters + jobs）
│  │  │  │  ├─ script_svc.py           #   两次生成、选区改写、来源锚点核对
│  │  │  │  ├─ audio_svc.py            #   单元划分 → TTS → 对齐 → 建轨
│  │  │  │  ├─ visual_svc.py           #   底图生成/上传、人物区域、样片区间
│  │  │  │  ├─ render_svc.py           #   渲染计划、分段提交、后处理编排
│  │  │  │  ├─ export_svc.py           #   MP4 / WAV / SRT 导出
│  │  │  │  └─ stage_svc.py            #   七态推导、过期标记（01 §13.1）
│  │  │  ├─ algorithms/                # 02 §5 的唯一实现处（纯函数，无 IO）
│  │  │  │  ├─ unit_split.py           #   §5.1 三层划分 + §5.2 停顿归属
│  │  │  │  ├─ timeline.py             #   §5.3 建轨 + §5.4 对齐决策 + §5.7 帧数
│  │  │  │  ├─ segmentation.py         #   §5.5 双轨适配 + §5.6 三区间
│  │  │  │  ├─ postprocess.py          #   §5.8 后处理顺序
│  │  │  │  └─ rerun.py                #   §5.9 重跑矩阵（权威实现）
│  │  │  ├─ adapters/                  # 提供方适配（无业务规则）
│  │  │  │  ├─ base.py                 #   Protocol 定义 + CapabilityRegistry（§8）
│  │  │  │  ├─ text_api.py             #   文本 API
│  │  │  │  ├─ image_api.py            #   图片 API（文生图 / 单图 / 多参考图分开）
│  │  │  │  ├─ local_tts.py            #   LocalTTS（ComfyUI 或隔离进程）
│  │  │  │  ├─ minimax_tts.py          #   MiniMax /v1/t2a_v2
│  │  │  │  └─ comfy_video.py          #   ComfyUI 对口型工作流
│  │  │  ├─ jobs/                      # 任务系统（§6）
│  │  │  │  ├─ scheduler.py            #   三队列调度（GPU / API / CPU）
│  │  │  │  ├─ manager.py              #   Job 生命周期：提交 → 快照 → 跟踪 → 登记
│  │  │  │  └─ recovery.py             #   02 §7.2 启动恢复顺序
│  │  │  └─ storage/                   # 持久化（§7）
│  │  │     ├─ project_store.py        #   单一写入入口 + 原子替换 + 防抖
│  │  │     ├─ artifacts.py            #   不可变版本路径 + 产物清单
│  │  │     ├─ machine.py              #   机器级设置与凭据引用
│  │  │     └─ cache.py                #   输入哈希 → 产物复用
│  │  ├─ tests/                        # §11
│  │  └─ pyproject.toml
│  ├─ web/                             # React SPA（Vite；05 的落地目标）
│  │  └─ src/…（见 05 §5）
│  └─ launcher/                        # 启动器：检查 → 启动进程 → 打开工作台（02 §7.4）
├─ workflows/                          # ComfyUI 工作流 JSON + 验证包（02 §3.3）
└─ docs/                               # 本文档系列
```

> 命名约定：`*_svc.py` 为业务编排；`algorithms/` 内模块为纯函数（给定输入 → 输出，不触网络与磁盘）；`adapters/` 不 import `services/`。

---

## 3. 模块职责与依赖规则

### 3.1 依赖方向（单向，禁止逆转）

```
api ──▶ services ──▶ algorithms（纯函数）
 │           │
 │           ├─▶ adapters ──▶ 外部（文本/图片/MiniMax API、ComfyUI）
 │           ├─▶ jobs ──▶ adapters
 │           └─▶ storage
domain ◀── 被所有层引用（纯数据，无行为依赖）
core   ◀── 被所有层引用（配置/日志/事件）
```

| 规则 | 说明 |
|---|---|
| R1 | `api/` 不直接调用 `adapters/` 与 `storage/`——所有编排经 `services/`，保证快照与版本规则不被绕过 |
| R2 | `algorithms/` 不做 IO：可独立单测，与 02 §5 逐条对照验收 |
| R3 | `adapters/` 无业务概念（不知道「话轮」「重跑矩阵」），只做请求/错误/能力翻译 |
| R4 | `jobs/` 是唯一的「长任务入口」：任何超过 2 秒的操作必须经 JobManager 提交，不允许路由内 await 长调用 |
| R5 | `storage/project_store.py` 是 project.json 的**唯一写入者**（02 §6.3 单一写入入口） |

### 3.2 模块职责表

| 模块 | 职责 | 关键类/函数 | 依赖 |
|---|---|---|---|
| `api/*` | 参数校验、调用编排、错误码翻译（02 §8.2） | 各 Router | services, domain |
| `core/config` | 端口、路径、上限；启动自检参数 | `Settings` | — |
| `core/eventbus` | 事件序号、订阅、补发 | `EventBus.publish / subscribe / replay` | — |
| `services/script_svc` | 两次生成流程；选区改写；候选版本登记 | `generate_script()`, `rewrite_selection()` | adapters, jobs, storage |
| `services/audio_svc` | 试听 → 单元合成 → 对齐 → 建轨；读音词典版本 | `synthesize_units()`, `assemble_timeline()` | algorithms, adapters, jobs |
| `services/visual_svc` | 底图生成/上传、区域标注版本、样片区间选择 | `create_visual_variant()`, `create_rehearsal()` | adapters, jobs |
| `services/render_svc` | 渲染计划（可复用/需重算/估算）；分段提交；后处理 | `plan_render()`, `submit_render()` | algorithms, jobs |
| `services/export_svc` | MP4 / WAV / SRT 导出与校验 | `export_outputs()` | jobs, storage |
| `services/stage_svc` | 七态推导、过期标记、确认点状态 | `derive_stage_states()` | domain, storage |
| `jobs/scheduler` | 三队列调度与互斥（GPU 单卡排他） | `Scheduler.dispatch()` | adapters |
| `jobs/manager` | Job 全生命周期与快照 | `JobManager.submit() / pause() / cancel()` | storage, eventbus |
| `jobs/recovery` | 启动恢复（02 §7.2 五步） | `recover_on_startup()` | storage, adapters |
| `storage/project_store` | 工程索引读写：防抖、原子替换、并发保护 | `ProjectStore.patch(expected)` | domain |
| `storage/artifacts` | 不可变路径、清单、引用回收 | `ArtifactStore.register()` | — |
| `storage/machine` | 机器级设置、凭据引用（keyring） | `MachineSettings` | — |
| `storage/cache` | 输入哈希 → artifactId 复用 | `CacheStore.lookup()` | artifacts |
| `adapters/*` | 提供方请求/错误/能力翻译 | 见 §8 | — |

---

## 4. API 层设计

### 4.1 路由 → 模块映射（接口清单权威：01 §12.1）

| Router | 端点 | 编排调用 |
|---|---|---|
| `providers.py` | `GET /api/providers/capabilities` | `CapabilityRegistry.snapshot()`（驱动 UI 能力门控） |
| | `POST /api/providers/{id}/test` | `adapters` 按能力测试；付费试听与只读连通分开标注 |
| `projects.py` | `GET / PATCH /api/projects/{id}` | `ProjectStore`（PATCH 携带 `expectedRevision`，冲突 409） |
| `script.py` | `POST …/script/generate` | `script_svc.generate_script()` → Job |
| | `POST …/script/rewrite` | `script_svc.rewrite_selection()` → 候选差异 |
| `audio.py` | `POST …/tts/preview` | `audio_svc` 短试听（付费标注） |
| | `POST …/tts/synthesize` | `audio_svc.synthesize_units()` → Job |
| | `POST …/audio/assemble` | `audio_svc.assemble_timeline()` → Job |
| `visual.py` | `POST …/visual/generate` | `visual_svc`（明确生成或编辑能力） |
| | `POST …/rehearsals` | `visual_svc.create_rehearsal()` → Job |
| `renders.py` | `POST …/renders/plan` | `render_svc.plan_render()`（可复用/需重算/估算/阻塞项） |
| | `POST …/renders` | `render_svc.submit_render(planVersion)` → Job |
| `approvals.py` | `POST /api/approvals` | 写 Approval 对象（类型 + 输入版本 + 规格） |
| `jobs.py` | `GET /api/jobs/{id}` | 读完整状态（**重连以此为准**） |
| | `POST …/jobs/{id}/pause · /cancel` | `JobManager`（返回真实状态，非乐观态） |
| `events.py` | `GET /api/events` | SSE/WS 订阅（§5） |

### 4.2 横切约定（实现于 api 层中间件/依赖注入）

| 约定 | 实现 |
|---|---|
| 命名 | 请求/响应字段与 01 §11 TS 定义一致（camelCase），pydantic `alias` 统一转换，杜绝两套命名 |
| 幂等 | 提交类接口要求 `clientToken`；JobManager 以 `(clientToken)` 去重——相同请求连续点击只创建一项任务 |
| 并发保护 | PATCH 校验 `expectedRevision`；不匹配返回 409 + 当前版本，前端提示刷新 |
| 错误码 | 异常统一映射到 02 §8.2 表（`PROVIDER_DOWN` … `RENDER_FAILED`），响应体 `{code, message, detail?}` |
| 渲染计划一致性 | `renders` 提交时校验 plan 的输入哈希；不匹配返回「用旧版本 / 重新规划」选择项，**不静默重算** |
| 静态资源 | 前端构建产物由本服务提供（日常使用不跑 Vite，02 §7.4） |

---

## 5. 事件总线设计

### 5.1 结构

```
EventBus
  ├─ seq: int                          # 全局单调递增序号
  ├─ events.jsonl（内存 + 落盘）       # append-only：{seq, ts, type, payload}
  ├─ subscribers: dict[queue_id, asyncio.Queue]
  ├─ publish(type, **payload)          # 写日志 → 逐订阅者投递（慢消费者丢弃策略：保留最新快照）
  └─ replay(after_seq) -> list[event]  # 从日志补发
```

### 5.2 订阅与重连语义（01 §12.2 权威类型）

| 场景 | 行为 |
|---|---|
| 首次连接 | 不回放全量历史；前端先 `GET /api/jobs` + `GET /api/projects/{id}` 建立快照，再订阅增量 |
| 带 `Last-Event-ID`（seq）重连 | `replay(after_seq)` 补发缺失事件；窗口外（日志已轮转）返回 `snapshot_required` 提示，前端重新拉快照 |
| 事件去重 | 前端按 `seq` 单调过滤；乱序/回退一律丢弃 |
| 慢消费者 | 订阅队列有界；溢出时投递 `resync` 事件，前端走快照路径 |

事件类型沿用 01 §12.2（`job.stage` / `job.done` / `job.failed` / `approval.requested` / `system.vram`），实现中新增内部事件（`job.queued`、`job.unknown`）同样走 EventBus，**前端永远不轮询进度**。

---

## 6. 任务系统设计

### 6.1 JobManager 生命周期

```
submit(task_spec, clientToken)
  ├─ 幂等检查（clientToken 已存在 → 返回原 Job）
  ├─ 冻结输入快照（inputSnapshot：版本 ID + 参数 + 依赖哈希）
  ├─ 写 jobs/{id}/job.json（attempt 意图先落盘，02 §7.1）
  └─ 入队（按 task_spec.queue_class）

dispatch（Scheduler）
  ├─ GPU 队列：asyncio.Lock 单卡排他；本地 TTS / 视频 / 对齐 / 补帧共享
  ├─ API 队列：按 providerProfileId 限流；不占 GPU 锁
  └─ CPU 队列：Semaphore(n)（解码 / 对齐 / 后处理）

跟踪（适配器回调 → EventBus）
  ├─ stage 推进：prepare → infer → download → verify → post（02 §7.3）
  ├─ 提交后立即记录 providerTaskId（ComfyUI prompt_id / 远程任务 ID）
  └─ 完成：产物校验（下载成功且可解码）→ ArtifactStore 登记 → job.done
```

### 6.2 状态机实现要点（权威：02 §7.1）

- 状态转换表集中定义于 `domain/job.py`（一个 `TRANSITIONS` 常量），`manager.py` 是唯一执行者。
- `pause_requested / cancel_requested` 为**请求态**：Scheduler 停止派发新段，当前段完成后落 `paused / cancelled`；「正在取消」与「已取消」在 API 与事件中分开表达。
- `unknown`：适配器报告「提交后结果不明」时进入；`recovery` 启动时对 unknown 任务优先执行提供方查询，**不支持查询的保持 unknown 并计入 UI 待核实**。
- ComfyUI 无定向取消能力时：停止后续派发并等待当前任务结束，状态显示「取消待生效」（02 §7.3）。

### 6.3 恢复流程（`jobs/recovery.py`，权威：02 §7.2）

启动时按序执行，每步写日志：

1. 扫描 `jobs/` 下未结束任务与执行尝试记录（含 attempt 意图与 providerTaskId 之间的不确定窗口）。
2. 校验已落盘输出的完整性（文件哈希 + 对应输入快照）。
3. 有 ComfyUI `prompt_id` 的：查询队列/历史——仍在运行则继续跟踪，已完成则恢复取结果。
4. 有远程任务 ID 的 API 任务：使用提供方查询能力；不支持查询且结果不明的标 `unknown`。
5. 确认未提交或确定失败的才重新派发；**不把旧 running 一律变成 queued**。

---

## 7. 存储层设计

### 7.1 ProjectStore（project.json 唯一写入者）

| 规则 | 实现 |
|---|---|
| 单一写入入口 | 所有元数据变更走 `ProjectStore.apply(patch, expectedRevision)`；services 不直接写文件 |
| 原子替换 | 写 `tmp/` 临时文件 → `os.replace` 原子替换 + fsync；崩溃后以事件记录恢复（02 §6.3） |
| 防抖 | 编辑类 PATCH 2s 防抖合并落盘；内存态即时可读 |
| 版本冲突 | `expectedRevision` 不匹配 → 409；旧页面不能覆盖新状态 |
| 路径 | 工程内存相对路径；`machine.py` 管理机器级绝对路径与凭据 |

### 7.2 ArtifactStore 与缓存

- 产物登记：`artifacts/manifest.json` 追加 `{artifactId, kind, path, fileHash, dependencyHash, paramsSnapshot, workflowVersion}`——**正式产物使用不可变版本路径**，不覆盖同名文件（02 §6.2）。
- 图片产物：下载成功且可解码后才登记为工程资产；远程 URL 只作来源记录。
- `cache.py`：`dependencyHash → artifactId` 查表；命中即复用（不重复推理与计费，02 §5.9）。
- 临时输出：先写 `tmp/`，校验通过后移入正式路径并登记（02 §7.4）。
- 清理：按引用关系回收；不删除进行中任务、当前成片、保留版本引用的文件；**清理前返回可释放空间清单**（由 UI 展示确认）。

### 7.3 MachineSettings

- 凭据（API Key）存 Windows 凭据管理器（keyring）；机器级配置文件只存 `providerProfileId → 引用`。
- 工程与打包导出**只含配置 ID，不含密钥**（02 §6.2）。

---

## 8. 适配器设计

### 8.1 Protocol 接口（`adapters/base.py`）

```python
class TextProvider(Protocol):
    capabilities: TextCapabilities        # 流式、最大上下文、结构化输出
    async def generate(self, req: TextRequest) -> TextResult: ...
    async def rewrite(self, req: RewriteRequest) -> RewriteResult: ...

class ImageProvider(Protocol):
    capabilities: ImageCapabilities       # 文生图 / 单图编辑 / 多参考图编辑 / 比例 / 异步 / 取消 / 下载（分开登记）
    async def generate(self, req: ImageRequest) -> ImageResult: ...

class TTSProvider(Protocol):
    capabilities: TtsCapabilities         # 音色克隆 / 情绪 / 语速 / 停顿 / 读音 / 时间戳 / 流式 / 取消
    async def synthesize(self, req: TtsRequest) -> TtsResult: ...   # TtsResult.sampleCount 为权威时长来源
    async def audition(self, req: TtsRequest) -> TtsResult: ...     # 付费试听与只读检查分开

class VideoProvider(Protocol):
    capabilities: VideoCapabilities       # 双轨输入、分辨率、帧率、进度查询、取消
    async def submit(self, req: VideoRequest) -> str: ...           # 返回 prompt_id
    async def query(self, prompt_id: str) -> VideoJobState: ...
    async def fetch(self, prompt_id: str) -> VideoResult: ...
```

### 8.2 CapabilityRegistry

- 启动与配置变更时探测各 profile 的能力与就绪状态，供 `GET /api/providers/capabilities` 返回——**UI 能力门控的唯一数据源**（01 §1.6.4：不支持的选项不显示）。
- 能力探测区分「连通性」与「真实能力」（连接成功 ≠ 支持多参考图编辑，02 §4.1）。

### 8.3 适配器实现约束

| 约束 | 说明 |
|---|---|
| 错误翻译 | 提供方错误 → 02 §8.2 错误码；`PROVIDER_AUTH` 直接终止不重试 |
| 重试与费用 | 输入哈希命中缓存不重发；下载失败优先恢复获取；unknown 不盲目重试（02 §4.5） |
| ComfyUI 归属 | 首选工作台管理的专用实例；接用户实例时校验 prompt 归属，不中断他人工作流（02 §7.3） |
| 本地 TTS 隔离 | 语音依赖与视频依赖冲突时走隔离进程；接口不变（仍是 `providerProfileId` 指向的 TTSProvider） |
| 参数注入 | ComfyUI 参数用节点 ID + 字段的结构化映射，不做 JSON 文本替换（02 §3.3） |

---

## 9. 领域模型与派生规则落位

- pydantic 模型按 §2 的 `domain/` 分文件，字段与 01 §11 的 TypeScript 定义**同名同义**（camelCase alias），新增字段需先改 01 再落码。
- 派生规则（01 §11.2）归属：

| 派生量 | 实现位置 |
|---|---|
| 节目总时长 T（N / sampleRate） | `algorithms/timeline.py` |
| 句级全局位置 | `algorithms/timeline.py` |
| 阶段七态 | `services/stage_svc.py`（读产物版本 + Job 状态推导，**前端不自算**） |
| 过期标记 | `services/stage_svc.py` + `storage/cache.py`（依赖哈希对比） |

---

## 10. 前后端协作流程

```
启动      launcher → server 自检（驱动/GPU/显存/模型/磁盘）→ web 打开 → capabilities 拉取 → UI 门控渲染
编辑      PATCH project（expectedRevision）→ 2s 防抖落盘 → 保存状态经事件推送
生成      POST xxx/generate（clientToken）→ Job + 输入快照 → events 推送 stage → done 登记候选
采用      VersionPicker「采用」→ PATCH project（当前使用版本）→ stage_svc 标下游过期 → 事件推送
重连      GET /api/jobs + GET project 快照 → SSE 带 Last-Event-ID 补发 → seq 去重
确认      ApprovalPanel 全勾选 → POST /api/approvals → 打回带 reasonTarget → 导航
```

---

## 11. 测试与验证策略

| 层 | 内容 | 对照权威 |
|---|---|---|
| 单元（algorithms） | 重跑矩阵全部触发路径、样本数建轨、三区间边界、停顿单一归属、帧数换算 | 02 §5 逐条 |
| 契约（adapters） | mock 提供方：错误码映射、能力登记、取消语义、unknown 窗口 | 02 §4 / §8.2 |
| 集成（jobs） | 三队列互斥、暂停/取消请求态、恢复五步、快照隔离（运行中编辑不干扰） | 02 §7 |
| 存储 | 原子替换（模拟中断）、expectedRevision 冲突、清理白名单 | 02 §6 |
| 端到端 | A–E 关口验收用例（含 5 分钟完整片、中途恢复、局部重跑不扩散） | 02 §12 |

> 算法测试用例直接从 02 §12.1 功能验收表反向生成，保证「文档说到的每一条行为都有对应断言」。

---

## 12. 实施顺序（对齐 A–E 交付关口，02 §11）

| 关口 | 后端交付 | 关键模块 |
|---|---|---|
| **A 工作流验证** | 无 GPU 契约检查脚本、ComfyUI 双轨适配试验、硬件实测记录 | `adapters/comfy_video.py`、`workflows/` 验证包 |
| **B 最小闭环** | 已有脚本导入 → TTS（本地/MiniMax）→ 上传底图 → 视频 → 导出；**失败恢复有效** | `jobs/*`、`storage/*`、`audio_svc`、`render_svc`、`export_svc` 最小版 |
| **C 内容与图片 API** | 两次生成、选区改写、图片生成/编辑、资产复用 | `script_svc`、`visual_svc`、`adapters/text_api.py`、`adapters/image_api.py` |
| **D 编辑工作台** | 全 API 面、事件总线补发、七态推导、版本与影响提示 | `api/*` 全量、`stage_svc`、`core/eventbus.py` |
| **E 自动执行与交付** | 启动器、启动自检、恢复打磨、字幕与竖屏版式导出 | `launcher/`、`export_svc` 完整版 |

> **关口纪律**：阶段 A 未通过前不做 D 的全量页面接口；B 起任务快照与产物哈希即为必需（不是后期补丁）。旧九周计划不作为承诺，排期在 A 后按实测重估（02 §11）。

---

## 附录：与 02 的关系声明

本文档未引入与 02 冲突的任何决定；所有新增内容仅为**组织性补充**（仓库结构、模块边界、接口签名形态、协作流程）。若后续实现中发现本文与 02 §5 / §7 / §8 冲突，以 02 为准并回改本文。
