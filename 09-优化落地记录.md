# 优化落地记录 · 2026-09-11

## 本次基线与范围

开始时仓库已包含 v3.0 文档、九屏设计稿与五阶段前后端演示骨架，工作区无未提交改动。因此本次直接修复现有实现的流程正确性，没有重复搭建页面。

本轮落实 04 的音频时长、任务快照、旧结果保留、恢复和能力真实性要求。仍使用 mock 提供方，不能把演示通过当作本地模型或 MiniMax 已接通。

## 已落地

| 问题 | 修改后的行为 |
|---|---|
| 发言停顿被保存却不参与计时 | 单元偏移和总样本数包含 transitionGapMs、leadInMs、tailOutMs；校验负数、数量和单位 |
| 不同采样率被直接当成 48kHz | 适配器未返回规范 48kHz 时明确失败，避免时长静默翻倍；真实重采样仍由后续适配器负责 |
| 合成忽略语速与语气 | 请求传递模型、提供方、声音绑定、语气和语速；模拟器按语速改变估算值，不冒称真实音频 |
| 单元编号随排序变化 | 单元 ID 基于稳定话轮 ID |
| 排队期间编辑导致读取新稿 | 配音冻结工程与脚本快照，改写冻结脚本快照；执行不再动态取当前稿 |
| 老任务覆盖新稿或丢结果 | 生成脚本加入历史，只有输入未变时自动选用；配音结果加入 audioHistory，输入未变时更新当前配音 |
| 生成版本 ID 重启后可能重复 | 脚本版本使用唯一 ID，不采信提供方的进程内计数 |
| 三队列实际全部串行，队列容量被误作并发数 | GPU 保持单任务；API/CPU 具备独立的受限并发，等待任务不因 GPU 单并发而只容纳一项 |
| 幂等键跨工程混用 | 按工程和任务类型隔离 clientToken，重启后恢复历史幂等记录 |
| 启动只扫描而不恢复管理器 | 装入所有可读任务历史；queued 重新排队，执行情况不明的任务标 unknown，不重复提交 |
| 暂停、取消停留在请求态 | 排队任务可直接暂停或取消；在途请求完成后保留结果并转入 paused/cancelled；增加 resume 接口和按钮 |
| 服务退出遗漏工程落盘 | 停止后台执行等待，未核实任务保存 unknown，刷新工程存储 |
| 超过五分钟仍可渲染 | 前后端检查真实口径的总样本数；拒绝超限和过期配音，不截尾 |
| 旧脚本确认记录确认了新稿 | 确认按脚本版本匹配，并采用该版本最后一次决定 |
| 模拟数据冒充实际成片 | 能力返回 mode/productionReady，任务结果标 simulated；顶部显示演示模式，模拟渲染不改正式输出指针、不登记假视频 |
| 页面凭旧输出宣称片段可复用 | 无后端哈希计划时不显示复用成功；模拟分段明确为示意 |
| 顶部显存写死 17% | 改为模拟提示或尚未测定，不伪装监测数值 |
| 快速创建工程复用了未落盘 ID | 工程列表合并内存与磁盘，未刷新磁盘的工程也参与 ID 分配 |

## 验证

- 后端自动回归：22 项通过，临时目录隔离，不读取或覆盖已有工程。
- 包含应用级流程：新建两期、脚本、配音、五分钟校验、模拟渲染、关闭服务再重启、任务与幂等恢复。
- 并发回归验证 API 能在 GPU 等待期间完成，而两个 GPU 任务不并行。
- 运行中暂停后继续不会再次调用提供方；取消后保留已有返回结果。
- 同一脚本重新合成并采用配音后，旧配音/样片确认失效；新构图也撤销旧样片确认。
- TypeScript 检查与 Vite 生产构建通过。
- pnpm 默认入口受本机依赖管理配置影响，构建使用项目内 TypeScript/Vite 入口完成，未修改锁文件。

可复跑命令：

```powershell
# 在仓库根目录
& 'apps/server/.venv/Scripts/python.exe' -m pytest apps/server/tests -q

# 在 apps/web
node node_modules/typescript/bin/tsc --noEmit
node node_modules/vite/bin/vite.js build
```

本次仅进行了类型、构建和自动化流程验证，未进行浏览器视觉回归或 RTX 5080 推理实测。

## 后续实施边界

1. 真实提供方仍未装配：文本与图片服务信息尚未指定；MiniMax 音色与凭据、ComfyUI 经验证的工作流需要在联调时配置。
2. 当前 TTS 只返回模拟样本数，没有实际 PCM 文件；真实母轨合成、对齐和字幕仍需实现。
3. 当前暂停点是一次业务 Job 结束，不是多段视频内部检查点；真实分段渲染接入时需增加单段落盘与检查点恢复。
4. unknown 状态可见但尚无提供方查询适配器；不会自动重发不确定任务。
5. audioHistory 已保留候选；候选音频的真实文件试听和采用界面需随真实 TTS 完善。配音/样片确认仍需进一步绑定完整资产哈希，而非仅脚本版本。
6. 视频分段、上下文续接、可复用计划和横竖屏导出尚为设计与示意，未验证动态效果，不自动采用替代视频形式。
7. 能力中的 ready 表示演示接口可调用，productionReady 才区分真实接入；真实适配器还需通过连接与样片验证才能对用户宣称可生产。

下一轮优先接通实际语音文件与 MiniMax 适配，再完成已对齐双轨到 ComfyUI 的最小真实样片。上述基础修复已为这些接入提供可测试的输入、状态与恢复规则。
