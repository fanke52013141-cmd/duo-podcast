# 双声播客工坊 DuoCast · 代码审查报告（第二轮）

> 审查日期：2026-09-11 ｜ 基线：`d925a3e` ｜ 依据：[10-代码审查规划.md](10-代码审查规划.md)
> 方法：全部源码逐文件通读（后端 44 文件 / 前端 28 文件）+ 22 项既有回归复跑 + 12 项新场景实证（脚本保留在 `.review/scenario_tests.py`，可复跑）。
> 证据标注：`文件:行号` 为定位；「实锤」表示已用脚本复现，「推演」表示仅代码路径推演未实测。

---

## 0. 结论速览

### 0.1 总体判断

**09 声称的 18 项修复经独立复核，17 项证实、1 项部分证实**，质量高于上一轮审查时的状态。但本轮发现：**07 报告的两个 P0（历史版本编辑覆盖草稿、409 静默丢弃）从未被修复**，且 09 的修复引入/残留了若干新问题——最重要的是「重复取消广播假失败事件」和「任务卡片操作错位」。

| 严重度 | 数量 | 说明 |
|---|---|---|
| **P0** | 2 | 均为 07 报告已提出、至今未修的丢数据/静默失败类缺陷 |
| **P1** | 6 | 假失败事件、UI 操作错位、SSE 静默缺口、路径穿越、任务凭空消失、确认状态不一致 |
| **P2** | 15 | 线程安全、死代码、契约漂移、文案失真等 |

### 0.2 基线复验（轮 0 结果）

| 检查 | 结果 |
|---|---|
| `pytest apps/server/tests -q`（22 项，临时存储） | **22 passed** ✅ |
| `smoke_e2e.py`（对 8100 实跑） | **ALL PASS**（12 步全绿；样本数 157440 = 3×42240 + 2×15360，与 0.22s/字 + 320ms 间隔口径完全吻合）✅ |
| `tsc --noEmit` + `vite build` | 通过，产物 301.97 kB / gzip 92.29 kB ✅ |
| `GET /`（dist 构建后） | 返回 HTML（`apps/web/dist` 静态托管正常）✅ |
| 中文路径（仓库根 `双人播客项目`） | 服务正常启动、目录正常创建、UTF-8 无乱码 ✅ |

### 0.3 必须先修的 2 件事（P0）

1. **P0-1 历史版本编辑覆盖当前草稿**（07 首报，未修）——`upsertDraftTurns` 缺 `baseRevId` 参数。
2. **P0-2 保存/确认类 mutation 的 409 被静默吞掉**（07 首报，未修）——`useSaveDraft`/`useConfirm` 全链路无 onError。

---

## 1. G1 复核：09「已落地」18 项逐条结论

| # | 09 声称的修复 | 结论 | 证据 |
|---|---|---|---|
| 1 | 停顿计时含 transitionGapMs/leadIn/tailOut | ✅ 证实 | `voice_svc.py:84-104`；`test_audio_includes_all_gaps`（offset 4800/76800、总 134400 手工复算一致） |
| 2 | 非 48kHz 明确失败 | ✅ 证实 | `voice_svc.py:95-96`；`test_sample_rate_mismatch…` |
| 3 | 语速/语气传入提供方 | ✅ 证实 | `voice_svc.py:87-91`（emotion/speedRatio）；`test_audio_includes_all_gaps` 断言请求体 |
| 4 | 单元 ID 基于稳定话轮 ID | ✅ 证实 | `voice_svc.py:30`（`U-{turn.id}`）；`test_unit_ids_stay_stable_after_reordering` |
| 5 | 排队期间编辑不读新稿（配音冻结快照） | ✅ 证实 | `api/voice.py:27-35`（projectSnapshot 进 input_snapshot）；`test_voice_api_freezes_revision_at_submission` |
| 6 | 老任务不覆盖新稿/丢结果 | ✅ 证实 | `voice_svc.py:111-116`（history 恒追加、仅快照一致时替换当前轨）；`script_svc.py:45-46`（版本变更则只入历史）；两组测试 |
| 7 | 版本 ID 重启不重号 | ✅ 证实 | `script_svc.py:28`（uuid）；mock 的进程内 `revisionId`（`mock.py:89`）已被 `build_script_revision` 忽略 |
| 8 | 三队列真并发 | ✅ 证实 | `manager.py:50-54`（API/CPU 按 cap 开 worker）+ GPU 排他锁 `:171`；`test_api_can_run_while_gpu_busy…` |
| 9 | 幂等键按工程+类型隔离 | ✅ 证实 | `manager.py:102-106`；`test_job_snapshot_and_idempotency_are_scoped` |
| 10 | 启动装入全部任务历史、queued 重新入队、不明标 unknown | ✅ 证实 | `recovery.py:31-40` + `manager.restore`；`test_restore_queued_history_and_unknown`。⚠️ 但见 P1-5（损坏清单跳过） |
| 11 | 暂停/取消收敛到终态、保留结果 | ✅ 证实 | `manager.py:176-182`；`test_running_controls_reach_final_state…`。⚠️ 但见 P1-1（重复取消假失败） |
| 12 | 服务退出补落盘 | ✅ 证实 | `main.py:153-158`（stop → flush → log）。⚠️ kill -9 场景见 P2-9 |
| 13 | 超五分钟拒绝渲染、不截尾 | ✅ 证实 | `api/render.py:22-28`（422 + 409 过期配音）；`test_render_duration_limit_includes_tail`；前端 `RenderPage.tsx:108` 同口径 |
| 14 | 确认按脚本版本匹配 | ✅ 证实 | `stage_svc.py:26-28`（approval 过滤 `input_revision_id == current_draft_revision`）；`test_old_approval_cannot_confirm_new_revision` |
| 15 | 模拟数据不冒充真实（mode/productionReady/simulated） | ✅ 证实 | `mock.py`（capabilities 全 `simulated:True`）、`main.py:104-107`（simulated_runner）、`RenderPage.tsx:79,227,242`、TopBar 演示模式横幅 `AppShell.tsx:24,32`；smoke「renders: demo completed without claiming real video」 |
| 16 | 无哈希计划不宣称复用 | ✅ 证实 | `RenderPage.tsx:128`（`reusedCount = 0` 注释明确）、`:273`（`reused = false`） |
| 17 | 顶部显存不伪装 | ✅ 证实 | `AppShell.tsx:32`（「演示模式·不生成真实媒体」/「显存：尚未测定」）。`types.ts:213-214` 的 VRAM 常量已无引用（死代码，见 P2-4） |
| 18 | 快速创建工程 ID 不复用未落盘 ID | ✅ 证实 | `project_store.py:60`（list_ids 合并内存+磁盘）。⚠️ 但见 P1-6（重复 id 静默别名） |

**部分证实项**：第 10/11/18 项主路径成立，但各自存在本轮新发现的边界缺陷（见下）。

---

## 2. P0 级缺陷

### P0-1 查看历史版本时编辑，内容覆盖当前草稿（07 P0-1 未修，本轮确认仍在）

- **位置**：`apps/web/src/lib/api.ts:153-179`（`upsertDraftTurns` 无基版本参数）；`apps/web/src/pages/EditScriptPage.tsx:134-142`（`scheduleSave` 直接把本地 `turns` 交给 `upsertDraftTurns(proj, nextTurns)`）。
- **路径**：版本选择器可切换 `viewRevId` 到历史版本（`EditScriptPage.tsx:351`），页面 Alert 承诺「编辑将基于该版本创建新草稿」（`:378`）。但 `upsertDraftTurns` 内部 `cur` 恒取 `currentDraftRevision`（api.ts:154-155），不感知用户正在查看的版本——把 R1 的话轮内容**原位替换进当前草稿 R3**（api.ts:164-167 未确认且在尾部时）。
- **后果**：用户在查看历史版本时的任何一次按键（2s 防抖后自动保存），都会把草稿话轮整体替换为历史版本内容 + 该处编辑，且 revision 正确递增——**无冲突、无提示、不可撤销**（详见 P0-2：保存失败都看不住，覆盖成功更看不住）。
- **修法**（07 §7 已给）：`upsertDraftTurns(proj, turns, baseRevId)`；`baseRevId !== currentDraftRevision` 时强制走「新建草稿版本」分支。

### P0-2 保存与确认的 409 冲突被静默吞掉（07 P0-2 未修，本轮确认仍在）

- **位置**：`EditScriptPage.tsx:139`（`saveDraft.mutate` 无 onError）、`:203`（`handleConfirm` 的 `confirm.mutate` 无 onError）；`VoicePage.tsx:139`（确认配音）、`VisualPage.tsx:93`（确认样片）同样裸调用。
- **机制**：TanStack Query 的 `.mutate()` 错误只进入 mutation state，不抛出、不重试、无 UI 反馈。服务端 409（`PROJECT_REVISION_CONFLICT`）后，本地编辑继续基于过期 revision，**每次自动保存都 409，用户全程无感知**，直到刷新页面丢失全部未保存编辑。确认类操作静默失败则更糟：按钮已回弹、无任何提示，用户以为已确认。
- **对照**：同页面 `handleRewrite`/`handleRegenerate`（EditScriptPage.tsx:190,213）与 `RenderPage.tsx:121` 均已正确接 onError——修复模式在库内已有，属于漏网。
- **修法**：`useSaveDraft`/`useConfirm` 层统一 onError（toast + 409 时以服务端返回重放：重取 `proj.revision` 后重新 mutate 一次）。

---

## 3. P1 级缺陷

### P1-1 重复取消运行中任务 → 广播矛盾的 `job.failed`（07 P0-3 残留）【实锤 S07】

- **位置**：`manager.py:150-160`（`cancel()` 对已处于 `CANCEL_REQUESTED` 的任务落到 `:159` 再转移）；`domain/job.py:45`（转移表 `CANCEL_REQUESTED → {CANCELLED, FAILED, UNKNOWN}`，不含自身）；`manager.py:110-113`（非法转移路径广播 `job.failed`）。
- **复现**（`.review/scenario_tests.py::s07`）：running → cancel → cancel → 总线收到 `job.failed`，`error="illegal transition cancel_requested->cancel_requested"`，且 `job.status` 仍是 `cancel_requested`。前端任务中心会把该任务渲染为失败态文案（`TasksPage.tsx:61` 把 `cancel_requested` 归入 fail 图标）。
- **修法**：`cancel()` 首行加 `if job.status == CANCEL_REQUESTED: return job`；同时把 `transition()` 非法转移路径改为**不广播 job.failed**（记日志即可）——07 §7 第一批第 3 条的后半句至今未做。

### P1-2 任务卡片的「暂停/取消」作用于另一条被选中的任务

- **位置**：`TasksPage.tsx:106`（`handleCancel = () => cancel.mutate(selected.id)`）、`:158-166`（卡片按钮 onClick 只 `stopPropagation` 后调 `handlePause()/handleCancel()`，不传卡片自身 job，且不先 `setSelectedId(j.id)`）。
- **路径**：选中任务 A（点过卡片 A）→ 直接点卡片 B 的「取消」→ **A 被取消**，B 原样运行。按钮可见性又用 `selected` 的 `canCancel`（`:103`）门控所有卡片（`:162`），两套状态源混用。
- **修法**：卡片操作改为 `cancel.mutate(j.id)`（或点击按钮时先 setSelectedId）。

### P1-3 SSE 缓冲轮转后部分补发无 resync 提示 → 事件静默缺口【实锤 S11】

- **位置**：`api/events.py:26-33` + `core/eventbus.py:79-83`。`replay(after_seq)` 非空即走补发分支；只有**完全为空**才发 `system.resync`。缓冲（cap=20000，`config.py:27`）轮转掉断线期间早期事件时，客户端收到的是「缺口之后的部分事件」，永远收不到缺口语段，也不触发全量回读。
- **复现**（`s11`）：cap=5、发 12 事件、`replay(2)` → 首条补发 seq=8，seq 3-7 静默丢失。
- **修法**：`replay` 返回时对比 `missed[0].seq > after_seq + 1` 即判定缺口，改发 resync；或 `events.py` 校验首条序号连续性。

### P1-4 工程 ID 无校验：路径穿越 + 重复 ID 静默别名【实锤：穿越已复现】

- **位置**：`api/projects.py:39`（`payload.get("id")` 直通）；`project_store.py:42-43`（`root / project_id` 无规范化校验）。
- **复现**：`store.create("../evil")` + flush → 目录落在存储根**之外**（`.../duocast-x-*/evil/project.json`，`is_relative_to(root)` 为 False）。`load()` 同样可越界读。
- **同源问题**：重复 POST 相同 id 返回**已存在的旧工程**（`project_store.py:84-85`），调用方无从得知未创建（`s06` 实锤）。
- **修法**：服务端拒绝客户端 id 或校验 `^EP[A-Za-z0-9_-]+$` 且 `Path(id).name == id`；重复创建返回 409 而非旧工程。

### P1-5 损坏的 job.json 使任务在 UI 凭空消失（与日志自相矛盾）【实锤 S08】

- **位置**：`jobs/recovery.py:28-30`——manifest 不可读时 `log("recovery", "job manifest unreadable, marking unknown", …)` 然后 **`continue`**：既没标 unknown 也没装入管理器。
- **后果**：重启前可见的任务，重启后从任务中心消失——正是 07 P0-4 要杜绝的现象，只是触发条件从「不加载历史」缩到「历史中有个别损坏文件」。
- **修法**：不可读时生成占位 Job（status=unknown, error=manifest unreadable）装入管理器，让用户至少看见它存在。

### P1-6 确认状态与阶段推导不一致：确认历史版本显示「已确认」但阶段仍待确认

- **位置**：`EditScriptPage.tsx:201-204`（`handleConfirm` 用 `revision.id`——即**当前查看**的版本，可以是任何历史版本）；`:217-219`（`scriptApproved` 也按查看版本判断，徽章变绿）；`stage_svc.py:26-27`（后端只认 `input_revision_id == current_draft_revision`）。
- **后果**：用户查看 R1 时点「确认脚本」→ 徽章「脚本 R1 · 已确认」、可点「下一步」，但阶段轨始终「待确认」，配音页 `voiceApproved` 依赖的 script 确认也未生效——两条状态口径分裂，用户被引导进入半就绪状态。
- **修法**：确认按钮仅对 `viewIsDraft` 可用；或后端确认时校验 `input_revision_id == current_draft_revision` 否则 422。

---

## 4. P2 级问题

### 4.1 后端

| # | 问题 | 证据 |
|---|---|---|
| P2-1 | **存储/任务管理器线程安全**：`list_projects`/`get_project`/`list_jobs`/`get_job` 是同步 `def`，跑在线程池，与事件循环线程并发读写 `ProjectStore._projects` / `JobManager._jobs` 字典（迭代中变更可抛 RuntimeError → 间歇 500）。`patch/create` 是 async 无此问题 | `api/projects.py:30,45`；`api/jobs.py:17,23`；`project_store.py:60`（`list_ids` 迭代合并）；推演 |
| P2-2 | `visual.generate` 用 **await 前加载的 project** 提交 `expected_revision`（`main.py:84-90` → `visual_svc.py:26-28`），合成期间任何编辑 → `ProjectRevisionConflict` → 任务失败。tts/script 的写回在 await 后重新 load，无此问题（对照 `voice_svc.py:108-116`） | 推演 |
| P2-3 | 慢消费者只弹出 **1 条**旧事件就塞 resync，队列中其余旧事件仍会先于 resync 被消费，注释「丢弃历史事件」与实现不符 | `eventbus.py:56-68` |
| P2-4 | `render_svc.apply_output_version` 死代码（main.py 未导入未调用）；`ArtifactStore.register` 无任何生产路径调用 → job 结果 `artifactIds`（如 `[revision.id]`）与资产 manifest 永不对应（smoke 实测 `artifacts: 0`）。mock 阶段「不登记假资产」是对的，但接真实适配器前需要把登记链路接上，否则 P1-5（07）以更隐蔽的方式复活 | `main.py:36-107`；`smoke_e2e.py` 输出 |
| P2-5 | `patch_project` 接受**任意字段**白名单缺失：客户端可注入 `revision`、`id`、`approvals`、`audioTimeline` 等任意 schema 合法但业务非法的状态 | `api/projects.py:53-64` |
| P2-6 | `build_script_revision` 硬编码 `source_input={"kind": "article", …}`——三种入口（topic/article/script）的 kind 信息丢失 | `script_svc.py:29` |
| P2-7 | `events.jsonl` 只追加无轮转（`event_log_cap` 仅限内存缓冲）；长期使用磁盘无限增长 | `eventbus.py:38-47` |
| P2-8 | `EventBus.publish` 可从任意线程调用（docstring 声称线程安全），但 `_subscribers` 迭代与 `subscribe/unsubscribe` 无锁 | `eventbus.py:49-69` |
| P2-9 | 防抖 2s 窗口内 kill -9：**已返回 200 的编辑不在磁盘**（`s09` 实锤）。属设计权衡，建议在 HANDOFF「已知坑」中明示，或把防抖缩短到 500ms/编辑即 fsync | `project_store.py:93-106`；实测 |

### 4.2 前端

| # | 问题 | 证据 |
|---|---|---|
| P2-10 | **VoicePage 间隔滑杆错位**：`gapOverride` 只写入 `gapMs[0]`（`i === 0 ? gapOverride : v`），但滑杆显示的是「选中话轮对应」的 `gapMs[gapIdx]`——选中第 3+ 话轮调间隔时，改动落在第一个间隔上、显示值回跳，**送往后端的间隔与用户意图不符** | `VoicePage.tsx:96-99,155,220-228` |
| P2-11 | `types.ts` 缺 `audioHistory` 字段（后端 `Project` 已序列化该字段，09 的候选保留机制前端无类型可依） | `types.ts:120-137` vs `domain/project.py:92` |
| P2-12 | 慢 consumers 收到 `id: -1` 的 SSE 事件（07 P1-6 残留，`s10` 实锤）：浏览器会以 `Last-Event-ID: -1` 重连 → 服务端全量补发。后果有限（resync 兜底），但 07 的修法（resync 不带 id）仍未做 | `eventbus.py:19`；`api/events.py:42` |
| P2-13 | EditScriptPage 统计条提示「Ctrl+Z 撤销」但无任何撤销实现；读音词典数据仅存组件 state，刷新即失（后者 09/README 已声明未做，前者是文案失真） | `EditScriptPage.tsx:525,538-542` |
| P2-14 | `HomePage.handleCreate` 的 `mutateAsync` 无 try/catch → 创建失败变成未处理 rejection，弹层卡住 | `HomePage.tsx:110-114` |
| P2-15 | 候选改写弹层会把**历史会话中已完成的旧 rewrite 任务**重新弹出（页面挂载时按「任意 succeeded 的 rewrite」回填），接受与否语义含混 | `EditScriptPage.tsx:121-131` |
| P2-16 | `voiceApproved` 后 `canSynthesize` 永远 false，但 Alert 文案承诺「重新合成将覆盖当前时间轨」——承诺与可用性矛盾（无解除确认的入口） | `VoicePage.tsx:101,298-303` |
| P2-17 | 阶段轨把 `stale` 也渲染成 done 高亮（`st === 'done' || 'pending_confirm' || 'stale'`），过期态与就绪态视觉不可分 | `AppShell.tsx:55` |

### 4.3 07 报告遗留项状态核对

| 07 编号 | 本轮结论 |
|---|---|
| P0-1 / P0-2 | **未修**（本轮 §2 重新立案并细化证据） |
| P0-3 | 大体修复（终态收敛✓）；「transition 不再发 job.failed」后半句**未做**（本轮 P1-1） |
| P0-4 | 修复✓（全量装入 + unknown；残余边界见 P1-5） |
| P1-1 / P1-2 / P1-3 / P1-7 / P1-9 / P1-11 | 修复✓（TimelineBar 已抽成壳级组件三屏共享，`AppShell.tsx:114-162`） |
| P1-4 | 部分修复：voice/visual stale✓（`stage_svc.py:39-46`）；**render 无 stale 推导**（渲染后改稿不标过期），确认记录仅绑脚本版本不绑资产哈希（09 边界 #5 已声明） |
| P1-5 | 未修——但与「mock 不登记假资产」原则一致，转入 P2-4（接入真实适配器前必须接通） |
| P1-6 | 未修（`s10` 实锤仍在），转入 P2-12 |
| P1-8 | 未复验（色值已改为 `--wu-semantic-brand-hover` 变量，需按 tokens 实际值重算对比度后定论） |
| P1-10 | 未验证（响应式超本轮范围，维持 07 结论） |

---

## 5. 与设计文档的偏差

| 项 | 文档 | 实现 |
|---|---|---|
| 阶段七态的「已过期」覆盖面 | 01 §13.1 全阶段失效传播 | render 阶段无 stale；确认记录不绑资产哈希（09 边界已声明一半） |
| PATCH 语义 | 01 §12.1 expectedRevision 冲突保护 | 缺 `expectedRevision` 一律 409（`s02` 实锤）——比文档更严格，但对「乐观锁可选」的调用方不友好，建议文档写明 |
| 凭据存储 | 06 §7.3「只存 credentialRef 不落明文」 | `patch_machine` 不校验/不改写 `providerProfiles` 内容，明文密钥可经 API 落盘 `machine.json`（前端当前只发 ref，无实际泄露路径，属约束未强制） |
| 工程目录布局 | 06 §7.1 project.json 唯一写入者 | ✓ 成立（apply/create 是唯一写入口，原子替换 + fsync 落实） |

---

## 6. 做得好的（应保持）

1. **时间权威数学零误差**：lead-in/gap/tail 全为整数样本、48ms×样本率换算、bool 拒绝（`type(ms) is not int` 恰好排除 True）、非 48kHz 硬失败——`voice_svc.py:84-104` 可直接当教科书。
2. **快照-采用双轨写回**：`audio_history` 恒追加 + 「输入未变才更新当前轨」的条件写回，是 09 最有价值的设计，测试覆盖到位。
3. **mock 诚实性贯穿全栈**：capability `simulated` → runner `simulated:True` → 前端演示模式横幅 → 渲染页「流程演示」徽标，四层一致，无一处把模拟产物标成真实。
4. **21 个新回归测试**质量高：不用运行中的服务、临时目录隔离、直接断言领域不变量（offsets、幂等范围、恢复语义），是对 09 修复的真实护栏。
5. 原子落盘（tmp + fsync + `os.replace`）在 store/manager/artifacts 三处实现一致。

---

## 7. 修复路线建议

**第一批（演示前，~1 天）**
1. P0-1：`upsertDraftTurns` 加 `baseRevId`，跨版本强制新建草稿。
2. P0-2：`useSaveDraft`/`useConfirm` 统一 onError + 409 重放。
3. P1-1：`cancel()` 幂等短路；`transition()` 非法路径停发 `job.failed`。
4. P1-2：任务卡片操作绑定卡片自身 job id。
5. P2-10：间隔滑杆读写同一索引。

**第二批（真实提供方接入前，~1 天）**
6. P1-4：工程 ID 服务端生成或白名单校验；重复 id 返回 409。
7. P1-3：`events.py` 校验补发连续性，缺口改发 resync。
8. P1-5：损坏 manifest 生成 unknown 占位任务。
9. P1-6：确认按钮限当前草稿；后端同步校验。
10. P2-4：资产登记链路设计（真实适配器的 artifactId ↔ manifest ↔ job.result 对应关系）。
11. P2-1：四个同步端点改 `async def`（或给 store/manager 加锁）。

**随迭代**：P2-2/3/5/6/7/8/9/11-17 按 07 报告第二批同样方式排入。

---

## 附录：复跑方式

```bash
# 回归基线（35 项：22 项契约 + 13 项审查修复回归）
cd apps/server && .venv/Scripts/python.exe -m pytest tests -q

# 审查场景（12 项，证据脚本已入库 tests/scenario_checks.py，临时目录不污染数据）
cd apps/server && .venv/Scripts/python.exe tests/scenario_checks.py
```
