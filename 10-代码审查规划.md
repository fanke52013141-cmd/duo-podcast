# 双声播客工坊 DuoCast · 代码审查规划

> 编制日期：2026-09-11 ｜ 基线：`d925a3e`（v3.0 五阶段骨架 + 09 优化落地）｜ 前置文档：[04-详细优化方案.md](04-详细优化方案.md)（权威需求）、[07-代码与UI交互审查报告.md](07-代码与UI交互审查报告.md)（上轮缺陷）、[09-优化落地记录.md](09-优化落地记录.md)（上轮修复）
>
> 本文档回答：审什么、按什么标准审、按什么顺序审、每一步怎么验证、产出什么。

---

## 0. 审查定位与目标

当前状态一句话：**流程与编排为真、内容生成为假（mock 适配器）**。09 记录声称修复了 07 报告的大部分 P0/P1，但修复未经独立复核；同时 09 列出 7 条「后续实施边界」属于已知未做，不算缺陷。

本轮审查的三重目标（按优先级）：

| # | 目标 | 说明 |
|---|---|---|
| G1 | **复核 09 声称的修复是否真实闭环** | 09 的「已落地」表共 18 项，逐项给出「证实 / 部分证实 / 未证实」结论。上轮修复与验证出自同一次改动，自证不足 |
| G2 | **找出修复未覆盖区域的新缺陷** | 07 审查时部分模块尚在演进；本轮以 d925a3e 为准重新过一遍核心链路 |
| G3 | **为「真实提供方接入」做前置风险审查** | 下一迭代的接口是 `adapters/base.py` 契约 + 时间权威数学 + 任务快照/恢复。审查这些接缝，确保真实适配器接入时不会被骨架层的隐藏缺陷污染 |

**明确不在本轮范围**：UI 视觉对齐回归（08 报告已专项处理）、真实模型/ComfyUI 实测（关口 A 未到）、性能压测（单用户本地应用，仅在缺陷涉及性能时记录）。

---

## 1. 审查对象与热点分级

代码量：后端 Python 约 2,300 行（含测试），前端 TS/TSX 约 4,800 行。

### 1.1 后端热点分级

| 级别 | 文件 | 行数 | 入选理由 |
|---|---|---|---|
| **A（必逐行）** | `jobs/manager.py` | 217 | 任务状态机 + 三队列并发 + 暂停/取消/恢复，07 报 P0-3/P1-2 均在此；09 改动最大 |
| **A** | `main.py` | 218 | `_job_runner()` 任务分派 + 装配顺序 + 启动恢复接线 + 静态托管（曾出 parents[4] 事故） |
| **A** | `storage/project_store.py` | 124 | revision 乐观锁 + 防抖落盘，07 报 P0-1/P0-2 的后端根；落盘竞态是数据丢失高危点 |
| **A** | `services/voice_svc.py` | 124 | 时间权威数学（样本数累计、transitionGapMs、48kHz 强制），07 报 P1-1，README 三条技术事实的落点 |
| **A** | `core/eventbus.py` + `api/events.py` | 87+53 | SSE 事件序号与重连语义，07 报 P1-6（Last-Event-ID 污染） |
| **B（重点抽查）** | `jobs/recovery.py`、`storage/artifacts.py`、`storage/machine.py`、`services/{script,visual,render,stage}_svc.py`、`adapters/{base,mock}.py` | — | 恢复链路、资产一致性（P1-5 artifactIds 悬空）、能力声明诚实性（mode/productionReady） |
| **B** | `api/*.py`（10 个路由文件） | ≈450 | 输入校验、错误码、并发写工程、路径处理（中文/空格） |
| **C（通读即可）** | `domain/*`、`core/{config,logging}.py`、`tests/*`、`smoke_e2e.py` | — | 模型定义正确性顺带核对；测试本身是审查的验证工具 |
| **C** | `tests/test_optimization.py` | 250 | 09 的回归底座，先读懂它才能判断 G1 复核到什么程度 |

### 1.2 前端热点分级

| 级别 | 文件 | 行数 | 入选理由 |
|---|---|---|---|
| **A** | `lib/api.ts` | 338 | 全部请求 + Query hooks；07 报 P0-2（409 静默丢弃）的修复点 |
| **A** | `lib/sse.ts` + `App.tsx` | 41+95 | 重连、Last-Event-ID、`system.resync` 全量回读、跨工程状态切换（P1-7） |
| **A** | `pages/EditScriptPage.tsx` | 572 | 最大页面；历史版本编辑（P0-1）、自动保存、候选改写采用流 |
| **B** | `pages/{VoicePage,RenderPage,VisualPage}.tsx` | 433/325/360 | 确认点逻辑、五分钟校验、分段复用声明的诚实性（09 两条相关修复） |
| **B** | `stores/index.ts`、`lib/types.ts` | 63+268 | Zustand 工程切换清理；类型与后端 Pydantic 逐字段对照 |
| **C** | 其余 pages、`components/{wu,AppShell}.tsx`、styles | — | 结构已对齐设计稿（08 报告），通读 + 抽查 AppShell 三条硬规则即可 |

### 1.3 可复用验证资产

| 资产 | 状态 | 用途 |
|---|---|---|
| `apps/server/tests/test_optimization.py`（22 项）等 4 个测试文件 | 在库 | G1 复核的基础，轮 0 跑通并读懂覆盖面 |
| `apps/server/smoke_e2e.py` | 在库 | 全链路冒烟（需后端起在 8100，见 HANDOFF 环境陷阱） |
| `.review/` 审查脚本（vocab-audit、shot-final、full-audit） | **本地不存在**（gitignored，在上台机器） | 本轮按需重建；07 §1.2 有复现手段记录可循 |

---

## 2. 审查维度与具体问题清单

六个视角，每个视角附「审的时候要问的问题」。结论必须落到 `文件:行号` 证据。

### D1 正确性与状态机

- 任务状态机 `queued→running→paused/cancelled→succeeded/failed/unknown` 的**转移矩阵是否完备**：有没有「组合状态 + 事件」导致非法转移（如 running 中 resume、paused 中 cancel、重启时 queued→unknown）？
- 07 报 P0-3（pause/cancel 卡非终态 + 矛盾的 `job.failed` 事件）修复后：取消信号是真中断还是仍等请求完成？`transition` 是否还会发 `job.failed`？
- 阶段状态七态（未准备/可执行/执行中/待确认/已就绪/已过期/失败）推导（`stage_svc.py`）：确认失效传播是否实现（07 P1-4）？确认记录是否绑定输入版本 + 规格（09 声称按脚本版本匹配）？
- 候选改写「不落盘、接受才替换」与配音 `audioHistory` 的版本选用逻辑：输入未变才自动选用——「未变」的判定是快照哈希还是引用相等？

### D2 并发与异步安全

- `project_store.py` 防抖落盘：**防抖定时器 flush 与进程退出 flush、与同步读的竞态**。是否原子写（临时文件 + rename）？Windows 上 rename 语义是否处理了目标存在？
- revision 乐观锁：比较-and-set 是否在写路径全程持锁？内存态与磁盘态谁权威？列表合并内存与磁盘（09 提到 ID 分配修复）是否引入双写不一致？
- `manager.py` 三队列：GPU 单并发 + API/CPU 受限并发的实现是否真并行（09 声称已修，07 P1-2）？同一工程的两个任务并发写 `project.json` 是否串行化？
- `eventbus.py`：事件序号单调性、慢消费者背压、`resync` 事件的 seq=-1 污染（07 P1-6）是否真修？
- `_job_runner` 完成后写回工程：与用户同时编辑的 revision 冲突如何收敛？会静默覆盖吗？

### D3 契约一致性（三层对照）

- **后端 Pydantic ↔ 前端 `types.ts`**：逐模型对照（Project/话轮/audio_timeline/variant/output_version/job/capabilities），字段名、可空性、枚举值。建议写一次性对照脚本放 `.review/`。
- **实现 ↔ 01 §12 接口契约**：10 个路由的路径、方法、请求/响应形状、错误码逐条比对。
- **mock 能力声明 ↔ 实际行为**：`mode: mock` / `productionReady: false` 是否全链路一致（capabilities、任务结果 `simulated` 标记、渲染不登记假媒体、前端演示模式横幅）？有没有哪条路径仍可能把模拟产物标成真实？
- 文档单一权威：代码注释/文案里有没有另立口径（时长、双音轨、失效矩阵）？

### D4 数据完整性与恢复

- 重启恢复（`recovery.py` + 09 修复）：queued 重新入队、执行不明标 unknown、幂等记录恢复——**unknown 任务后续生命周期**（可查询吗？会永远占位吗？09 已声明无提供方查询适配器，核对边界是否如实暴露给用户）。
- 资产登记（`artifacts.py`）：`artifactIds` 悬空（07 P1-5）是否修复？哈希复用逻辑：同哈希复用是否校验文件真实存在（当前无实体文件，边界在哪）？
- 快照冻结：配音冻结工程+脚本快照、改写冻结脚本快照——快照存在哪、多大、会不会随任务数膨胀？
- 五分钟校验：后端拒绝超限与过期配音——「过期配音」判定条件是否与确认失效规则一致？

### D5 健壮性与安全（本地单用户，按「脏输入与坏环境」标准）

- **路径与编码**：仓库根定位（`config.py` parents[4]）、工程 ID 直接拼路径（越界 ID 如 `../x`？）、中文/空格路径、07 报 4.4 提到的 UTF-8 提交乱码（`?????` 工程）——API 层是否校验编码与字符集？
- 输入校验：负数/超量停顿（09 已加）、空话轮、超长输入、畸形 JSON 的报错质量。
- 资源泄漏：SSE 连接断开后订阅清理、任务协程取消后句柄、`events.jsonl` 无限增长、快照/历史无清理策略（记录为 P2 即可）。
- 密钥占位（keyring 关口 B）：确认当前无密钥明文落盘。
- 静态托管：`dist` 不存在时的占位行为；路径穿越（静态文件服务默认安全，核对挂载根）。

### D6 可维护性与测试缺口

- 依赖方向（06 §3.1 单向规则）是否被破坏（如 api 直接 import storage 细节、service 反向依赖 api）。
- 修订 ID 生成收敛到一处（07 P1-3）是否完成；重复的时长计算逻辑是否只有一个权威函数。
- 测试缺口清单：现有 22 项回归覆盖了什么、没覆盖什么（对照 D1–D5 每个维度给出「应有而未有」的测试用例建议，直接产出可执行的 pytest 骨架）。
- 前端：9 个页面有无重复的确认/轮询逻辑可收敛到 hooks。

---

## 3. 分轮执行计划

总工作量估计 **4–6 人日**。每轮有明确退出标准，打断可从下一轮续起。

### 轮 0 · 环境重建与基线复验（约 0.5 天）

本机三件套缺失（venv / node_modules / .review 均不存在），必须先建：

| 步骤 | 命令/动作 | 退出标准 |
|---|---|---|
| 0.1 | `cd apps/server && python -m venv .venv && .venv/Scripts/pip install -e . pytest` | `import duocast` 成功 |
| 0.2 | 启动后端（**务必按 HANDOFF 绕过本机代理**，`NO_PROXY=127.0.0.1`）；`GET /api/health` 200；`GET /` 返回 HTML（若报「前端未构建」先做 0.3） | 健康检查通过 |
| 0.3 | `cd apps/web && pnpm install && pnpm build` | dist 生成，8100 可整站访问 |
| 0.4 | `pytest apps/server/tests -q`（用 09 的可复跑命令） | 记录通过数基线（09 声称 22 项） |
| 0.5 | `python smoke_e2e.py`（后端起 8100 后） | ALL PASS |
| 0.6 | 通读 `test_optimization.py`，列出 22 项各自覆盖的 09 修复条目 | 形成「G1 已被自动回归覆盖 / 需人工复核」两张清单 |

### 轮 1 · 后端 A 类逐行深审（约 1–1.5 天）

按 1.1 的 A 级五组，**逐文件**过 D1–D5 问题清单。每个文件产出：证据行号 + 初步定级（P0/P1/P2/存疑）。重点专项：

1. **状态机转移矩阵**：把 `manager.py` 全部 `transition()` 调用点列成表（源态 × 事件 → 目标态），与 02 §7.1 权威状态机比对，找非法转移与死态。
2. **落盘竞态推演**：画 `project_store` 的写路径时序（API 写内存 → 防抖 → flush → 退出 flush），标出每个可中断点，推演进程崩溃各点的数据结局。
3. **样本数数学**：手工复算 `voice_svc` 的累计公式（含 gap/leadIn/tailOut），构造边界用例（0 单元、负 gap、溢出、非 48kHz）。
4. **SSE 序号**：模拟慢消费者 + 断线重连 + Last-Event-ID 三种时序，核对补发与去重。

### 轮 2 · 后端 B/C 类 + 契约对照（约 1 天）

- B 级模块按维度抽查；`api/` 十个路由逐个对照 01 §12。
- 写 `.review/contract-check.mjs`（或 Python 脚本）：从 Pydantic 模型与 `types.ts` 双向抽取字段做机器对照，人工只看差异。此脚本留作回归资产。
- 适配器接缝专项（G3）：`adapters/base.py` Protocol 的每个方法签名字段，核对 mock 返回结构与 04 契约的偏差；确认真实适配器需要实现哪些「mock 里被偷懒跳过」的义务（如真实 sampleCount、重采样、错误分类）。

### 轮 3 · 前端 A/B 类深审（约 1–1.5 天）

- `api.ts`/`sse.ts`/`App.tsx`/`stores` 逐行：错误传播链（任何 mutation 失败用户能否感知？）、Query 失效策略、工程切换清理（P1-7 修复复核）。
- `EditScriptPage`：历史版本编辑 baseRevId（P0-1 修复复核）、自动保存 409 处理（P0-2 修复复核）、候选改写采用流的竞态。
- `VoicePage`/`RenderPage`：确认按钮的守卫条件与后端确认失效规则是否同一口径；分段复用提示的诚实性。
- 顺带核对 AppShell 三条硬规则（HANDOFF「应用壳」节）未被本轮页面改动破坏。

### 轮 4 · 交叉验证与边界场景实测（约 1 天）

起真实服务，按场景表逐条执行并截图/录日志，产出到 `.review/`：

| # | 场景 | 步骤要点 | 期望 |
|---|---|---|---|
| S1 | 生成中强杀进程 | 脚本生成 running 时结束 uvicorn，重启 | 任务恢复或标 unknown，不凭空消失（对应 07 P0-4） |
| S2 | 防抖窗口内杀进程 | 保存话轮后 500ms 内 kill | 重启后内容不丢或可见地回退，不出现半写 JSON |
| S3 | 双客户端并发编辑 | 两个浏览器同时改同一工程保存 | 一方收到冲突提示，不静默覆盖（对应 P0-2） |
| S4 | 排队时改稿 | 发起配音后立即编辑脚本 | 合成用快照旧稿；完成后旧结果入历史不覆盖（09 声称项） |
| S5 | 暂停→改稿→继续 | pause 后编辑，resume | 不二次调用提供方，结果按 09 规则处置 |
| S6 | 五分钟边界 | 构造恰好 300s 与 300s+1 样本 | 后者被拒且报错可读，不截尾 |
| S7 | 断网/慢网 SSE | DevTools 断网 30s 再恢复 | 重连后界面状态与后端一致（resync 生效） |
| S8 | 中文与特殊字符 | 工程 ID/标题含中文、空格、引号 | 全链路不乱码、路径不炸 |
| S9 | 全 mock 演示诚实性 | 走完五阶段 | 无任何一步把模拟产物标成真实（productionReady/simulated 标记一致） |

### 轮 5 · 汇总与报告（约 0.5 天）

产出三件：

1. **`11-代码审查报告.md`**：沿用 07 的结构（结论速览 → P0 → P1 → P2 → 做得好的 → 修复路线）。G1 复核表（09 的 18 项 × 证实/部分证实/未证实）作为独立章节。
2. **`.review/` 复现脚本**：contract-check、S1–S9 中可脚本化的场景。
3. **修复路线建议**：分「演示前必须 / 真实提供方接入前必须 / 随迭代」三批，衔接 HANDOFF「下一步开工顺序」。

---

## 4. 缺陷分级与报告纪律

沿用 07 口径：

| 级别 | 定义 | 处置 |
|---|---|---|
| **P0** | 丢数据、状态永久错误、结果不可信（把假的当真的） | 报告单列，给出最小修复方案 |
| **P1** | 与 04/01/02 文档承诺不符、核心机制失真、可访问性硬伤 | 报告单列 |
| **P2** | 一致性、可维护性、误导性文案、资源缓慢增长 | 表格列出 |

纪律约定：

- **审查不改代码**——所有发现只记录，修复走下一轮（避免审查与修复互相污染）。
- 每个结论必须带 `文件:行号` + 复现/推演路径；推演得出的并发问题要标注「未实测，推演依据」。
- 存疑项单独放「待与作者确认」节，不硬定级。
- 09 已声明的「后续实施边界」7 条是**已知未做**，不再算缺陷；但若发现实现边界**大于**声明（声称未做实际做了半套）则要报。

---

## 5. 风险与注意

1. **本机代理陷阱**：所有本地请求前 `NO_PROXY="127.0.0.1,localhost,::1"`，出现 502 先查这个（HANDOFF 已记）。
2. **运行时数据**：`apps/storage/` 是 gitignored 的本地状态；测试与场景实测一律用 `DUOCAST_STORAGE` 指到临时目录，**不碰已有工程数据**。
3. **后端改 Python 代码必须重启**才生效；改 `config.py` 路径逻辑后必须验证 `GET /` 返回 HTML。
4. `.review/` 脚本在上台机器，本轮重建时以 07 §1.2 的记录为准，重建后应提交可入库的部分（或至少在报告中记录重建清单），避免下次换机再次丢失。
